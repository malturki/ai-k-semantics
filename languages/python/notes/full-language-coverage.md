# Python Full-Language Coverage Map

This file is the completeness ledger for Python 3.14.6. A construct is not considered complete until it has:

- direct syntax in K or a documented reason for a faithful desugaring
- semantic rules linked to the official docs
- local positive and negative tests
- CPython differential coverage, unless classified as intentionally non-CPython
- known ambiguity/divergence notes where relevant

Status values:

- `not-started`: no K syntax or semantics yet
- `syntax`: syntax exists, semantics incomplete
- `partial`: executable semantics exists for a documented subset
- `profile`: only covered in an implementation/profile module
- `complete`: docs-linked semantics and tests are in place

## Recent Construct Notes

- 2026-07-06: Extended the `NotImplemented` truth-testing slice through current protocol consumers using Python 3.14.6 truth value testing, comprehensions, and the Standard Library `all`, `any`, `filter`, `sorted`, `zip`, and `map` builtins as provenance. The current semantics raises the TypeError sentinel when `NotImplemented` is truth-tested by concrete-list/tuple/dict-key/set/frozenset and iterator-ref `all()`/`any()`, callable-sentinel iterator reducer results, `filter(None, ...)`, predicate-returning `filter(...)`, `sorted(..., reverse=...)`, `sorted(..., key=..., reverse=...)`, `zip(..., strict=...)`, `map(..., strict=...)`, and the current adapter-backed simple and recursive-target list/set/dict comprehension filter clauses plus materialized generator-expression filters. Covered behavior is in `tests/adapter/adapter-notimplemented-truth-consumers.py`. Remaining work includes exact TypeError message text, full generator laziness, arbitrary iterator/user object `__bool__`/`__len__` dispatch, mutation-sensitive partial consumption, broader multi-generator comprehension filter families beyond the explicitly guarded current profiles, and remaining bool-consuming library options.
- 2026-07-06: Added the `NotImplemented` singleton slice using Python 3.14.6 Built-in Constants, Data Model singleton behavior, expression truth-value testing, simple statement truth contexts, and the Standard Library `builtins` module docs as provenance. The current semantics models `NotImplemented` as a first-class singleton value with identity/self-equality, fixed hashability, `str`/`repr`/`ascii`/empty `format` output, shadowable builtin-name fallback, `builtins.NotImplemented`, `from builtins import NotImplemented`, `from builtins import *` rebinding, and TypeError truth testing for `bool()`, `not`, `and`/`or`, conditional expressions, `if`, `while`, and `assert`. It also covers current `float(NotImplemented)` and `complex(NotImplemented)` TypeError profiles. Covered behavior is in `tests/adapter/adapter-notimplemented-singleton.py`. Remaining work includes exact TypeError message text, `types.NotImplementedType`, binary special-method dispatch returning `NotImplemented`, and full object/type hierarchy integration.
- 2026-07-06: Added an explicit adapter-backed execution-context/frame isolation slice for Python Language Reference chapter 4 Execution model. The current semantics uses the `<env>` cell plus `restoreEnv` continuations for supported function calls, temporary class-body namespaces for supported class definitions, and saved-environment comprehension iterators for supported comprehension scopes. Covered behavior is in `tests/adapter/adapter-execution-context-frames.py`, including function local non-leakage, function reads of module globals, class-body global reads and class-local non-leakage, and list-comprehension target restoration. Remaining work includes real frame objects, locals/globals/builtins mappings, closures/cells/free variables, frame introspection and traceback integration, exact `UnboundLocalError`, annotation scopes, generator/coroutine suspended frames, and `exec`/`eval` locals semantics.
- 2026-07-06: Added the first adapter-backed source-encoding slice for Python Language Reference 2.1.4 Encoding declarations. The adapter now reads raw source bytes and lets CPython `ast.parse`/`compile` perform documented source decoding before AST-to-K lowering, rather than forcing UTF-8 text up front. Covered behavior is in `tests/adapter/adapter-source-encoding-cookie.py`, which distinguishes a Latin-1 coding cookie from UTF-8 pre-decoding. Remaining work includes a native K tokenizer-level encoding model, UTF-8 BOM handling and BOM/cookie conflicts, invalid-encoding diagnostics, exact source spans, and non-adapter parser coverage.
- 2026-07-06: Added the first fixed `builtins` module slice using Python 3.14.6 Built-in Constants, Built-in Functions, the Language Reference import system, and the Standard Library `builtins` module docs as provenance. The supported fixed module can be imported as `builtins`, returned by `__import__("builtins")` and `importlib.import_module("builtins")`, exposes `__name__`, `__import__`, `Ellipsis`, `NotImplemented`, `__debug__`, and string-keyed `None`/`True`/`False`, supports selected from-imports, missing-name `ImportError`, public star-import rebinding for `Ellipsis` and `NotImplemented` while preserving underscore-name exclusion for `__import__`, and reuses env-reachable module snapshots after mutation. Covered behavior is in `tests/adapter/adapter-builtins-module.py` and `tests/adapter/adapter-notimplemented-singleton.py`. Remaining work includes the full builtins namespace as first-class values, built-in function/type/exception objects, exact module dictionary/star-import behavior for keyword-spelled names, true `sys.modules`, builtins injection into global namespaces, and exact CPython diagnostics/introspection.
- 2026-07-06: Added the first built-in `__import__` fixed-module profile using Python 3.14.6 Built-in Functions `__import__`, the Language Reference import system, and the Standard Library `importlib` docs as provenance. The shadowable builtins-name fallback exposes callable `__import__`, the fixed `importlib` module exposes callable `__import__`, and calls support the current fixed-module absolute-name subset for `math`, `keyword`, and `importlib` through direct, positional-argument-list, keyword-only, and mixed positional/keyword call paths, including ignored `globals`/`locals`/`fromlist` in the covered profile, level-0 absolute imports, env-reachable module-cache reuse after mutation, and current sentinel exceptions for dotted non-package names, unsupported names, empty or relative names, non-string names, wrong arity, duplicate supplied arguments, unknown keywords, negative levels, and non-int-like levels. Covered behavior is in `tests/adapter/adapter-import-builtin-dunder.py`. Remaining work includes a real `builtins` module, exact CPython diagnostics and C/Python function identity, positive package/dotted imports, true `sys.modules`, `globals`/`locals` package-context handling, arbitrary `fromlist` behavior, import hooks/finders/loaders/specs, and the importlib/builtins test-suite extraction.
- 2026-07-06: Added explicit `importlib.import_module` dotted absolute-name failure coverage for the current non-package fixed-module profile. Dotted names such as `"math.foo"` and `"not_a_real_module_for_k_semantics.child"` now have a distinct K predicate/rule path to the current `ModuleNotFoundError` sentinel, including the two-argument absolute-name profile. Covered behavior is in `tests/adapter/adapter-importlib-import-module.py`. Remaining work includes positive dotted package loading, `ModuleSpec`, real `sys.modules`, finder/loader traversal, package anchors for relative names, and exact diagnostics.
- 2026-07-06: Added adapter-backed absolute dotted import failure coverage for the current non-package fixed-module profile. The adapter lowers `import math.foo`, `import math.foo as alias`, `from math.foo import name`, `from math.foo import name as alias`, and `from math.foo import *` to string-preserving dotted import forms, and the current semantics raises the `ModuleNotFoundError` sentinel before binding the root, alias, imported name, or star-imported names. Covered behavior is in `tests/adapter/adapter-dotted-import-failures.py`. Remaining work includes positive dotted package imports, package binding rules, import-time package execution, `sys.modules` interactions, and exact diagnostics.
- 2026-07-06: Added a CPython-observed `module.__all__` range sequence slice for supported fixed-module star imports. Empty `range` values now behave as empty `__all__` sequences and import nothing; non-empty ranges continue through the current TypeError path because their indexed items are integers, matching the observable no-binding behavior for the covered cases. Covered behavior is in `tests/adapter/adapter-from-import-star-all.py`. Remaining work includes custom sequence objects with `__getitem__`, exact TypeError diagnostics, arbitrary string-to-global binding, package/user-module star imports, and real module dictionaries.
- 2026-07-06: Extended fixed-module star import to cover the current identifier-shaped string-key dynamic module attributes used by the adapter profile. In the no-`__all__` path, `from math import *` now imports supported public string-key attributes such as `dynamic` set by `setattr(math, "dynamic", value)` while still filtering underscore-prefixed string keys in the covered profile. In the explicit `__all__` path, supported string-key names such as `dynamic` are imported when named by `keyword.__all__`. Covered behavior is in `tests/adapter/adapter-from-import-star-fixed-modules.py` and `tests/adapter/adapter-from-import-star-all.py`. Remaining work includes arbitrary string-to-global binding for keys that are not K identifiers, a general identifier recognizer/string-to-Id bridge, arbitrary iterable `__all__` objects, package/user-module star imports, exact diagnostics, and real module dictionaries.
- 2026-07-06: Added a first `module.__all__`-driven star-import slice for supported fixed modules using Python 3.14.6 Language Reference 7.11 The import statement public-name rules as provenance. When a module has a supported string-list or tuple `__all__`, `#fromImportStar` now binds those known fixed-module names in order; missing string names raise the current `AttributeError` sentinel, non-string items raise the current `TypeError` sentinel, and earlier `__all__` names remain bound before a later failure, matching CPython's partial-binding behavior for the covered cases. Covered behavior is in `tests/adapter/adapter-from-import-star-all.py`, including runtime reassignment of `keyword.__all__`, importing only listed names, tuple and empty-string `__all__`, and missing/non-string failure entries. Remaining work includes arbitrary string-to-identifier/global binding, custom sequence-object `__all__` handling, exact diagnostics, package/user-module star imports, and real module dictionaries.
- 2026-07-06: Extended the adapter-backed top-level explicit relative-import failure profile to comma-separated, star, and dotted-module relative from-import clauses. The adapter lowers non-star relative import clauses to `#fromRelativeImport`/`#fromRelativeImportAs`, relative star imports to `#fromRelativeImportStar`, and dotted relative module paths to string-preserving `#fromRelativeDottedImport*` forms; the current script-without-package semantics raises the `ImportError` sentinel before later clauses execute. Covered behavior is in `tests/adapter/adapter-relative-import-no-package.py`, including `from . import a, b as c`, `from .module import x, y`, `from . import *`, `from .module import *`, and `from .pkg.sub import x`/`*` in the no-package profile. Remaining work includes positive package-relative resolution, package context metadata, user package execution, and exact diagnostics.
- 2026-07-06: Added adapter-backed comma-separated fixed-module import clauses using Python 3.14.6 Language Reference 7.11 The import statement as provenance. The adapter now lowers `import a, b as c` and `from module import x, y as z` into the same sequence of clause-level K import statements the reference docs specify, preserving left-to-right partial binding before later `ImportError` in the current supported fixed-module profile. Covered behavior is in `tests/adapter/adapter-import-multi-clause.py`, including mixed import aliases, multi-name from-import aliases, dynamic module attributes, `importlib.import_module` cache reuse, and partial successful binding before a later missing from-import name. Remaining work includes dotted/package imports, exact/package/relative star-import semantics, user-module execution, exact diagnostics, and real `sys.modules` behavior.
- 2026-07-06: Added adapter-backed fixed-module `from module import *` coverage using Python 3.14.6 Language Reference 7.11 The import statement and 5 The import system as provenance. The adapter now lowers absolute star imports for supported fixed modules to `#fromImportStar(M)`, and K imports public identifier-backed attributes from the current env-reachable mutated module snapshot or the fixed builtin module value. Covered behavior is in `tests/adapter/adapter-from-import-star-fixed-modules.py`, including `math` constants, dynamic public module attributes, underscore-name exclusion, `keyword` public names, and `importlib.import_module`. Remaining work includes exact `__all__` iteration semantics, string-key dynamic module attributes, errors for missing names named by `__all__`, dotted/package star imports, relative star imports, user-module execution, exact diagnostics, and real `sys.modules` behavior.
- 2026-07-06: Expanded adapter-backed fixed-module `from ... import name` coverage to allow arbitrary simple imported names for supported fixed modules instead of a hardcoded per-module attribute allowlist. K semantics now decides whether the current env-reachable module snapshot contains the requested name or raises the current `ImportError` sentinel. Covered behavior is in `tests/adapter/adapter-from-import-dynamic-module-attrs.py`, including `from math import answer` after runtime module mutation and `ImportError` after deletion or missing names. Remaining work includes broader multi-name imports outside the fixed-module profile, exact/package/relative star-import semantics, dotted/module-package imports, user-module execution, exact diagnostics, and full `sys.modules` behavior.
- 2026-07-06: Extended the fixed-module cache slice using Python 3.14.6 Language Reference import system module cache behavior and Standard Library `importlib.import_module` as provenance. Future supported fixed-module `import`, `import ... as ...`, `from module import name`, and `importlib.import_module` now reuse a current env-reachable `moduleVal` snapshot after module attribute mutation/deletion, including current direct env aliases and list/tuple/dict-value-held module aliases. Covered behavior is in `tests/adapter/adapter-module-import-cache-mutation.py`, including mutated `math.pi`, dynamic module attributes, deletion visibility, and `from math import pi` `ImportError` after deletion. Remaining work includes a real `sys.modules` table independent of environment reachability, initialization/in-progress module states, user module execution, package/module specs, broader module metadata, deterministic cache identity, failed-import cache behavior, importlib/sys integration, and exact diagnostics.
- 2026-07-06: Added a first already-imported fixed-module attribute mutation/deletion slice using Python 3.14.6 Language Reference import system, attribute references, assignment statements, and `del`, Python Data Model module objects, and Built-in Functions `getattr`/`hasattr`/`setattr`/`delattr` as provenance. Current `moduleVal` values support direct `module.attr = value`, `del module.attr`, `setattr(module, "attr", value)`, and `delattr(module, "attr")` for fixed supported module values already reachable from the current environment, including aliases and values nested in current concrete list, tuple, and dictionary values. The sweep intentionally updates dictionary values but not keys, mirroring the current class-value identity profile. Covered behavior is in `tests/adapter/adapter-module-attribute-mutation.py`. Remaining work includes a real `sys.modules` table, mutation visibility for future imports and `importlib.import_module`, user module objects, package/module specs, broader module metadata, arbitrary protocol-held aliases, heap-backed identity, and exact diagnostics.
- 2026-07-05: Added a first container-held class attribute mutation/deletion slice using Python 3.14.6 Language Reference assignment, `del`, class definitions, object identity, Data Model attribute lookup/assignment/deletion, and Built-in Functions `setattr`/`delattr` as provenance. The current environment sweep for runtime class mutation/deletion now recursively updates class and user-object snapshots held in current concrete list values, tuple values, and dictionary values, including nested combinations; dictionary keys are intentionally not rewritten in this slice because Python class identity should not depend on mutating structural key payloads. Covered behavior is in `tests/adapter/adapter-class-container-attribute-mutation.py`. Remaining work includes heap-backed object identity, aliasing in arbitrary containers/protocol objects, dict keys/sets under an identity-preserving heap model, module attributes, descriptors/metaclasses/type/object integration, multiple inheritance/C3 MRO, and exact diagnostics.
- 2026-07-05: Added a first single-base inherited runtime class attribute mutation/deletion slice using Python 3.14.6 Language Reference assignment, `del`, class definitions, Data Model attribute lookup/assignment/deletion, and Built-in Functions `setattr`/`delattr` as provenance. Current base-class runtime assignment/deletion propagates through the copied single-base descendant class tails used by the current user-class profile; subclass assignment shadows inherited attributes instead of mutating the base tail, subclass deletion of inherited-only attributes raises the current `AttributeError` sentinel, and direct dot plus `setattr`/`delattr` are covered. Covered behavior is in `tests/adapter/adapter-class-inherited-attribute-mutation.py`. Remaining work includes true heap-backed class identity, broader container/protocol-held class values beyond current list/tuple/dict values, multiple inheritance, C3 MRO, descriptors/metaclasses/type/object integration, module attributes, and exact diagnostics.
- 2026-07-05: Added a first runtime class attribute mutation/deletion slice using Python 3.14.6 Language Reference assignment, `del`, class definitions, Data Model attribute customization, and Built-in Functions `setattr`/`delattr` as provenance. Current class values support direct `C.x = value`, `del C.x`, `setattr(C, "x", value)`, and `delattr(C, "x")` for exact class identities reachable from the current environment, including class aliases and direct env-bound instances with the same class id; instance attribute shadowing is preserved and missing class deletion raises the current `AttributeError` sentinel. Covered behavior is in `tests/adapter/adapter-class-attribute-mutation.py`. Remaining work includes full MRO-sensitive class mutation and deletion, class values stored inside containers or nested runtime data, module attributes, descriptors/metaclasses/type/object integration, full heap object identity, and exact diagnostics.
- 2026-07-05: Added focused inherited attribute-hook coverage for the current single-base user-class profile using Python 3.14.6 Data Model customization of attribute access, assignment, deletion, and class inheritance as provenance. Because current `#classBase` construction appends inherited base attributes after child attributes, inherited `__getattribute__`, `__getattr__`, `__setattr__`, `__delattr__`, and property getter fallback now have explicit CPython-differential smoke coverage in `tests/adapter/adapter-class-inherited-attribute-hooks.py`, including child override precedence for `__getattr__`, direct `object.__getattribute__` bypass of fallback, inherited `object.__setattr__`/`object.__delattr__` delegation, and non-`AttributeError` propagation. Remaining work includes rebinding-sensitive lookup after class mutation, multiple inheritance/MRO, builtin bases, metaclasses, exact special-method lookup bypass details, and real builtin `object`/`type` behavior.
- 2026-07-05: Added a first property-getter `AttributeError` fallback slice using Python 3.14.6 Data Model customization of attribute access and descriptor invocation as provenance. Current user-object direct attribute lookup, `getattr()`, `getattr(..., default)`, and `hasattr()` now treat `AttributeError` raised by the current class-body `@property` getter profile like a failed lookup: `__getattr__` is tried when present, `getattr(..., default)` returns the default after fallback `AttributeError`, and `hasattr()` returns `False`; non-`AttributeError` getter exceptions still propagate. Covered behavior is in `tests/adapter/adapter-class-property-getattr-fallback.py`. Remaining work includes full descriptor protocol fallback beyond the current property profile, descriptor precedence, exact diagnostics, and real builtin `object`/`type` integration.
- 2026-07-05: Added a first user-defined `__getattribute__` dispatch slice using Python 3.14.6 Data Model customization of attribute access as provenance. Current user-object direct attribute lookup, `getattr()`, `getattr(..., default)`, and `hasattr()` now dispatch to a class-stored `__getattribute__` method before the current default user-object lookup profile; if the hook raises `AttributeError`, the current `__getattr__` fallback/default/hasattr behavior is applied, while non-`AttributeError` exceptions propagate. Covered behavior is in `tests/adapter/adapter-class-getattribute-method.py`, including hook-computed attributes, delegation through explicit `object.__getattribute__`, instance/class attribute access after delegation, `__getattr__` fallback after hook `AttributeError`, default return after fallback `AttributeError`, `hasattr()` truth/failure behavior, and non-`AttributeError` propagation. Remaining work includes inherited and rebinding-sensitive `__getattribute__` lookup details, full descriptor precedence, real builtin `object`/`type` behavior, exact diagnostics, and broader object-model integration.
- 2026-07-05: Added a first explicit `object.__getattribute__` base-lookup slice using Python 3.14.6 Data Model customization of attribute access as provenance. The adapter lowers direct `object.__getattribute__(obj, name)` calls to an internal form that evaluates receiver and name in call-argument order, requires a string name, bypasses current `__getattr__`, and reuses the current default user-object instance/class/property lookup profile. Covered behavior is in `tests/adapter/adapter-object-getattribute-delegation.py`, including instance attributes, class attributes, bound user methods, property getters, non-string-name `TypeError`, and bypassing `__getattr__` for missing names. Remaining work includes inherited/rebinding-sensitive `__getattribute__` lookup details, real builtin `object`/`type` objects, exact diagnostics, and full descriptor/object-model integration.
- 2026-07-05: Added a first user-defined `__getattr__` fallback slice using Python 3.14.6 Data Model customization of attribute access as provenance. Current user-object direct attribute lookup, `getattr()`, `getattr(..., default)`, and `hasattr()` now invoke the class attribute profile for `__getattr__` only after the current default instance/class lookup fails; `getattr(..., default)` returns the default when the fallback raises `AttributeError`, and `hasattr()` returns `False` only for fallback `AttributeError` while propagating other fallback exceptions. Covered behavior is in `tests/adapter/adapter-class-getattr-method.py`, including normal lookup bypassing `__getattr__`, computed fallback values, default handling, `hasattr` behavior, and default-argument evaluation before lookup. Remaining work includes inherited/rebinding-sensitive `__getattribute__` details, exact special-method lookup details, real builtin `object.__getattribute__`, full descriptor protocol, exact diagnostics, and broader object-model integration.
- 2026-07-05: Added a first explicit `object.__setattr__`/`object.__delattr__` base-delegation slice using Python 3.14.6 Data Model customization of attribute assignment/deletion as provenance. The adapter lowers direct `object.__setattr__(obj, name, value)` and `object.__delattr__(obj, name)` calls to internal forms that evaluate receiver/name/value in the current call-argument order profile, require string names, bypass user-defined `__setattr__`/`__delattr__`, and reuse the current property setter/deleter plus instance-store default mechanism for user objects. Covered behavior is in `tests/adapter/adapter-object-attribute-delegation.py`, including custom hook delegation, deliberate deletion suppression before delegation, direct base calls, non-string-name `TypeError`, and property setter/deleter delegation. Remaining work includes real builtin `object`/`type` objects, rebinding-sensitive lookup, inherited/special lookup details, class and module attribute mutation/deletion, full descriptor protocol, exact diagnostics, and broader object-model integration.
- 2026-07-05: Added a first user-defined `__delattr__` dispatch slice using Python 3.14.6 Data Model customization of attribute deletion as provenance. Current direct user-object attribute deletion and builtin `delattr()` now check the class attribute profile for `__delattr__` before the default property/instance-store path, bind it as a method, and pass the receiver plus attribute-name string; the method return is ignored through the existing statement/`delattr()` completion markers. Covered behavior is in `tests/adapter/adapter-class-delattr-method.py`, including exception propagation from `__delattr__` and suppression of default instance deletion when the method returns normally. Remaining work includes inherited/special lookup details, broader base-object behavior, class and module deletion, interaction with full descriptor precedence, exact diagnostics, and method-global/class-method scope gaps.
- 2026-07-05: Added a first user-defined `__setattr__` dispatch slice using Python 3.14.6 Data Model customization of attribute assignment as provenance. Current direct user-object attribute assignment and builtin `setattr()` now check the class attribute profile for `__setattr__` before the default property/instance-store path, bind it as a method, and pass the receiver, attribute-name string, and value; the method return is ignored through the existing statement/`setattr()` completion markers. Covered behavior is in `tests/adapter/adapter-class-setattr-method.py`, including exception propagation from `__setattr__` and suppression of default instance storage when the method returns normally. Remaining work includes inherited/special lookup details, broader base-object behavior, class and module assignment, interaction with full descriptor precedence, exact diagnostics, and method-global/class-method scope gaps.
- 2026-07-05: Added an adapter-backed `setattr(object, name, value)` builtin slice using Python 3.14.6 Standard Library Built-in Functions `setattr` and the Data Model attribute-assignment/descriptor profile as provenance. The adapter lowers three-argument `setattr` calls to a distinct `#setattr` expression that evaluates object, name, and value in Python call-argument order; the semantics requires a string name after all arguments evaluate, returns `None` on success, stores current user-object instance attributes through a new string-keyed runtime attribute entry, makes identifier-like string attributes visible to direct dot lookup/deletion, supports arbitrary string names through dynamic `getattr`/`hasattr`, shadows non-property class attributes, dispatches current same-name class-body property setters, and preserves read-only `AttributeError` plus current non-string-name `TypeError`. Covered behavior is in `tests/adapter/adapter-setattr-builtin.py`. Remaining work includes class and module attribute assignment, broader `__setattr__` behavior including `object.__setattr__`, full descriptor `__set__`, exact diagnostics, and aliasing/object-identity effects outside the current user-object attribute store.
- 2026-07-05: Added an adapter-backed `delattr(object, name)` builtin slice using Python 3.14.6 Standard Library Built-in Functions `delattr` and the Data Model attribute-deletion/descriptor profile as provenance. The adapter lowers two-argument `delattr` calls to a distinct `#delattr` expression that evaluates the object before the name expression; the semantics requires a string name, returns `None` on success, deletes current user-object instance attributes with class fallback, dispatches current same-name class-body property deleters, preserves read-only/missing `AttributeError`, and raises the current `TypeError` sentinel for non-string names. Covered behavior is in `tests/adapter/adapter-delattr-builtin.py`. Remaining work includes class and module attribute deletion, `__delattr__` dispatch, non-identifier and dunder-name edge cases beyond the current class-attribute string matcher, full descriptor `__delete__`, exact diagnostics, and aliasing/object-identity effects outside the current user-object attribute store.
- 2026-07-05: Added an adapter-backed file-input framing smoke case for `top.file-input` using Python 3.14.6 Top-level Components "Complete Python programs" and "File input" as provenance. The case runs through normal `--mode exec` parsing with `ast.parse(..., mode='exec')` and covers shebang/comment/coding-line comments, blank lines, a compound statement, semicolon-separated simple statements, and final expression framing before `ENDMARKER` within the current executable subset. Remaining work includes native or verified full `file_input`, invalid encoding diagnostics, exact newline/EOF behavior, source spans, `__main__`/builtins/sys initialization, and all syntax/semantic constructs outside the adapter subset.
- 2026-07-05: Added a first CPython PEG grammar profile using Python 3.14.6 `Grammar/python.gram` and `Doc/reference/grammar.rst` as pinned provenance. The profile records SHA-256 hashes for both files, a 249-rule grammar inventory, 61 `invalid_*` second-pass diagnostic rules, the four CPython start rules (`file`, `interactive`, `eval`, `func_type`), and the current parser-oracle boundary where `harness/python_to_k_input.py` relies on `ast.parse(...)` before lowering supported AST constructs to K. The local CPython reference run `./python -m test --single-process --timeout 240 test_grammar test_ast test_tokenize test_syntax test_codeop` passed with 474 tests run and 2 skipped. Remaining work is a native K parser or verified frontend that preserves every official construct, token/span, soft keyword, invalid-rule diagnostic, and concrete syntax distinction needed by semantics.
- 2026-07-05: Added a first CPython bytecode-internals profile using Python 3.14.6 `dis`, code-object, `marshal`, `py_compile`, `compileall`, and importlib bytecode-loader docs plus CPython 3.14.6 `Lib/dis.py`, `Lib/opcode.py`, `Python/bytecodes.c`, `Python/compile.c`, `Python/ceval.c`, and `Objects/codeobject.c` as provenance. Current coverage records the portable source-semantics boundary: opcode selection, inline caches, adaptive specialization, code-object layout, marshal format, `.pyc` cache generation, and eval-loop dispatch stay out of portable Python unless an explicit CPython implementation profile is selected. The CPython classification ledger now tracks `test_dis`, `test_codeop`, `test_compiler_codegen`, `test_compiler_assemble`, `test_marshal`, `test_py_compile`, and `test_compileall`, and the local CPython reference run `./python -m test --single-process --timeout 240 test_dis test_compile test_codeop test_compiler_codegen test_compiler_assemble test_marshal test_py_compile test_compileall` passed with 601 tests run and 14 skipped.
- 2026-07-05: Added a first CPython resource-limits profile using Python 3.14.6 Language Reference implementation notes, `sys` recursion and integer-string-conversion limit docs, the Unix `resource` module docs, and CPython 3.14.6 `Lib/test/test_sys.py`, `Lib/test/test_int.py`, and `Lib/test/test_resource.py` as provenance. Current coverage classifies implementation-note boundaries and optional-extension/environment availability separately from portable language semantics, while recursion limits, `int_max_str_digits`, command-line/environment limit profiles, process resource APIs, and host resource limits are explicit future profile work. The validator now checks the resource-limits profile against the CPython test classification ledger, and the local CPython reference run `./python -m test --single-process --timeout 180 test_sys test_int test_resource` passed with 162 tests run and 11 skipped.
- 2026-07-05: Added a first CPython diagnostics normalization profile using CPython 3.14.6 `Lib/test/test_syntax.py`, `Lib/test/test_traceback.py`, `Lib/test/test_exceptions.py`, and `Lib/test/test_compile.py` as upstream diagnostic provenance. The current executable level is `exception-class`: adapter diagnostics tests compare raised exception categories through the current K sentinel/exception-instance profile, while exact message text, source locations, warning filtering, traceback object state, and rendered traceback text are explicitly future normalization levels. The validator now checks the diagnostics profile against `adapter-smoke.json`, and the CPython classification ledger tracks `test_syntax` and `test_traceback` as diagnostic-only upstream suites.
- 2026-07-05: Added the first non-generic `type` statement and lazy type alias slice using Python 3.14.6 Language Reference 4.2.3 Annotation scopes, 4.2.4 Lazy evaluation, and 7.14 The type statement as provenance. The adapter lowers simple-name `type Alias = expr` statements to `#typeAlias` without evaluating the RHS; the semantics binds a fresh `typeAliasVal` object, exposes current `__name__`, `__module__`, `__type_params__`, and `__parameters__` metadata, evaluates `__value__` lazily through direct attribute access and `getattr`, caches the value after first evaluation, and compares aliases by allocated identity. Covered behavior is in `tests/adapter/adapter-type-alias-lazy.py`. Remaining work includes generic type aliases, function/class type parameters, real annotation scopes and local closure capture, lazy annotations and TypeVar bounds/defaults, exact `typing.TypeAliasType` introspection, `hasattr` lazy-evaluation behavior, annotation-scope syntax restrictions, and exact diagnostics.
- 2026-07-05: Added a fixed builtin `keyword` module slice using pinned CPython 3.14.6 `Lib/keyword.py`, generated from `Grammar/python.gram`, as provenance. The supported module exposes `__name__`, `__all__`, `kwlist`, `softkwlist`, callable `iskeyword`, and callable `issoftkeyword`; the keyword and soft-keyword lists match the pinned reference profile, non-string membership probes return `False`, and wrong positional arity raises the current `TypeError` sentinel. Covered behavior is in `tests/adapter/adapter-import-keyword.py`. Remaining work for this target includes `ast`, `token`, `tokenize`, token constants/exact-token mappings, byte/source tokenization behavior, parser-oracle boundaries, keyword function metadata/introspection, import mutation/cache behavior, and exact diagnostics.
- 2026-07-05: Added the first package-relative import slice using Python 3.14.6 Language Reference 5.7 Package Relative Imports and CPython 3.14.6 top-level script behavior as provenance. The adapter lowers explicit relative `from` imports to distinct `#fromRelativeImport`/`#fromRelativeImportAs`/`#fromRelativeImportStar` and dotted-module `#fromRelativeDottedImport*` statements that preserve relative level, optional simple or dotted module path, imported name or star, and alias; the semantics raises the current `ImportError` sentinel for top-level relative imports with no known parent package. Covered behavior is in `tests/adapter/adapter-relative-import-no-package.py`. Remaining work includes real `__package__`/`__path__`/`__spec__`, parent package loading, regular and namespace package objects, positive relative resolution, filesystem/import-path profiles, `sys.modules` interaction, and exact diagnostics.
- 2026-07-05: Added the first `importlib` slice using Python 3.14.6 Standard Library `importlib.import_module` as provenance. The supported fixed `importlib` module exposes `__name__` and callable `import_module`; `import_module` returns the current fixed `math`, `keyword`, or `importlib` module values for supported absolute names, accepts an ignored second package argument for absolute names, raises the current `ModuleNotFoundError` sentinel for unsupported nonempty absolute names including the current explicit dotted-name profile, raises the current `ValueError` sentinel for empty names, raises the current `TypeError` sentinel for relative names without a package anchor, raises the current `AttributeError` sentinel for non-string names in the CPython profile, and raises the current `TypeError` sentinel for wrong arity. Covered behavior is in `tests/adapter/adapter-importlib-import-module.py`. Remaining work includes `importlib.__import__`, `invalidate_caches`, `reload`, `abc`, `machinery`, `util`, `ModuleSpec`, real `sys.modules` and finder/loader integration, positive package-relative resolution, filesystem/namespace/package profiles, exact diagnostics, and importlib test-suite extraction.
- 2026-07-05: Added the first adapter-backed `async def` slice using Python 3.14.6 Language Reference 8.9.1 Coroutine function definition and the data-model function metadata profile as provenance. Supported top-level async function declarations now lower to a distinct `#asyncDef` statement and bind `functionMetaVal` wrappers around `asyncFunctionVal`, preserving the body and exposing current `callable()`, `__name__`, `__qualname__`, `__module__`, and `__doc__` observations. The semantics does not yet allocate coroutine objects on call or execute/await coroutine bodies. Covered behavior is in `tests/adapter/adapter-async-def-metadata.py`. Remaining work includes coroutine object creation/execution, `await`, async generators, `async for`, `async with`, decorators, defaults, positional-only/varargs/keyword-only/kwargs signatures, type parameters, annotation metadata, and exact diagnostics.
- 2026-07-05: Added adapter-backed top-level function metadata for the current function-definition profile using Python 3.14.6 Language Reference 8.7 Function definitions as provenance. Supported top-level function definitions now bind `functionMetaVal` wrappers around the existing `functionVal...` and current simple `generatorFunctionVal` representations, initialize `__name__` and `__qualname__` from the function identifier, `__module__` as `"__main__"`, and `__doc__` from an initial function-body string literal or `None`. The wrapper delegates positional, keyword-only, and mixed calls to the underlying function value and supports direct lookup plus `getattr`/`hasattr` over `"__name__"`, `"__qualname__"`, `"__doc__"`, and `"__module__"`. Covered behavior is in `tests/adapter/adapter-function-metadata.py`. Remaining work includes mutable function attributes and object identity beyond the current wrapper profile, nested/local `__qualname__`, modules beyond the current `__main__` profile, decorator-time function metadata and decorated-function docstring metadata, closures/cells/frames, annotation metadata and annotation scopes, type parameters, descriptors/method binding, exact diagnostics, and real lazy generator frame/resume behavior.
- 2026-07-04: Added adapter-backed class metadata initialization for the current class-definition profile using Python 3.14.6 Language Reference 8.8 Class definitions as provenance. Supported class definitions now initialize class namespaces with `__module__ = "__main__"`, top-level `__qualname__ = class name`, and `__doc__` from the first class-body string literal or `None`. The metadata entries are modeled as ordinary class attributes, are visible to later supported class-body assignments and method-default expressions, can be overridden by later simple assignments, and are stored on pass-only, single-base, and decorated current-profile classes. Direct lookup plus `getattr`/`hasattr` over `"__doc__"`, `"__module__"`, and `"__qualname__"` are covered by `tests/adapter/adapter-class-metadata.py`. Remaining work includes full namespace mapping behavior, nested/function-local `__qualname__`, modules beyond the current `__main__` profile, metaclasses, full MRO, broader class-body execution, and exact diagnostics.
- 2026-07-04: Extended the adapter-backed user class method profile to ordinary, `@staticmethod`, and `@classmethod` class-body methods combining positional-only parameters with `*args`, `**kwargs`, `*args` plus `**kwargs`, `*args` plus keyword-only parameters, `*args` plus keyword-only parameters plus `**kwargs`, and keyword-only parameters plus `**kwargs`, including optional positional suffix defaults and sparse keyword-only defaults, using Python 3.14.6 Language Reference 8.8 Class definitions, 8.7 Function definitions parameter-list/default-parameter semantics, and the Standard Library `staticmethod`/`classmethod` descriptors as provenance. The adapter lowers simple undecorated methods to the `#classMethodPosOnly...` constructor family, simple `@staticmethod` methods to the `#classStaticMethodPosOnly...` family, and simple `@classmethod` methods to the `#classClassMethodPosOnly...` family. The semantics evaluates positional defaults and then keyword-only defaults in the active class namespace, stores the matching `functionValPosOnly...` value directly or through staticmethod/classmethod wrappers, and reuses the positional-only varargs/kwargs call paths. Covered behavior includes positional-only `__init__` methods with `*args`, keyword-only parameters, and `**kwargs`, instance-bound calls, direct class function calls, inherited methods, dynamic `getattr`, static methods, class methods with subclass owner binding, decorated classes and decorated base classes, default capture before later class-body rebinding, keyword collection excluding fixed and keyword-only parameters, and current `TypeError` handling for missing positional-only or keyword-only parameters. This removes the previous positional-only-with-varargs/kwargs class-method signature gap; remaining class-method work is decorated method signatures beyond the staticmethod/classmethod/property profile, exact diagnostics/introspection, and full descriptor/MRO/metaclass interactions.
- 2026-07-04: Extended the adapter-backed user class method profile to ordinary, `@staticmethod`, and `@classmethod` class-body methods combining ordinary positional parameters, `*args`, keyword-only parameters, and `**kwargs`, including optional positional suffix defaults and sparse keyword-only defaults, using Python 3.14.6 Language Reference 8.8 Class definitions, 8.7 Function definitions parameter-list/default-parameter semantics, and the Standard Library `staticmethod`/`classmethod` descriptors as provenance. The adapter lowers simple undecorated methods to `#classMethodVarArgsKwOnlyKwArgs(...)`/`#classMethodVarArgsKwDefaultsKwArgs(...)`, simple `@staticmethod` methods to `#classStaticMethodVarArgsKwOnlyKwArgs(...)`/`#classStaticMethodVarArgsKwDefaultsKwArgs(...)`, and simple `@classmethod` methods to `#classClassMethodVarArgsKwOnlyKwArgs(...)`/`#classClassMethodVarArgsKwDefaultsKwArgs(...)`. The semantics evaluates positional defaults and then keyword-only defaults in the active class namespace, stores `functionValVarArgsKwOnlyKwArgs(...)` or `functionValVarArgsKwDefaultsKwArgs(...)` directly or through staticmethod/classmethod wrappers, makes those values bindable as user methods, and reuses the mixed varargs-plus-keyword-only-plus-kwargs call path. Covered behavior includes keyword-only `__init__` with `*args` and `**kwargs`, instance-bound calls, direct class function calls, inherited methods, dynamic `getattr`, static methods including no ordinary positional parameters, class methods with subclass owner binding, decorated classes and decorated base classes, default capture before later class-body rebinding, `**kwargs` collection excluding fixed and keyword-only parameters, and current `TypeError` handling for missing required keyword-only parameters and duplicate positional/keyword binding. Remaining method-signature work includes positional-only combinations with varargs/kwargs, decorated method signatures beyond the staticmethod/classmethod/property profile, exact diagnostics/introspection, and full descriptor/MRO/metaclass interactions.
- 2026-07-04: Extended the adapter-backed user class method profile to ordinary, `@staticmethod`, and `@classmethod` class-body methods combining ordinary positional parameters, keyword-only parameters, and `**kwargs`, including optional positional suffix defaults and sparse keyword-only defaults, using Python 3.14.6 Language Reference 8.8 Class definitions, 8.7 Function definitions parameter-list/default-parameter semantics, and the Standard Library `staticmethod`/`classmethod` descriptors as provenance. The adapter lowers simple undecorated methods to `#classMethodKwOnlyKwArgs(...)`/`#classMethodKwDefaultsKwArgs(...)`, simple `@staticmethod` methods to `#classStaticMethodKwOnlyKwArgs(...)`/`#classStaticMethodKwDefaultsKwArgs(...)`, and simple `@classmethod` methods to `#classClassMethodKwOnlyKwArgs(...)`/`#classClassMethodKwDefaultsKwArgs(...)`. The semantics evaluates positional defaults and then keyword-only defaults in the active class namespace, stores `functionValPosKwOnlyKwArgs(...)` or `functionValPosKwDefaultsKwArgs(...)` directly or through staticmethod/classmethod wrappers, makes those values bindable as user methods, and reuses the mixed positional-plus-keyword-only-plus-kwargs call path. Covered behavior includes keyword-only `__init__` with `**kwargs`, instance-bound calls, direct class function calls, inherited methods, dynamic `getattr`, static methods including no ordinary positional parameters, class methods with subclass owner binding, decorated classes and decorated base classes, default capture before later class-body rebinding, `**kwargs` collection excluding fixed and keyword-only parameters, and current `TypeError` handling for missing required keyword-only parameters and duplicate positional/keyword binding. Remaining method-signature work includes positional-only combinations with varargs/kwargs, decorated method signatures beyond the staticmethod/classmethod/property profile, exact diagnostics/introspection, and full descriptor/MRO/metaclass interactions.
- 2026-07-04: Extended the adapter-backed user class method profile to ordinary, `@staticmethod`, and `@classmethod` class-body methods combining ordinary positional parameters, `*args`, and keyword-only parameters, including optional positional suffix defaults and sparse keyword-only defaults, using Python 3.14.6 Language Reference 8.8 Class definitions, 8.7 Function definitions parameter-list/default-parameter semantics, and the Standard Library `staticmethod`/`classmethod` descriptors as provenance. The adapter lowers simple undecorated methods to `#classMethodVarArgsKwOnly(...)`/`#classMethodVarArgsKwDefaults(...)`, simple `@staticmethod` methods to `#classStaticMethodVarArgsKwOnly(...)`/`#classStaticMethodVarArgsKwDefaults(...)`, and simple `@classmethod` methods to `#classClassMethodVarArgsKwOnly(...)`/`#classClassMethodVarArgsKwDefaults(...)`. The semantics evaluates positional defaults and then keyword-only defaults in the active class namespace, stores `functionValVarArgsKwOnly(...)` or `functionValVarArgsKwDefaults(...)` directly or through staticmethod/classmethod wrappers, makes those values bindable as user methods, and reuses the mixed varargs-plus-keyword-only call path. Covered behavior includes keyword-only `__init__` with `*args`, instance-bound calls, direct class function calls, inherited methods, dynamic `getattr`, static methods including no ordinary positional parameters, class methods with subclass owner binding, decorated classes and decorated base classes, default capture before later class-body rebinding, and current `TypeError` handling for missing required keyword-only parameters, duplicate positional/keyword binding, and unknown keywords. Remaining method-signature work includes positional-only combinations with varargs/kwargs, decorated method signatures beyond the staticmethod/classmethod/property profile, exact diagnostics/introspection, and full descriptor/MRO/metaclass interactions.
- 2026-07-04: Extended the adapter-backed user class method profile to ordinary, `@staticmethod`, and `@classmethod` class-body methods combining positional-only, positional-or-keyword, and keyword-only parameters, including optional positional suffix defaults and sparse keyword-only defaults, using Python 3.14.6 Language Reference 8.8 Class definitions, 8.7 Function definitions parameter-list/default-parameter semantics, and the Standard Library `staticmethod`/`classmethod` descriptors as provenance. The adapter lowers simple undecorated methods to `#classMethodPosOnlyKwOnly(...)`/`#classMethodPosOnlyKwOnlyDefaults(...)`, simple `@staticmethod` methods to `#classStaticMethodPosOnlyKwOnly(...)`/`#classStaticMethodPosOnlyKwOnlyDefaults(...)`, and simple `@classmethod` methods to `#classClassMethodPosOnlyKwOnly(...)`/`#classClassMethodPosOnlyKwOnlyDefaults(...)`. The semantics evaluates positional defaults and then keyword-only defaults in the active class namespace, stores `functionValPosOnlyKwOnly(...)` or `functionValPosOnlyKwOnlyDefaults(...)` directly or through staticmethod/classmethod wrappers, makes those values bindable as user methods, and reuses the mixed positional-only-plus-keyword-only call path. Covered behavior includes keyword-only `__init__` with positional-only defaults, instance-bound calls, direct class function calls, inherited methods, dynamic `getattr`, static methods, class methods with subclass owner binding, decorated classes and decorated base classes, default capture before later class-body rebinding, and current `TypeError` handling for keyword binding of positional-only parameters, missing required keyword-only parameters, and duplicate positional/keyword binding. Remaining method-signature work includes positional-only combinations with varargs/kwargs, decorated method signatures beyond the staticmethod/classmethod/property profile, exact diagnostics/introspection, and full descriptor/MRO/metaclass interactions.
- 2026-07-04: Extended the adapter-backed user class method profile to ordinary, `@staticmethod`, and `@classmethod` class-body methods with ordinary positional parameters followed by keyword-only parameters, including optional positional suffix defaults and sparse keyword-only defaults, using Python 3.14.6 Language Reference 8.8 Class definitions, 8.7 Function definitions parameter-list/default-parameter semantics, and the Standard Library `staticmethod`/`classmethod` descriptors as provenance. The adapter lowers simple undecorated methods to `#classMethodKwOnly(...)`/`#classMethodKwDefaults(...)`, simple `@staticmethod` methods to `#classStaticMethodKwOnly(...)`/`#classStaticMethodKwDefaults(...)`, and simple `@classmethod` methods to `#classClassMethodKwOnly(...)`/`#classClassMethodKwDefaults(...)`. The semantics evaluates positional defaults and then keyword-only defaults in the active class namespace, stores `functionValPosKwOnly(...)` or `functionValPosKwDefaults(...)` directly or through staticmethod/classmethod wrappers, makes those values bindable as user methods, reuses the mixed positional-plus-keyword-only call path, and adds current `TypeError` fallbacks for invalid calls. Covered behavior includes keyword-only `__init__`, instance-bound methods, direct class function calls, inherited methods, dynamic `getattr`, static methods including no ordinary positional parameters, class methods with subclass owner binding, default capture before later class-body rebinding, and missing/extra-positional `TypeError` handling. Remaining method-signature work includes positional-only combinations with keyword-only parameters, decorated method signatures beyond the staticmethod/classmethod/property profile, exact diagnostics/introspection, and full descriptor/MRO/metaclass interactions.
- 2026-07-04: Extended the adapter-backed user class method profile to positional-only ordinary, `@staticmethod`, and `@classmethod` class-body methods, including optional positional suffix defaults, using Python 3.14.6 Language Reference 8.8 Class definitions and 8.7 Function definitions parameter-list and default-parameter semantics as provenance. The adapter lowers simple undecorated class-body methods to `#classMethodPosOnly(...)`/`#classMethodPosOnlyDefaults(...)`, simple `@staticmethod` methods to `#classStaticMethodPosOnly(...)`/`#classStaticMethodPosOnlyDefaults(...)`, and simple `@classmethod` methods to `#classClassMethodPosOnly(...)`/`#classClassMethodPosOnlyDefaults(...)`. The semantics stores `functionValPosOnly(...)` and `functionValPosOnlyDefaults(...)` values directly or through the staticmethod/classmethod wrappers, makes those values bindable as user methods, evaluates suffix defaults in the active class namespace, reuses the generic positional-only call path, and adds current TypeError fallbacks for invalid positional-only calls. Covered behavior includes defaulted positional-only `__init__`, instance-bound methods, direct class function calls, inherited methods, dynamic `getattr`, static methods, class methods with subclass owner binding, default capture before later class-body rebinding, and current TypeError handling for missing direct receiver, missing required positional-only parameters, and keyword binding attempts against positional-only names. Remaining method-signature work includes keyword-only methods, positional-only combinations with keyword-only parameters, `*args`, and `**kwargs`, decorated method signatures beyond the staticmethod/classmethod/property profile, exact diagnostics/introspection, and full descriptor/MRO/metaclass interactions.
- 2026-07-04: Extended the adapter-backed user class method profile to ordinary-positional class-body methods with both `*args` and `**kwargs`, including optional positional suffix defaults, using Python 3.14.6 Language Reference 8.8 Class definitions and 8.7 Function definitions parameter-list and default-parameter semantics as provenance. The adapter lowers simple undecorated class-body methods to `#classMethodVarKwArgs(...)` or `#classMethodVarKwArgsDefaults(...)`, simple `@staticmethod` methods to `#classStaticMethodVarKwArgs(...)`/`#classStaticMethodVarKwArgsDefaults(...)`, and simple `@classmethod` methods to `#classClassMethodVarKwArgs(...)`/`#classClassMethodVarKwArgsDefaults(...)`. The semantics stores `functionValVarKwArgs(...)` and `functionValVarKwArgsDefaults(...)` values directly or through the staticmethod/classmethod wrappers, makes those values bindable as user methods, evaluates suffix defaults in the active class namespace, reuses the generic varargs-plus-kwargs call path, and adds current TypeError fallbacks for missing required fixed parameters and duplicate positional/keyword binding in that function profile. Covered behavior includes defaulted varargs-plus-kwargs `__init__`, instance-bound methods, direct class function calls, inherited methods, dynamic `getattr`, keyword binding for fixed parameters with kwargs collection, static methods with zero or fixed ordinary parameters, class methods with subclass owner binding, default capture before later class-body rebinding, and current TypeError handling for missing direct receiver, missing required fixed parameter, and duplicate positional/keyword binding. Remaining method-signature work includes positional-only, keyword-only, combinations of positional-only/keyword-only with varargs and kwargs, decorated method signatures beyond the staticmethod/classmethod/property profile, exact diagnostics/introspection, and full descriptor/MRO/metaclass interactions.
- 2026-07-03: Extended the adapter-backed user class method profile to plain `*args` methods with positional suffix defaults using Python 3.14.6 Language Reference 8.8 Class definitions and 8.7 Function definitions default-parameter and parameter-list semantics as provenance. The adapter lowers simple undecorated class-body methods with suffix defaults plus `*args` to `#classMethodVarArgsDefaults(...)`, simple `@staticmethod` methods to `#classStaticMethodVarArgsDefaults(...)`, and simple `@classmethod` methods to `#classClassMethodVarArgsDefaults(...)`. The semantics evaluates suffix defaults in the active class namespace before binding the method name, stores `functionValVarArgsDefaults(...)` values directly or through the staticmethod/classmethod wrappers, makes those function values bindable as user methods, reuses the generic varargs-default call path, and adds current TypeError fallbacks for too-few or conflicting calls into that function profile. Covered behavior includes defaulted varargs `__init__`, instance-bound methods, direct class function calls, inherited methods, dynamic `getattr`, static methods with defaulted fixed parameters plus `*args`, class methods with subclass owner binding, default capture before later class-body rebinding, and current TypeError handling for missing direct receiver, duplicate positional/keyword binding, and unknown keywords. Remaining method-signature work includes positional-only, keyword-only, positional-only combinations with keyword-only parameters, varargs, and kwargs, decorated method signatures beyond the staticmethod/classmethod/property profile, exact diagnostics/introspection, and full descriptor/MRO/metaclass interactions.
- 2026-07-03: Extended the adapter-backed user class method profile to ordinary positional `**kwargs` methods using Python 3.14.6 Language Reference 8.8 Class definitions and 8.7 Function definitions parameter-list semantics as provenance. The adapter lowers simple undecorated class-body methods with `**kwargs` to `#classMethodKwArgs(...)` or `#classMethodKwArgsDefaults(...)`, simple `@staticmethod` methods to `#classStaticMethodKwArgs(...)`/`#classStaticMethodKwArgsDefaults(...)`, and simple `@classmethod` methods to `#classClassMethodKwArgs(...)`/`#classClassMethodKwArgsDefaults(...)`. The semantics stores `functionValKwArgs(...)` and `functionValKwArgsDefaults(...)` values directly or through the staticmethod/classmethod wrappers, makes those function values bindable as user methods, evaluates suffix defaults in the active class namespace, and reuses the generic kwargs call path. Covered behavior includes kwargs `__init__`, instance-bound methods, direct class function calls, inherited kwargs methods, dynamic `getattr`, zero-ordinary-parameter static methods, static methods with suffix defaults, class methods with subclass owner binding, keyword-only and mixed calls into the kwargs profile, and current TypeError handling for missing fixed parameters or too many positional arguments. Remaining method-signature work includes positional-only, keyword-only, positional-only combinations with keyword-only parameters, varargs, and kwargs, decorated method signatures beyond the staticmethod/classmethod/property profile, exact diagnostics/introspection, and full descriptor/MRO/metaclass interactions.
- 2026-07-03: Extended the adapter-backed user class method profile to plain `*args` methods using Python 3.14.6 Language Reference 8.8 Class definitions and 8.7 Function definitions parameter-list semantics as provenance. The adapter lowers simple undecorated class-body methods with `*args` to `#classMethodVarArgs(...)`, simple `@staticmethod` methods with `*args` to `#classStaticMethodVarArgs(...)`, and simple `@classmethod` methods with `*args` to `#classClassMethodVarArgs(...)`. The semantics stores `functionValVarArgs(...)` values directly or through the staticmethod/classmethod wrappers, makes those function values bindable as user methods, and reuses the generic varargs call path, including a new current TypeError fallback for missing required fixed parameters before `*args`. Covered behavior includes varargs `__init__`, instance-bound methods, direct class function calls, inherited varargs methods, dynamic `getattr`, zero-ordinary-parameter static methods, static methods with fixed-plus-varargs parameters, class methods with subclass owner binding, and current TypeError handling for missing required fixed parameters. Remaining method-signature work includes positional-only, keyword-only, positional-only combinations with keyword-only parameters, varargs, and kwargs, decorated method signatures beyond the staticmethod/classmethod/property profile, exact diagnostics/introspection, and full descriptor/MRO/metaclass interactions.
- 2026-07-03: Extended the adapter-backed user class profile to ordinary-positional class-body method suffix defaults using Python 3.14.6 Language Reference 8.8 Class definitions and 8.7 Function definitions default-parameter evaluation as provenance. The adapter lowers simple undecorated methods with suffix defaults to `#classMethodDefaults(...)`, simple `@staticmethod` methods with suffix defaults to `#classStaticMethodDefaults(...)`, and simple `@classmethod` methods with suffix defaults to `#classClassMethodDefaults(...)`. The semantics evaluates default expressions in the active class namespace before binding the method name, stores `functionValDefaults(...)` values directly or through the staticmethod/classmethod wrappers, and reuses the existing bound-method and generic function-default call paths. Covered behavior includes defaulted `__init__`, instance-bound methods, direct class function calls, inherited defaulted methods, default expressions capturing earlier class attributes before later class-body rebinding, defaulted static methods, defaulted class methods with subclass owner binding, dynamic `getattr`, and current TypeError arity handling. Remaining method-signature work includes positional-only, keyword-only, positional-only combinations with keyword-only parameters, varargs, and kwargs, decorated method signatures beyond the staticmethod/classmethod/property profile, exact diagnostics/introspection, and full descriptor/MRO/metaclass interactions.
- 2026-07-03: Extended the adapter-backed simple property profile to same-name class-body `@name.getter` chaining using Python 3.14.6 Language Reference 8.8 Class definitions, Python Standard Library Built-in Functions `property.getter`/`setter`/`deleter`, and the data model descriptor invocation rules as provenance. The adapter lowers otherwise-simple one-parameter getter replacements following a prior property of the same class-body name to `#classPropertyGetter(...)`. The semantics replaces the getter while preserving any existing setter and/or deleter wrapper. Covered behavior includes getter replacement after setter/deleter, inherited chained properties, setter preservation, deleter preservation, read-only assignment/delete failures for missing accessors, and normal descriptor get/set/delete dispatch after chaining. Remaining property/descriptor work includes property constructor calls and property method chaining beyond the current same-name class-body decorator profile, exact property object identity/introspection/doc behavior, full data-descriptor precedence over pre-existing instance attributes, `hasattr` exception-sensitive behavior, full `object.__getattribute__`, MRO/metaclass interactions, exact diagnostics, and class-pattern property access.
- 2026-07-03: Extended the adapter-backed simple property profile to `@name.deleter` and simple user-object attribute deletion using Python 3.14.6 Language Reference 7.5 The del statement and 8.8 Class definitions, Python Standard Library Built-in Functions `property`, and the data model descriptor invocation rules as provenance. The adapter lowers otherwise-simple one-parameter deleters following a prior property of the same class-body name to `#classPropertyDeleter(...)` and lowers `del obj.attr` to `#delAttr(...)`. The semantics records getter/deleter and getter/setter/deleter property wrappers, calls the bound deleter on instance deletion, raises `AttributeError` for deleting read-only properties, and removes current user-object instance attributes while preserving class fallback. Covered behavior includes direct and inherited property deleter dispatch, read-only property deletion failure, deleting instance attributes that shadow class attributes, deleting ad-hoc instance attributes, and missing-attribute `AttributeError` after deletion. Remaining property/descriptor work includes property constructor calls and property method chaining beyond the current same-name class-body decorator profile, exact property object identity/introspection/doc behavior, full data-descriptor precedence over pre-existing instance attributes, `hasattr` exception-sensitive behavior, full `object.__getattribute__`, MRO/metaclass interactions, exact diagnostics, and class-pattern property access.
- 2026-07-03: Extended the adapter-backed simple property profile to `@name.setter` using Python 3.14.6 Language Reference 8.8 Class definitions, Python Standard Library Built-in Functions `property`, and the data model descriptor invocation rules as provenance. The adapter lowers otherwise-simple two-parameter setters following a prior property of the same class-body name to `#classPropertySetter(...)`. The semantics upgrades the existing `propertyVal(getter)` wrapper to `propertyVal(getter, setter)`, calls the bound setter on instance assignment, preserves setter side effects, and raises `AttributeError` for assignment to a read-only property in the current internal exception profile. Covered behavior includes constructor-time setter dispatch, later setter assignment, inherited property setter assignment, dynamic `getattr` reads after setter mutation, class-level property non-callability/truthiness, and read-only property assignment handling. Remaining property/descriptor work includes property constructor calls and property method chaining beyond the current same-name class-body decorator profile, exact property object identity/introspection/doc behavior, full data-descriptor precedence over pre-existing instance attributes, `hasattr` exception-sensitive behavior, full `object.__getattribute__`, MRO/metaclass interactions, exact diagnostics, and class-pattern property access.
- 2026-07-02: Extended the adapter-backed simple method-decorator profile to read-only `@property` getters using Python 3.14.6 Language Reference 8.8 Class definitions, Python Standard Library Built-in Functions `property`, and the data model descriptor invocation rules as provenance. The adapter lowers otherwise-simple one-parameter property getters to `#classProperty(...)`. The semantics stores explicit `propertyVal(...)` wrappers in class attributes, returns the wrapper on class lookup, invokes the getter through the bound-method call path on instance direct lookup and literal/dynamic `getattr`, and preserves instance attributes as higher priority. Covered behavior includes direct property reads, inherited property reads with subclass class-attribute fallback, dynamic/defaulted `getattr`, class-level property non-callability/truthiness, and repeated getter side effects. Remaining property/descriptor work includes property constructor calls and property method chaining beyond the current same-name class-body decorator profile, exact property object identity/introspection/doc behavior, data-descriptor precedence, `hasattr` exception-sensitive behavior, full `object.__getattribute__`, MRO/metaclass interactions, exact diagnostics, and class-pattern property access.
- 2026-07-02: Extended the adapter-backed simple method profile to `@staticmethod` and `@classmethod` in class bodies using Python 3.14.6 Language Reference 8.8 Class definitions, Python Standard Library Built-in Functions `staticmethod`/`classmethod`, and the data model descriptor invocation rules as provenance. The adapter lowers those method decorators to `#classStaticMethod(...)` and `#classClassMethod(...)` for otherwise-simple ordinary-positional method definitions. The semantics stores explicit wrapper values in class attributes, unwraps static methods on class and instance lookup, binds class methods to the current owner class/subclass on class and instance lookup, and routes literal/dynamic `getattr` through the same access helper. Covered behavior includes class and instance calls, inherited static/class methods, subclass binding for inherited class methods, `getattr`, and `callable` over the resulting access values. Remaining method-decorator work includes arbitrary method decorators, full descriptor protocol, method defaults/varargs/keyword-only/positional-only signatures, descriptor interaction with MRO/metaclasses, exact diagnostics, and introspection.
- 2026-07-02: Extended the adapter-backed user class profile to class decorators over the current pass/attribute/method/single-base class profiles using Python 3.14.6 Language Reference 8.8 Class definitions and the decorator ordering described with function definitions as provenance. The adapter lowers decorated current-profile classes to `#classDecorated(...)` or `#classBaseDecorated(...)`. The semantics evaluates decorator expressions top to bottom, evaluates a simple base name after decorator expressions for decorated base classes, collects the class body into a fresh class value without first binding the class name, applies decorators bottom to top by reusing the existing callable application path, and binds the final decorator result. Covered behavior includes identity decorators, decorator expressions returning decorators, decorator replacement of the class binding, bottom-to-top decorator application order, and decorated single-base classes. Remaining decorator/class work includes interactions with metaclasses, keywords, builtin/multiple bases and full MRO, arbitrary class-body execution, descriptor-producing decorators such as `staticmethod`/`classmethod`/`property`, exact diagnostics, and full namespace mapping behavior.
- 2026-07-02: Extended the adapter-backed user class profile to one simple user-class base using Python 3.14.6 Language Reference 8.8 Class definitions, Data Model 3.2 Objects, values and types, 6.3.1 Attribute references, and Built-in Functions `isinstance`/`issubclass`/`getattr`/`hasattr`/`callable` as provenance. The adapter now lowers `class Child(Base): ...` to `#classBase(...)` when `Base` is a simple non-builtin user class name, and dynamic user-class `issubclass(Child, Base)` to `#issubclassDynamic(...)`. The semantics records base lineage in runtime class attributes, appends inherited attributes after child attributes so overrides win, supports inherited class attributes, inherited methods, inherited `__init__`, grandparent ancestry checks, method overriding, direct class function calls, bound inherited methods, dynamic `isinstance`, and dynamic user-class `issubclass` for the covered profile. Remaining class work includes multiple bases, builtin bases, full MRO and C3 linearization, metaclasses, decorators, type parameters, arbitrary class-body statements, method defaults/varargs/keyword-only/positional-only signatures, staticmethod/classmethod/property behavior, descriptor protocol, `__new__`, class attribute mutation/deletion, instance deletion, real type objects, class patterns over user classes, exact diagnostics, and full namespace mapping behavior.
- 2026-07-02: Extended the adapter-backed user class profile to simple class-body method definitions, current `__init__` constructor dispatch, and per-object instance attributes using Python 3.14.6 Language Reference 8.8 Class definitions, 8.7 Function definitions, 7.2 Assignment statements, 6.3.1 Attribute references, Data Model 3.2 Objects, values and types, and Built-in Functions `getattr`/`hasattr`/`callable` as provenance. The adapter preserves supported class-body methods as `#classMethod(...)`, maps `__init__` to an internal dunder-init id for K parsing, and lowers attribute assignment targets such as `self.value = expr` to `#assignAttr(...)`. The semantics stores per-object instance attributes in `<objectAttrs>`, consults instance attributes before class fallback, binds class-stored user functions as simple bound methods, invokes current `__init__` on class calls and requires it to return `None`, and supports direct class function calls, bound method calls, `getattr`, `hasattr`, shadowing, alias-observed mutation, and missing-attribute `AttributeError` in the covered profile. Remaining class work includes bases, MRO, metaclasses, decorators, type parameters, arbitrary class-body statements, method defaults/varargs/keyword-only/positional-only signatures, staticmethod/classmethod/property behavior, descriptor protocol, `__new__`, class attribute mutation/deletion, instance deletion, real type objects, subclassing, user-class `issubclass`/patterns, exact diagnostics, and full namespace mapping behavior.
- 2026-07-02: Extended the adapter-backed user class profile to simple class-body namespace assignments using Python 3.14.6 Language Reference 8.8 Class definitions, Data Model 3.2 Objects, values and types, and Built-in Functions `getattr`/`hasattr` as provenance. The adapter now preserves class bodies containing `pass` and simple `NAME = expr` statements as `#classAttrs(...)`. The current semantics evaluates assignment expressions left to right in a temporary class namespace that can read globals and earlier class attributes, restores the outer environment before binding the fresh class object, stores the resulting class attributes in `classVal`, snapshots them into fresh no-argument instances, and supports direct class/instance fallback lookup plus `getattr`, defaulted `getattr`, `hasattr`, missing-attribute `AttributeError`, and non-leakage of class locals into the outer environment. This remains a narrow profile: arbitrary class-body statements, docstrings/`__doc__`, `__module__`, `__qualname__`, annotations, functions/methods, descriptors, instance attributes, mutation/deletion, bases, metaclasses, decorators, type parameters, real type objects, subclassing, and exact diagnostics remain pending.
- 2026-07-02: Added an adapter-backed pass-only user class profile using Python 3.14.6 Language Reference 8.8 Class definitions, Data Model 3.2 Objects, values and types, and the Built-in Functions `callable`, `isinstance`, and `bool` documentation as provenance. The adapter preserves `class C: pass` as a distinct `#class(C)` statement, and the current semantics allocates a fresh `classVal(C, id)` in the current environment, treats class values as truthy/callable, allocates fresh `userObjectVal(C, classId, objectId)` instances for no-argument class calls, and defines current class/object identity/default-equality plus dynamic `isinstance(obj, C)` checks for those instances, including class-name rebinding to a fresh class object. This is deliberately only a seed of Python's class system: class body execution, namespace creation, bases, MRO, metaclasses, decorators, type parameters, attributes, methods, descriptors, `__new__`/`__init__`, real type objects, subclassing, `issubclass`, user-class patterns, object introspection, and exact diagnostics remain pending.
- 2026-07-02: Added adapter-backed simple generator functions using Python 3.14.6 Language Reference 6.2.9 Yield expressions, 6.2.9.1 generator-iterator methods, 8.7 Function definitions, and the Built-in Types iterator protocol as provenance. The adapter preserves the covered construct as a distinct `#genDef(...)` statement with `#yield(...)`/`#yields(...)` internal yield-expression sequences for undecorated functions with ordinary positional parameters and bodies consisting only of top-level plain `yield` or `yield expr` statements. The current semantics evaluates those yield expressions under the function-call environment, restores the caller environment, materializes the finite yielded stream, and allocates a fresh stateful `iteratorRef`, so `next`, `next(default)`, `list`, `tuple`, `for`, `callable`, exhaustion, positional arguments, fresh calls, and bare `yield` as `None` are covered in this profile. Full CPython generator semantics remains pending: lazy execution on first resume rather than call, suspended frames, local state across resumes, side-effect interleaving, mutation-sensitive behavior, `send`/`throw`/`close`, `yield from`, generator return values carried by `StopIteration`, generator object identity/type/repr/introspection, decorated/default/keyword/positional-only/varargs generator-function signatures, nested control flow around `yield`, exception/finally interaction during close, arbitrary user iterators, exact diagnostics, async generators, and coroutine interaction are not covered by this materialized profile.
- 2026-07-02: Added adapter-backed non-async generator expressions for the current finite comprehension subset, using Python 3.14.6 Language Reference 6.2.8 Generator expressions, Language Reference 6.2.4 displays/comprehensions, and the Built-in Types iterator protocol as provenance. The adapter preserves generator expressions as a distinct `#genExp(...)` internal expression, then the current semantics explicitly materializes the corresponding finite list-comprehension stream and allocates a fresh stateful `iteratorRef` over it. This covers `next`, `next(default)`, `list`, `tuple`, `for`, filters, simple/recursive/starred targets, one-, two-, three-, and many-generator clauses, target-scope restoration, and exhaustion in the current concrete profile. Full CPython lazy generator expression semantics remains pending: generator object identity/type/repr, `send`/`throw`/`close`, generator frame/state attributes, lazy side-effect interleaving, mutation-sensitive iterable behavior after construction, arbitrary user iterator/protocol objects, exact diagnostics, async generator expressions/comprehensions, and `yield` interaction are not covered by this materialized profile.
- 2026-07-02: Fixed adapter-backed `bytes.decode(encoding, errors)` and `bytearray.decode(encoding, errors)` dispatch for invalid UTF-8/ASCII inputs with `ignore`, `replace`, and `backslashreplace`, using Python 3.14.6 Built-in Types bytes/bytearray decode documentation as provenance. The specific handler rules were already present; the generic known-non-strict-handler fallback also matched and could produce `NotImplementedError`. The fallback now excludes the implemented handlers, enabling the four manifest cases for invalid UTF-8 truncation, invalid UTF-8 continuation/prefix bytes, overlong-byte profiles, invalid ASCII bytes, bytearray result type checks, and Latin-1 no-error behavior to run in the shell smoke suite. Remaining decode work includes broader codecs, `namereplace`/custom handlers, exact diagnostic text, and full codec registry behavior.
- 2026-07-02: Added adapter-backed `iter(map(...))` and `iter(filter(...))` support for current finite concrete map/filter values, using Python 3.14.6 Built-in Functions `map`, `filter`, `iter`, and `next`, plus the Built-in Types iterator protocol as provenance. The rules reuse the existing map/filter collection profile to allocate a stateful `iteratorRef` over the collected stream, so `next`, `next(default)`, `list`, `tuple`, and `for` consume the ref through the same one-shot iterator state as other current iterator refs. This is a materialized bridge for the current finite profile: exact CPython map/filter object identity, `next(map_obj)` without an explicit `iter(...)` wrapper, per-`next` callable/predicate side-effect interleaving, strict-map mismatch timing under `next(default)`, iterator inputs to map/filter, arbitrary user iterator objects, mutation-sensitive behavior, and exact diagnostics remain pending.
- 2026-07-02: Extended adapter-backed `for` and `for ... else` semantics to consume current stateful `iteratorRef` values with flat sequence, nested sequence, and starred assignment targets, using Python 3.14.6 Language Reference 8.3 `for` statements, Language Reference 7.2 assignment targets, PEP 3132 extended iterable unpacking, Built-in Types iterator protocol, and CPython `Lib/test/test_iter.py` iterator-loop coverage as provenance. The rules stream one item at a time from current concrete iterator state, bind each item through the existing recursive target-binding rules before executing the body, preserve unconsumed iterator suffixes across `break`, rerun the same iterator state after `continue`, run loop `else` only on exhaustion, and propagate `return` and non-`StopIteration` exceptions. Callable-sentinel iterator refs share the same target-loop path, with sentinel or callable-raised `StopIteration` mapped to normal exhaustion. Arbitrary/user iterator protocol objects, mutation-sensitive container iterator behavior, exact target-arity error diagnostics, and broader assignment target forms remain pending.
- 2026-07-02: Extended adapter-backed arbitrary-many non-async list, set, and dict comprehension semantics to consume current stateful `iteratorRef` values as outer or nested generator sources, using Python 3.14.6 Language Reference display/comprehension sections, Built-in Types iterator protocol, and CPython `Lib/test/test_listcomps.py`/`Lib/test/test_setcomps.py` comprehension coverage as provenance. The rules materialize each current iterator ref through the existing list-constructor iterator profile before entering that generator level, then reuse the recursive `#listCompMany`, `#setCompMany`, and `#dictCompMany` traversal so simple-name and recursive sequence targets, zero or more filters per clause, nested iterable reevaluation in the enclosing target environment, list result order, set duplicate normalization, dict duplicate-key overwrite, key-before-value evaluation, environment restoration, and iterator exhaustion are preserved for four-or-more-generator comprehensions. CPython's exact item-by-item interleaving of iterator consumption with filter/body evaluation remains pending because this bridge materializes each current iterator source before traversing that generator level; arbitrary user iterator objects, mutation-sensitive behavior, exact diagnostics, object identity, async comprehensions, and generator expressions remain pending.
- 2026-07-02: Extended adapter-backed fixed three-generator list, set, and dict comprehension semantics for current sequence/starred target forms with current outer, middle, and inner filter clauses to consume stateful `iteratorRef` values as the outer, middle, or inner generator source, using Python 3.14.6 Language Reference display/comprehension sections, Built-in Types iterator protocol, and CPython `Lib/test/test_listcomps.py`/`Lib/test/test_setcomps.py` filtered target comprehension coverage as provenance. The rules materialize the current iterator ref through the existing list-constructor iterator profile at the generator level being entered, then re-enter the already-modeled target-aware filtered three-generator iterator rules so nested target binding, filter truth testing at each generator level, fresh middle iterators per outer item, fresh inner iterators per middle item, set duplicate normalization, dict duplicate-key overwrite, key-before-value evaluation, and exhaustion are preserved for the fixed three-generator profile. CPython's exact item-by-item interleaving of iterator consumption with filter/body evaluation remains pending because this bridge materializes each current iterator source before traversing that generator level; arbitrary-many generator comprehensions, arbitrary user iterator objects, mutation-sensitive behavior, exact diagnostics, and object identity remain pending.
- 2026-07-02: Extended adapter-backed fixed three-generator list, set, and dict comprehension semantics for simple-name targets with current outer, middle, and inner filter clauses to consume stateful `iteratorRef` values as the outer, middle, or inner generator source, using Python 3.14.6 Language Reference display/comprehension sections, Built-in Types iterator protocol, and CPython `Lib/test/test_listcomps.py`/`Lib/test/test_setcomps.py` filtered comprehension coverage as provenance. The rules materialize the current iterator ref through the existing list-constructor iterator profile at the generator level being entered, then re-enter the already-modeled filtered three-generator iterator rules so filter truth testing at each generator level, fresh middle iterators per outer item, fresh inner iterators per middle item, set duplicate normalization, dict duplicate-key overwrite, key-before-value evaluation, and exhaustion are preserved for the current simple-name profile. CPython's exact item-by-item interleaving of iterator consumption with filter/body evaluation remains pending because this bridge materializes each current iterator source before traversing that generator level; three-generator target-unpacking filter variants, arbitrary-many generator comprehensions, arbitrary user iterator objects, mutation-sensitive behavior, exact diagnostics, and object identity remain pending.
- 2026-07-02: Extended adapter-backed fixed three-generator list, set, and dict comprehension semantics for current sequence/starred target forms to consume stateful `iteratorRef` values as the outer, middle, or inner generator source, using Python 3.14.6 Language Reference display/comprehension sections, Built-in Types iterator protocol, and CPython `Lib/test/test_listcomps.py`/`Lib/test/test_setcomps.py` target-unpacking comprehension coverage as provenance. The rules materialize the current iterator ref through the existing list-constructor iterator profile at the generator level being entered, then re-enter the already-modeled target-aware three-generator iterator rules so nested target binding, fresh middle iterators per outer item, fresh inner iterators per middle item, set duplicate normalization, dict duplicate-key overwrite, key-before-value evaluation, and exhaustion are preserved for the current no-filter profile. CPython's exact item-by-item interleaving of iterator consumption with body evaluation remains pending because this bridge materializes each current iterator source before traversing that generator level; three-generator filter variants, arbitrary-many generator comprehensions, arbitrary user iterator objects, mutation-sensitive behavior, exact diagnostics, and object identity remain pending.
- 2026-07-02: Extended adapter-backed fixed three-generator list, set, and dict comprehension semantics for simple-name targets to consume current stateful `iteratorRef` values as the outer, middle, or inner generator source, using Python 3.14.6 Language Reference display/comprehension sections, Built-in Types iterator protocol, and CPython `Lib/test/test_listcomps.py`/`Lib/test/test_setcomps.py` comprehension coverage as provenance. The rules materialize the current iterator ref through the existing list-constructor iterator profile at the generator level being entered, then re-enter the already-modeled three-generator iterator rules so persistent middle/inner iterator exhaustion, fresh middle iterators per outer item, fresh inner iterators per middle item, set duplicate normalization, dict duplicate-key overwrite, key-before-value evaluation, and exhaustion are preserved for the current simple-name profile. CPython's exact item-by-item interleaving of iterator consumption with filter/body evaluation remains pending because this bridge materializes each current iterator source before traversing that generator level; three-generator target-unpacking/filter variants, arbitrary-many generator comprehensions, arbitrary user iterator objects, mutation-sensitive behavior, exact diagnostics, and object identity remain pending.
- 2026-07-02: Extended adapter-backed two-generator list, set, and dict comprehension semantics to consume current stateful `iteratorRef` values as either the outer source or the inner source expression for simple-name targets, current sequence/starred target forms, and current outer/inner `if` filter clauses, using Python 3.14.6 Language Reference display/comprehension sections, Built-in Types iterator protocol, and CPython `Lib/test/test_listcomps.py`/`Lib/test/test_setcomps.py` comprehension coverage as provenance. The rules materialize the current iterator ref through the existing list-constructor iterator profile, then re-enter the already-modeled two-generator comprehension iterators so persistent inner iterator exhaustion, fresh inner iterators per outer item, target binding, filter truth testing, set duplicate normalization, dict duplicate-key overwrite, key-before-value evaluation, and outer/inner exhaustion are preserved for this profile. CPython's exact item-by-item interleaving of iterator consumption with filter/body evaluation remains pending because this bridge materializes each current iterator source before traversing that generator level; three-or-more generator iterator refs, arbitrary user iterator objects, mutation-sensitive behavior, exact diagnostics, and object identity remain pending.
- 2026-07-02: Extended adapter-backed list, set, and dict comprehension semantics to consume current stateful `iteratorRef` values as the outermost single-`for` source for simple-name targets, current sequence/starred target forms, and current `if`/multiple-`if` filter clauses, using Python 3.14.6 Language Reference display/comprehension sections, Built-in Types iterator protocol, and CPython `Lib/test/test_listcomps.py`/`Lib/test/test_setcomps.py` comprehension coverage as provenance. The rules drain current concrete and callable-sentinel iterator refs through the existing list materialization profile, then reuse the already-modeled comprehension iterator rules so result ordering, set duplicate normalization, dict duplicate-key overwrite, key-before-value evaluation, target binding, filter truth testing, and exhaustion are preserved for this profile. CPython's exact item-by-item interleaving of iterator consumption with filter/body evaluation remains pending because this bridge materializes before running the comprehension body; nested inner `for` iterator refs, arbitrary user iterator objects, mutation-sensitive behavior, exact diagnostics, and object identity remain pending.
- 2026-07-02: Extended adapter-backed `bytes(source)` and `bytearray(source)` constructor semantics to consume current stateful `iteratorRef` sources, using Python 3.14.6 Standard Library built-in constructors, Built-in Types binary sequence constructor behavior, Built-in Types iterator protocol, and CPython `Lib/test/test_bytes.py` constructor coverage as provenance. The rules drain current concrete and callable-sentinel iterator refs through the existing list materialization profile, then reuse the current iterable-of-byte-integers validation and result construction for `bytes` and `bytearray`, including exhausted iterators and current `TypeError`/`ValueError` item diagnostics. CPython's exact partial-consumption timing for the first invalid byte item remains pending because the bridge materializes before item validation; arbitrary user iterator objects, mutation-sensitive behavior, exact diagnostics, and object identity remain pending.
- 2026-07-02: Extended adapter-backed unpacking assignment to consume current stateful `iteratorRef` RHS values for flat name targets, nested sequence targets, and starred assignment targets, using Python 3.14.6 Language Reference assignment statements, PEP 3132 extended iterable unpacking, and Built-in Types iterator protocol as provenance. The rules drain current concrete and callable-sentinel iterator refs through the existing list materialization profile, then reuse the existing arity checks and binding rules for sequence unpacking, including empty starred-rest lists and current `ValueError` paths. CPython's exact partial-consumption timing for iterator unpack arity errors remains pending because the current bridge materializes before checking arity; broader assignment target forms, user iterator objects, mutation-sensitive behavior, exact diagnostics, and object identity remain pending.
- 2026-07-02: Extended adapter-backed `set(iterable)` and `frozenset(iterable)` constructor semantics to consume current stateful `iteratorRef` values, using Python 3.14.6 Standard Library Built-in Types `set`/`frozenset` constructors, Built-in Types iterator protocol, and CPython `Lib/test/test_set.py` constructor/hashability coverage as provenance. The rules drain current concrete and callable-sentinel iterator refs through the existing constructor normalization profile, preserve duplicate elimination, update exhausted iterator state, support hashable `frozenset` elements, raise `TypeError` for unsupported/unhashable constructor inputs in the current profile, and add the matching adapter smoke case. The iterator-ref path currently materializes before applying hashability checks, so CPython's exact partial-consumption timing for the first unhashable element remains pending; arbitrary user iterator objects, mutation-sensitive behavior, subclass constructor behavior, exact diagnostics, and exact object identity remain pending.
- 2026-07-01: Extended adapter-backed `sorted(iterable, /, *, key=None, reverse=False)` semantics to consume current stateful `iteratorRef` values, using Python 3.14.6 Built-in Functions `sorted`, Built-in Types iterator protocol, and CPython `Lib/test/test_builtin.py` sorted iterable/key/reverse/stability coverage as provenance. The rules drain current concrete and callable-sentinel iterator refs into the existing concrete list sorting profile after preserving the current adapter's iterable/key/reverse expression evaluation order, then reuse current direct ordering, `key`, `key=None`, truth-tested `reverse`, stable equal-key ordering, noncallable-key-on-empty behavior, and callable-sentinel exhaustion paths. Arbitrary user iterator objects, mutation-sensitive iterator behavior, broader object comparison/key-result types, exact comparison/key error diagnostics, and full rich-comparison dispatch remain pending.
- 2026-07-01: Extended adapter-backed `min(iterable, /, *, key=None)`, `min(iterable, /, *, default, key=None)`, and corresponding `max` forms to consume current stateful `iteratorRef` values, using Python 3.14.6 Built-in Functions `min`/`max`, Built-in Types iterator protocol, and CPython `Lib/test/test_builtin.py` min/max iterable/default/key coverage as provenance. The rules drain current iterator refs into the existing concrete list profile before applying the already-modeled min/max ordering, default, `key`, `key=None`, and first-encountered tie behavior, and therefore fully consume concrete and callable-sentinel iterator refs including empty/default and sentinel/`StopIteration` exhaustion paths. This batch intentionally reuses the current concrete comparable-key profile; streaming comparison-error timing, unsupported comparison diagnostics and partial-consumption behavior, arbitrary user iterator objects, broader object ordering/key result types, exact rich-comparison dispatch, mutation-sensitive iterator behavior, and exact diagnostics remain pending.
- 2026-07-01: Extended adapter-backed `all(iterable)`, `any(iterable)`, and `sum(iterable[, start])` semantics to consume current stateful `iteratorRef` values, using Python 3.14.6 Built-in Functions `all`, `any`, and `sum`, Built-in Types iterator protocol, and CPython `Lib/test/test_builtin.py` iterator/short-circuit coverage as provenance. The rules stream concrete iterator-state items, preserve short-circuit consumption (`all` stops after the first false item, `any` after the first true item), leave unvisited iterator suffixes available to later `next`, handle exhausted iterators, enforce `sum` string/bytes start rejection before consuming, consume unsupported `sum` items before raising the current `TypeError`, and support the current callable-sentinel iterator profile including sentinel and `StopIteration` exhaustion. User-defined truthiness, arbitrary user iterator objects, broader lazy `map`/`filter`/`zip` iterator refs, mutation-sensitive container iterators, exact rich-comparison sentinel dispatch, exact diagnostics, and full numeric summation precision remain pending.
- 2026-07-01: Added adapter-backed `iter(callable, sentinel, /)` semantics for the current callable and concrete equality profile, using Python 3.14.6 Built-in Functions `iter`, Built-in Types iterator protocol, and CPython `Lib/test/test_iter.py` callable-iterator coverage as provenance. The semantics allocates the same `iteratorRef(id)` shape as concrete iterables, stores callable/sentinel iterator state, calls the callable lazily with no arguments on `next`, returns callable results until `valEq(result, sentinel)`, turns sentinel return or callable-raised `StopIteration` into an exhausted sink state, implements `next(iterator, default)` on both sentinel and callable-raised exhaustion, supports repeated exhausted `StopIteration`, `list(iterator)`/`tuple(iterator)` draining without later calls, simple-name `for`/`for ... else`, break preservation of remaining callable iterator state, non-`StopIteration` exception propagation without exhausting the iterator, delayed callable arity `TypeError`, and CPython's reentrant-exhaustion profile where recursive draining makes the outer call observe exhaustion. User-defined `__call__` instances, exact rich-comparison dispatch for sentinel equality, full object identity/mutable-default aliasing, exact iterator type identity/repr/diagnostics, and mutation-sensitive behavior remain pending.
- 2026-07-01: Added adapter-backed `iter(iterable, /)` and `next(iterator[, default], /)` semantics for a first stateful concrete iterator profile, using Python 3.14.6 Built-in Functions `iter`/`next`, Built-in Types iterator protocol, and CPython `Lib/test/test_builtin.py`/`Lib/test/test_iter.py` coverage as provenance. The semantics introduces explicit `iteratorRef(id)` values plus iterator-state cells, preserves left-to-right argument evaluation through `ArgExps`, allocates independent iterators for concrete iterable values, returns the same iterator ref for `iter(iterator)`, implements shared consumption across simple aliases, implements `next` item consumption, exhausted `StopIteration`, default return on exhaustion, repeated-exhaustion sink state, always-truthy iterator objects, `list(iterator)`/`tuple(iterator)` draining, and simple-name `for`/`for ... else` iteration over iterator refs with break/continue/return/exception propagation. The first profile covers current concrete list, tuple, string, bytes, bytearray, memoryview, dict-key, set, frozenset, range, enumerate, zip, and reversed iterable materialization sources. User-defined `__iter__`/`__next__`, sequence-protocol fallback through user `__getitem__`, map/filter lazy `next`, iterator refs as inputs to all existing iterable-consuming builtins/comprehensions, unpacking/target iterator loops, mutation-sensitive container iterator behavior, exact iterator type identity/repr/diagnostics, and full object identity remain pending.
- 2026-07-01: Added adapter-backed `filter(function, iterable, /)` semantics for the current finite concrete iterable and callable predicate profile, using Python 3.14.6 Built-in Functions `filter` and CPython `Lib/test/test_builtin.py` filter coverage as provenance. The semantics keeps `filter` as a distinct `#filterVal(predicate, iterable)` value, preserves left-to-right argument evaluation, performs exact two-argument and construction-time iterable validation, implements `function is None` by retaining truthy original items, applies callable predicates during list/tuple/direct-loop consumption, retains original iterable elements when predicate results are truthy, supports list/tuple conversion, simple and unpacking `for`/`for ... else` loops, always-truthy filter objects, delayed non-callable-predicate `TypeError`, predicate exception propagation, and current construction `TypeError` paths for wrong arity or unsupported iterables. Full lazy single-pass iterator identity/state, `iter()`/`next()`, filter values as generic iterables to other iterator-producing builtins and comprehensions, user-defined iterator objects, interleaving predicate side effects with loop bodies, mutation-sensitive behavior, and exact diagnostic text remain pending.
- 2026-07-01: Added adapter-backed `map(function, iterable, /, *iterables, strict=False)` semantics for the current finite concrete iterable and callable profile, using Python 3.14.6 Built-in Functions `map` as provenance. The semantics keeps `map` as a distinct `#mapVal(function, iterables, strict)` value, preserves left-to-right argument evaluation, performs construction-time arity and iterable validation, applies the stored callable during list/tuple/direct-loop consumption, supports shortest-input truncation, truth-tested `strict`, equal-length strict success, strict mismatch `ValueError`, list/tuple conversion, simple and unpacking `for`/`for ... else` loops, and current construction/consumption `TypeError` paths for unsupported iterables, missing iterable arguments, and non-callable functions. Full lazy single-pass iterator identity/state, `iter()`/`next()`, map values as generic iterables to other iterator-producing builtins and comprehensions, interleaving callable side effects with loop bodies, mutation-sensitive behavior, and exact diagnostic text remain pending.
- 2026-07-01: Added adapter-backed `reversed(object)` semantics for the current finite reversible built-in profile, using Python 3.14.6 Built-in Functions `reversed` as provenance and local CPython checks for built-in accept/reject behavior. The semantics keeps `reversed` as a distinct `#reversedVal` value containing current reversed items, covers list, tuple, string, bytes, bytearray, memoryview, range, and dictionary-key reversal, list/tuple conversion, simple and unpacking `for`/`for ... else` loops, comprehensions through current iterable argument extraction, and current `TypeError` profiles for unsupported set, frozenset, and current iterator values. Full single-pass reverse-iterator identity/state, `iter()`/`next()`, mutation-sensitive behavior, user-defined `__reversed__`, general `__len__`/integer `__getitem__` sequence protocol dispatch, and exact diagnostic text remain pending.
- 2026-07-01: Added adapter-backed `zip(*iterables, strict=False)` semantics for the current finite concrete iterable profile, using Python 3.14.6 Built-in Functions `zip` as provenance. The semantics keeps `zip` as a distinct `#zipVal` value containing current tuple rows, preserves left-to-right argument evaluation through `ArgExps`, covers zero-argument empty iterators, one-argument 1-tuples, shortest-input truncation, `strict=True` equal-length success and mismatch `ValueError`, list/tuple conversion, simple and unpacking `for`/`for ... else` loops, comprehensions through current iterable argument extraction, and current `TypeError` profiles for unsupported iterables. Full single-pass iterator identity/state, `iter()`/`next()`, mutation-sensitive laziness, shared-iterator chunking idioms, exact strict diagnostic text, and arbitrary iterator protocol objects remain pending.
- 2026-07-01: Added adapter-backed `enumerate(iterable, start=0)` semantics for the current finite concrete iterable profile, using Python 3.14.6 Built-in Functions `enumerate` as provenance. The semantics keeps `enumerate` as a distinct `#enumerateVal` value containing current `(index, value)` tuple items, supports default, positional, and `start=` integer-like starts, list/tuple conversion, simple and unpacking `for`/`for ... else` loops, comprehensions through current iterable argument extraction, and current `TypeError` profiles for unsupported iterables or non-integer-like starts. Full single-pass iterator identity/state, `iter()`/`next()`, mutation-sensitive behavior, `__index__`, and exact diagnostic text remain pending.
- 2026-07-01: Extended adapter-backed `min`/`max` one-iterable semantics for current concrete unordered iterables with mixed numeric-comparable values, using Python 3.14.6 Built-in Functions `min`/`max` and Built-in Types `dict`, `set`, and `frozenset` iterable behavior as provenance. Dict-key, set, and frozenset inputs now use the same int/bool/float comparison profile as list/tuple and multi-argument `min`/`max`, including non-empty `default=` and `key=None` paths. Arbitrary comparable objects, exact diagnostic text, broader iterator protocol objects, and CPython's unspecified unordered iteration order remain pending.
- 2026-06-30: Added adapter-backed `frozenset` support for the current concrete-iterable builtin profile of `all`, `any`, `sum`, `min`, and `max`, using Python 3.14.6 Built-in Functions and Built-in Types `frozenset` iterable behavior as provenance. The rules cover empty/non-empty truth reducers, numeric summation with current `TypeError` start/item checks, current int-like direct `min`/`max`, empty `ValueError`, `default=`, `key=`, `key=None`, and combined `default=`/`key=` paths through the current internal set-family order profile. Broader arbitrary comparable objects, exact diagnostic text, and general iterator protocol objects remain pending.
- 2026-06-30: Added adapter-backed sequence-unpacking assignment over concrete `frozenset` RHS values, using Python 3.14.6 Language Reference assignment statements, PEP 3132 extended iterable unpacking, and Standard Library Built-in Types `frozenset` iterable behavior as provenance. The rules now bind flat name targets, current nested sequence targets, current starred targets, empty `frozenset()` starred-rest assignment, and current arity `ValueError` paths through the same internal set-family item order profile used by other concrete `frozenset` operations. General iterator protocol objects, exact CPython diagnostic text, and broader assignment target forms beyond the adapter-supported name/sequence/starred target profile remain pending.
- 2026-06-30: Added adapter-backed direct `for`-statement iteration over concrete `frozenset` values, using Python 3.14.6 Language Reference `for` statements and Standard Library Built-in Types `frozenset` iterable behavior as primary provenance. The rules mirror the existing set iteration profile for empty/non-empty `frozenset` in simple-name targets, current sequence-unpacking targets, current starred targets, and corresponding `for ... else` normal-completion/break-suppression paths. The iteration order remains the current internal concrete set-family order profile; broader assignment targets in adapter-lowered `for` headers, general iterator protocol objects, and mutation/identity interactions remain pending.
- 2026-06-30: Added adapter-backed semantics for the `hash(object)` builtin over the current concrete hashable value profile, using Python 3.14.6 Built-in Functions `hash`, Data Model `object.__hash__`, Standard Types hashability/numeric-hash notes, and CPython `Lib/test/test_builtin.py`/`Lib/test/test_hash.py` as primary provenance. The rules return integers for current hashable concrete ints/bools/floats/complex numbers, strings, bytes, `None`, `Ellipsis`, tuples with hashable elements, `frozenset` with hashable elements, ranges, and slices with hashable components; preserve the language-level invariant that equal currently modeled hashable values share a hash; and raise `TypeError` for current mutable or recursively unhashable values. CPython's exact salted `str`/`bytes` hash values, exact tuple/frozenset/range/slice hash algorithms, hash width truncation, memoryview hashability, user-defined `__hash__`, identity-based object hashes, and full NaN/object-identity corner cases remain pending.
- 2026-06-30: Added adapter-backed semantics for concrete `frozenset` values and constructor calls, using Python 3.14.6 Standard Library Built-in Types set/frozenset documentation and CPython `Lib/test/test_set.py` `TestJointOps`/`TestFrozenSet` coverage as primary provenance. The rules model immutable set-family equality/order comparisons against `set` and `frozenset`, binary operators returning the type of the left operand, non-mutating set-family methods, truthiness, length, membership, list/tuple/bytes/bytearray conversion, concrete iterable use, and hashability as dict keys and set elements. Full `hash()` numeric values, object identity/aliasing, subclass behavior, arbitrary `*others` beyond the current adapter arity, and CPython diagnostic text remain pending.
- 2026-06-30: Added adapter-backed semantics for mutating concrete-set methods `add(elem)`, `remove(elem)`, `discard(elem)`, `pop()`, `clear()`, `update(*others)`, `intersection_update(*others)`, `difference_update(*others)`, and `symmetric_difference_update(other)`, using Python 3.14.6 Standard Library Built-in Types mutable set method documentation and CPython `Lib/test/test_set.py` mutation tests as primary provenance. The rules mutate simple-name receivers, return `None` for mutators except `pop()`, choose a deterministic representative for the docs-specified arbitrary `pop()` element, raise `KeyError` for missing `remove()`/empty `pop()`, accept the current concrete iterable profile for update methods, and enforce current hashability checks for mutating element/update inputs. Temporary set expression calls return the documented value or exception but do not model object identity/alias mutation. `frozenset`, arbitrary `*others` beyond the current adapter arity, and full object-aliasing semantics remain separate pending batches.
- 2026-06-30: Added adapter-backed semantics for non-mutating concrete-set methods `copy()`, `isdisjoint(other)`, `issubset(other)`, `issuperset(other)`, `union(*others)`, `intersection(*others)`, `difference(*others)`, and `symmetric_difference(other)`, using Python 3.14.6 Standard Library Built-in Types set method documentation and CPython `Lib/test/test_set.py` as primary provenance. The rules accept the current concrete iterable profile for method forms, unlike the operator forms, cover zero-argument copy-producing algebra methods, one-, two-, and three-argument `union`/`intersection`/`difference` profiles supported by the adapter, query booleans, and `TypeError` for non-iterable arguments and wrong arities in the covered methods. `frozenset`, arbitrary `*others` beyond the current adapter arity, object aliasing, and hashability enforcement for non-mutating set-producing inputs remain separate pending batches.
- 2026-06-30: Added adapter-backed semantics for set binary operators `|`, `&`, `-`, and `^`, plus simple-name augmented assignments `|=`, `&=`, `-=`, and `^=` over current concrete set values, using Python 3.14.6 Standard Library Built-in Types set operation documentation and CPython `Lib/test/test_set.py` binary-operator TypeError checks as primary provenance. The rules compute union, intersection, difference, and symmetric difference over the existing normalized concrete set representation, preserve the documented empty-set behavior, keep integer bitwise augmented assignment on its existing path, and reject non-set operands for operator forms with `TypeError`. Iterable-accepting method forms such as `set.union(iterable)`, `set.update(iterable)`, `set.intersection_update(iterable)`, `set.difference_update(iterable)`, and `set.symmetric_difference_update(iterable)`, as well as `frozenset` and hashability enforcement for set elements, remain separate pending batches.
- 2026-06-30: Added adapter-backed semantics for dictionary merge operators `d | other` and `d |= other`, using Python 3.14.6 Standard Library Built-in Types dictionary operation documentation and PEP 584 as primary provenance. The non-mutating `|` operator now returns a merged concrete dict only for dict/dict operands, preserving left insertion order and right overwrite behavior while leaving operands unchanged; non-dict merge operands raise current `TypeError` in the covered profiles. The in-place `|=` operator now mutates simple-name concrete dict receivers, returns statement completion, accepts current concrete dict values and iterable-of-pairs values, preserves the existing integer `|=` path for non-dict receivers, and reuses the CPython-observed `TypeError`/`ValueError` split from `dict.update`. Keyword update forms such as `d.update(x=1)`/`d.update(other, **kw)`, shared mutable aliasing from `fromkeys`, dynamic dictionary view objects from `keys()`/`values()`/`items()`, reverse iteration, and view set-like behavior remain separate pending batches.
- 2026-06-30: Added adapter-backed semantics for `dict.fromkeys(iterable[, value])`, `dict.update()`, and `dict.update(other)` over the current concrete iterable and dict profiles, using Python 3.14.6 Standard Library Built-in Types dictionary operation documentation as the primary source. `fromkeys` preserves first key occurrence order with default `None`, rejects non-iterables and unsupported unhashable key profiles with `TypeError`, and `update` mutates the receiver in place, returns `None`, preserves existing insertion order while appending new keys, consumes current dict values and current iterable-of-pairs values including list/tuple/string/range pair values, and covers CPython-observed `TypeError`/`ValueError` split for non-sequence elements, wrong-length pair elements, and unhashable keys. Keyword update forms such as `d.update(x=1)`/`d.update(other, **kw)`, shared mutable aliasing from `fromkeys`, dynamic dictionary view objects from `keys()`/`values()`/`items()`, reverse iteration, and view set-like behavior remain separate pending batches.
- 2026-06-30: Added adapter-backed semantics for current concrete-dict methods `dict.get(key[, default])`, `dict.copy()`, `dict.clear()`, `dict.pop(key[, default])`, `dict.popitem()`, and `dict.setdefault(key[, default])`, using Python 3.14.6 Standard Library Built-in Types dictionary operation documentation as the primary source. The rules preserve insertion order for updates, implement Python 3.7+ LIFO `popitem()`, mutate the receiver for destructive methods, return `None` for `clear()` and default-less `setdefault()` insertion, return defaults for missing `get`/two-argument `pop`, and cover current `KeyError`/`TypeError` diagnostics for missing required keys and unsupported unhashable key profiles. Dynamic dictionary view objects from `keys()`/`values()`/`items()`, reverse iteration, and view set-like behavior remain separate pending batches.
- 2026-06-30: Extended adapter-backed `list.sort()` semantics to the documented keyword-only surface `list.sort(*, key=None, reverse=False)` for current simple-name concrete list receivers, using Python 3.14.6 Standard Library Built-in Types `list.sort` and Language Reference call evaluation order as primary sources. The rules evaluate `key` and `reverse` in source keyword order, treat `key=None` as direct item sorting, apply callable keys once per current list element before sorting, use the current stable keyed-sort helper for equal-key ordering, honor general truthiness for `reverse`, mutate the receiver in place on successful sort, return `None`, preserve current TypeError behavior for noncallable/incomparable key profiles, and route positional `sort(arg)` to `TypeError`. CPython's documented implementation detail that the list appears empty during sorting and may detect mutation during sort is not modeled in the base semantics because the docs mark mutation/inspection during sort as undefined/implementation-specific.
- 2026-06-30: Added adapter-backed semantics for ordinary concrete-list query methods `list.count(value)` and `list.index(value[, start[, stop]])`, plus no-argument in-place `list.sort()`, using Python 3.14.6 Standard Library Built-in Types sequence/list method documentation as the primary source. The rules use Python equality for count/index, slice-style normalization for bounded index searches, `ValueError` for missing values, `TypeError` for invalid index bounds or invalid list-count arity, and the current `sorted()` comparison profile for no-key `sort()` mutation and incomparable-item `TypeError`.
- 2026-06-30: Full adapter-smoke validation while adding list mutators exposed three incomplete parser probes in existing `printf`-style bytes/bytearray `%` formatting: dot-start literal numeric precision such as `%#.3o`, ignored length modifiers before nonnumeric conversions such as `%ls`, and mapping-key option bodies such as `%(x)+05d`. The semantics now reduces those probes through the appropriate precision/lengthless/generic-field paths instead of leaving stuck K terms, matching CPython 3.14.6 behavior in `adapter-bytes-percent-format-numeric-options`, `adapter-bytes-percent-format-length-modifiers-direct`, and `adapter-bytes-percent-format-mapping-options`.
- 2026-06-29: Added adapter-backed semantics for the core concrete-list mutating method set `list.append(x)`, `list.extend(iterable)`, `list.clear()`, `list.copy()`, `list.reverse()`, `list.pop([index])`, `list.remove(value)`, and `list.insert(index, value)`, using Python 3.14.6 Standard Library Built-in Types mutable sequence operations as the primary source. The rules cover `None`-returning mutators, fresh value-profile copies, negative and bool-as-int pop/insert indices, concrete iterable extension from current list/tuple/string/bytes/bytearray/memoryview/dict-key/set/range values, first-match removal, clamped insert indices, and current `TypeError`/`ValueError`/`IndexError` diagnostics in `adapter-list-mutating-methods`.
- 2026-06-29: Added adapter-backed `bytes.maketrans`, `bytearray.maketrans`, `bytes.translate`, and `bytearray.translate` for current concrete bytes-like values, using Python 3.14.6 Standard Library Built-in Types as the primary source. Local CPython checks show `translate` accepts `bytearray` and `memoryview` translation tables of length 256 even though the docs phrase the table as a bytes object; the executable semantics follows the reference behavior and records that coverage in `adapter-bytes-maketrans-translate`, `adapter-bytearray-maketrans-translate`, and `adapter-bytes-translate-diagnostics`.
- 2026-06-29: Added a first adapter-backed `printf`-style bytes formatting slice for `bytes` and `bytearray` `%`, covering literal `%%`, `%b`/`%s` bytes-like insertion, `%a`/`%r` ASCII `repr` insertion over the current string-conversion domain, `%c` integer or one-byte bytes/bytearray insertion, tuple vs single-argument consumption, bytearray result preservation, and current TypeError/OverflowError diagnostics. Numeric conversions, flags, width, precision, length modifiers, and mapping-key formats remain separate pending slices.
- 2026-06-29: Extended adapter-backed `printf`-style bytes formatting to signed integer conversion types `%d`, `%i`, `%u`, `%o`, `%x`, and `%X` for `bytes` and `bytearray`. The decimal forms accept int-like values and finite floats with CPython-style truncation toward zero; octal and hexadecimal forms accept int-like values only; bool values format as integers; negative octal/hex output carries the sign before the digits; bytearray formatting still returns fresh bytearray values. The slice adds TypeError checks for invalid numeric operands plus decimal infinity `OverflowError` and NaN `ValueError`. Conversion flags, width, precision, length modifiers, mapping-key formats, and floating-point presentation types remain pending.
- 2026-06-29: Added literal precision parsing for adapter-backed `printf`-style bytes formatting for `%b`, `%s`, `%a`, `%r`, and `%c`. Precision truncates the byte output of bytes-like and ASCII-repr conversions and is accepted but ignored for `%c`, matching CPython 3.14.6. This slice preserves bytearray result type and extends precision-specific TypeError/OverflowError tests. Numeric precision, field width, flags, dynamic `*` width/precision, length modifiers, mapping-key formats, and floating-point presentation types remain pending.
- 2026-06-29: Added literal minimum field width and `-` left-adjustment parsing for adapter-backed `printf`-style bytes formatting for `%b`, `%s`, `%a`, `%r`, and `%c`, including width combined with literal precision. Padding uses ASCII spaces, precision is applied before padding, and bytearray formatting continues to return fresh bytearray values. Numeric flags/width/precision, ignored non-numeric flags, dynamic `*` width/precision, length modifiers, mapping-key formats, and floating-point presentation types remain pending.
- 2026-06-29: Added numeric conversion flags plus literal field width and precision for adapter-backed `printf`-style bytes formatting for `%d`, `%i`, `%u`, `%o`, `%x`, and `%X`. The slice covers sign flags, space flags, zero padding, left adjustment, alternate octal/hex prefixes, negative numeric prefix placement, precision zero-padding, bytearray result preservation, and option-preserving TypeError diagnostics for non-float invalid operands checked against CPython 3.14.6. Local Haskell-backend validation of bytes `%` finite/special float truncation and special-float diagnostics is currently blocked by the backend missing the `FLOAT.eq` hook; the semantic rules remain in the definition, but executable tests for those float paths need a backend/toolchain fix or an LLVM validation lane. Mapping-key formats, ignored non-numeric flags beyond the current `-` profile, dynamic `*` width/precision, length modifiers, and floating-point presentation types remain pending.
- 2026-06-29: Added ignored `h`/`l`/`L` length modifiers for the current adapter-backed `printf`-style bytes formatting subset, including direct conversions such as `%ld`/`%ls`, width and precision combinations such as `%5lb`/`%.1lc`, numeric flag combinations such as `%#06lx`, bytearray result preservation, and non-float TypeError/OverflowError diagnostics. Mapping-key formats, ignored non-numeric flags beyond the current `-` profile, dynamic `*` width/precision, and floating-point presentation conversions remain pending.
- 2026-06-29: Added accepted nonnumeric conversion flag prefixes for adapter-backed `printf`-style bytes formatting for `%b`, `%s`, `%a`, `%r`, and `%c`, matching CPython 3.14.6 behavior where `#`, `0`, space, and `+` are ignored and `-` left-adjusts padded output. Mapping-key formats, dynamic `*` width/precision, and floating-point presentation conversions remain pending.
- 2026-06-29: Added dynamic `*` minimum field width for the current nonnumeric adapter-backed `printf`-style bytes formatting conversions `%b`, `%s`, `%a`, `%r`, and `%c`, including negative-width left adjustment, literal precision and ignored length modifier combinations after `*`, bytearray result preservation, and TypeError behavior for non-integer or missing width arguments. Mapping-key formats, dynamic `*` precision, dynamic `*` numeric width, and floating-point presentation conversions remain pending.
- 2026-06-29: Added dynamic `*` precision for the current nonnumeric adapter-backed `printf`-style bytes formatting conversions `%b`, `%s`, `%a`, `%r`, and `%c`, including direct `%.*b`-style formats, literal-width `%5.*b` and flag-prefixed variants, dynamic-width `%*.*b` variants with negative-width left adjustment, bytearray result preservation, and TypeError behavior for non-integer or missing precision arguments. Dynamic negative precision is treated as an actual precision value for `%b`/`%s`/`%a`/`%r` and clips to empty output; `%c` accepts and ignores it, matching CPython 3.14.6. Mapping-key formats, dynamic `*` numeric width/precision, and floating-point presentation conversions remain pending.
- 2026-06-29: Added dynamic `*` minimum field width for current numeric adapter-backed `printf`-style bytes formatting conversions `%d`, `%i`, `%u`, `%o`, `%x`, and `%X`, including numeric flags, negative-width left adjustment, literal precision and ignored length modifier combinations after `*`, bytearray result preservation, and TypeError behavior for non-integer or missing width arguments. Mapping-key formats, dynamic `*` numeric precision, and floating-point presentation conversions remain pending.
- 2026-06-29: Added dynamic `*` precision for current numeric adapter-backed `printf`-style bytes formatting conversions `%d`, `%i`, `%u`, `%o`, `%x`, and `%X`, including direct `%.*d`-style formats, literal-width/flag-prefixed variants, dynamic-width `%*.*d` variants with negative-width left adjustment, ignored length modifier combinations after `*`, bytearray result preservation, and TypeError behavior for non-integer or missing precision arguments. Dynamic negative numeric precision is treated as no precision, matching CPython 3.14.6. Mapping-key formats and floating-point presentation conversions remain pending.
- 2026-06-29: Added adapter-backed mapping-key forms for current `printf`-style bytes/bytearray `%` formatting over concrete dict values, using balanced bytes mapping keys and bytes-key lookup, including bytes-like, repr/ascii, character, and integer conversions plus literal width, precision, flags, and ignored length modifiers. The slice preserves bytearray result type and covers TypeError for leading mapping-key formats with non-mapping RHS values, KeyError for missing bytes keys including string-key-only dictionaries, and ValueError for incomplete mapping keys. Dynamic `*` mapping-key diagnostics and floating-point presentation conversions were left as follow-up slices.
- 2026-06-29: Added CPython-checked diagnostics for disallowed dynamic `*` specifiers in current mapping-key `printf`-style bytes/bytearray `%` formats. The rules preserve bytes-key lookup order, so missing mapping keys still raise `KeyError`, while present mapping keys whose format body requires a sequential `*` width or precision raise `TypeError`, matching Python 3.14.6 and the standard-library docs rule that mapping formats cannot use `*`. Floating-point presentation conversions remain pending.

## Source Chapters

| Source area | Reference | Status | Notes |
| --- | --- | --- | --- |
| Lexical analysis | Language Reference, chapter 2 | partial | Initial kernel uses semicolon-separated simple statements with trailing semicolons and parses integers, simple K strings, CPython-AST-backed source string/bytes literal values for raw, adjacent, ignored-newline, named-character, hex, octal, and byte-boundary escape spellings, adapter-backed simple f-string text/formatted-expression/conversion/debug/empty-format parts plus literal and dynamic nested format-spec parts over the current format subset, `True`, `False`, `None`, and the adapter-backed `...` atom, bytes literal constants, and imaginary literal constants. Full source encoding, physical/logical lines, indentation, comments, token classes, native K parser coverage and diagnostics for full Python string/bytes prefixes and escapes, t-string details, and numeric literal edge cases remain. |
| Data model | Language Reference, chapter 3 | partial | Initial kernel has minimal integers, floats, complex values, strings, bytes, list/tuple/dict/set values, booleans, `None`, and `Ellipsis`. Full object identity, real bool-as-int subtype behavior, type hierarchy, attributes, descriptors, special methods, GC/lifetime notes, coroutines, and async generators remain. |
| Execution model | Language Reference, chapter 4 | partial | Current execution context uses the `<env>` cell plus `restoreEnv` continuations for supported function/lambda/method calls, temporary class-body namespaces for supported class definitions, saved-environment comprehension iterators for supported comprehension scopes, and an adapter-backed lazy type-alias object profile with cached `__value__` evaluation. Needs real frame objects, locals/globals/builtins mappings, closures/cells/free variables, frame introspection and traceback integration, exact `UnboundLocalError`, exceptions, annotation scopes, generator/coroutine suspended frames, `exec`/`eval` locals semantics, and broader runtime components. |
| Import system | Language Reference, chapter 5 | partial | Current fixed-module slice covers `math`, `keyword`, and `importlib` imports, top-level relative-import errors, structural identity for repeated supported imports, `importlib.import_module` for fixed supported names, already-imported fixed-module attribute mutation/deletion over current env/list/tuple/dict-held module values, and future supported fixed-module import/importlib reuse of env-reachable mutated/deleted module snapshots. Needs real `sys.modules`, find/load protocol, module specs, positive packages and relative imports, star and dotted imports, user module execution, broader importlib integration, host/filesystem profile boundaries, and `__main__`. |
| Expressions | Language Reference, chapter 6 | partial | Initial kernel covers integer/float/imaginary/string/bytes/list/tuple/dict/set/bool/None/Ellipsis atoms, adapter-backed simple f-string interpolation over the current string-conversion value subset with left-to-right expression evaluation, `!s`/`!r`/`!a` conversions, debug literal prefixes, empty, literal non-empty, and dynamic nested format specs over the current format subset, and exception propagation, names, parentheses, expression-backed list/tuple/dict/set displays through the adapter including starred unpacking over current ordered concrete iterables, unary `+`/`-`/`~`, arithmetic and bitwise integer operators, complex unary `+`/`-` plus complex addition/subtraction/multiplication/nonzero true division for current int/bool/float/complex values, string concatenation/equality/order, string/list/tuple/bytes/bytearray sequence repetition with integer-like counts plus current invalid-repeat-count `TypeError` diagnostics, string nonzero-step slicing by Unicode code point plus list/tuple/bytes/bytearray nonzero-step slicing plus `ValueError` for zero-step direct and slice-object subscriptions in the current subset, adapter-backed slice objects with start/stop/step attributes and slice-object subscriptions over current string/list/tuple/bytes/bytearray/range values, numeric `real`/`imag` and int-like `numerator`/`denominator` attributes, string substring membership and string indexing by Unicode code point and bytes/bytearray integer indexing with current out-of-range `IndexError` and invalid-index `TypeError` diagnostics, bytes/bytearray subsequence membership with current ValueError diagnostics for out-of-range integer needles and TypeError diagnostics for invalid non-bytes-like needles, list/tuple same-type concatenation, bytes/bytearray same-family concatenation, current cross-type sequence concatenation `TypeError` diagnostics over string/bytes/bytearray/list/tuple, list/tuple/dict/set/bytes/bytearray equality, list/tuple/bytes/bytearray lexicographic ordering, set-to-set ordering comparisons, current concrete unsupported ordering `TypeError` diagnostics, list/tuple membership/indexing with current out-of-range `IndexError` and invalid-index `TypeError` diagnostics, dict key membership/lookup with current missing-key `KeyError` and unsupported-key `TypeError` diagnostics, set membership, range equality/int-like and float-equality membership/indexing/nonzero-step slicing/start-stop-step attributes with current out-of-range `IndexError` and invalid-index `TypeError` diagnostics, singleton identity comparisons, chained comparisons over supported operators including current unsupported-ordering `TypeError` diagnostics, truthiness for simple values, short-circuit `and`/`or`, `not`, conditional expressions, simple-name assignment expressions, positional-argument lambdas/calls including suffix defaults, positional-only lambdas including suffix defaults, varargs-only, kwargs-only, keyword-only, keyword-only-plus-kwargs, varargs-plus-kwargs, varargs-plus-keyword-only, and varargs-plus-keyword-only-plus-kwargs combinations, varargs parameters including suffix defaults and keyword binding for fixed parameters, `*args` plus keyword-only lambda signatures including suffix and sparse keyword-only defaults and kwargs collection, `*args, **kwargs` lambda signatures including suffix defaults, keyword-only parameters in the no-positional subset including suffix defaults and kwargs collection, mixed positional-plus-keyword-only parameters including suffix defaults and kwargs collection, and starred positional call unpacking over current ordered concrete iterables including bytes and bytearray, and a binary comparison subset. All other expression constructs remain. |
| Simple statements | Language Reference, chapter 7 | partial | Initial kernel covers expression statements, `pass`, `assert` in the current non-optimized profile including success-path message skipping and false-path `AssertionError` sentinel raising after optional message evaluation, module-scope single-name and multi-name `global`, simple assignment, simple named list/dict subscription assignment and augmented assignment with current simple-target `IndexError`/`KeyError`/`TypeError` diagnostics, two-level simple-name-rooted list/dict subscription assignment, simple named list slice assignment including explicit step-1 resizing, fixed-length non-unit-step replacement over current concrete iterable RHS values, and `ValueError` for mismatched non-unit-step replacement lengths, simple-name annotated assignment in the current no-introspection profile, flat, non-starred nested, starred, and nested starred sequence unpacking assignment over current string/bytes/bytearray/list/tuple/dict/set/range/memoryview RHS values with current `ValueError` arity checks for flat, top-level starred-too-few, and top-level nested target mismatches, simple-name `+=`/`-=`/`*=`/`@=`/`/=`/`%=`/`**=`/`//=`/`<<=`/`>>=`/`&=`/`^=`/`|=`, simple-name/multi-target simple-name `del`, simple named list/dict subscription `del` with current `IndexError`/`KeyError`/`TypeError` diagnostics, current two-level simple-name-rooted list/dict subscription `del`, simple named list slice `del`, and adapter-backed `raise`, `raise Name`, `raise Name(args...)`, `raise Name from None`, and `raise Name from Cause` over the current internal exception-value subset, including positional constructor-argument evaluation, with bytes/bytearray/memoryview yielding integers, dictionaries yielding keys, and sets using the current internal set iteration order profile. Remaining statements are not started. |
| Compound statements | Language Reference, chapter 8 | partial | Adapter-backed `if`, `while`, string/bytes/bytearray/list/tuple/dict/set/range/memoryview `for`, current stateful iterator-ref `for` target loops, flat, non-starred nested, starred, and nested starred unpacking `for` targets, normal-completion and return/break/continue/exception-propagating `try/finally`, including `break`/`continue`/`return`/new exception issued by `finally` bodies overriding a pending exception, one or more named, tuple-named, or bare `except` handlers over the current internal exception-value subset, including `except as` binding/cleanup, normal completion, first-match handler order, nested mismatch propagation, bare catch-all handlers, `else` clauses, and combined `try`/`except`/`else`/`finally` forms by modular composition, basic `match` statements over value, singleton, wildcard, simple capture, non-binding OR, non-binding AS, zero-argument builtin class patterns over current concrete value families, single-positional builtin class patterns over documented whole-object matching types with non-binding subpatterns, non-binding fixed-length sequence patterns, non-binding starred sequence `*_` patterns, top-level starred sequence `*name` capture patterns, non-binding mapping patterns over current concrete dicts with constant keys, top-level mapping `**rest` capture patterns, and guarded cases over the current pattern subset, positional-parameter `def` subsets, decorators on current function-signature families with optional suffix and sparse keyword-only defaults, and one-or-more-manager `with` statements over the current `memoryview(bytes-or-bytearray)` context-manager profile with optional simple-name, sequence, and starred sequence `as` targets exist, including `break`, `continue`, bare/value `return` propagation, exception propagation through current while/for/with header and body continuations, and loop `else` for the current while/for subsets, with bytes/bytearray/memoryview yielding integers, dictionaries yielding keys, and sets using the current internal set iteration order profile, and with unpacking support for current concrete string/bytes/bytearray/list/tuple/dict/set/range item values plus current iterator-ref item values in the supported target profiles. Arbitrary/user iteration, broader `except` matching forms, `except*`, real exception objects, general context-manager protocol, broader `match` patterns/protocol behavior, full function objects/frames, classes, coroutines, type parameter lists, annotations remain. |
| Top-level components | Language Reference, chapter 9 | partial | Current adapter-backed file input parses supported complete Python files through CPython `ast.parse(..., mode='exec')`, including the current file-input framing smoke case for shebang/comment/coding-line comments, blank lines, compound statements, semicolon-separated simple statements, and final expression framing before `ENDMARKER`; interactive and eval modes have current adapter-backed start-symbol smoke cases. Needs native or verified full `file_input`, `single_input`, and `eval_input` frontends, invalid encoding diagnostics, exact newline/EOF behavior, source spans, `__main__`/builtins/sys initialization, REPL/displayhook behavior, incomplete-input behavior, and full construct coverage beyond the adapter subset. |
| Full grammar | Language Reference, chapter 10 | partial | A first CPython PEG grammar profile pins Python 3.14.6 `Grammar/python.gram` and `Doc/reference/grammar.rst` by SHA-256, records the 249-rule inventory, 61 `invalid_*` second-pass diagnostic rules, and four start rules, and documents the current `ast.parse(...)` parser-oracle boundary used by the construct-preserving adapter for the executable subset. A first construct-preserving CPython-AST adapter exists for the current executable subset, including ordinary `//`, list displays, tuple displays, and simple `JoinedStr`/`FormattedValue` f-string nodes with conversion, empty format-spec markers, literal non-empty format-spec markers, and dynamic nested format-spec part markers over the current format subset. The full grammar must still be mirrored construct-by-construct or covered by a verified parser frontend that preserves construct identity, concrete syntax distinctions, token/source spans, soft keywords, and `invalid_*` diagnostics. |
| Built-ins and core library | Standard Library reference | partial | Internal `range`, `len` with strings counted by Unicode code points, `bool`, `str`/`repr`/`ascii` over the current int/bool/None/Ellipsis/simple-string/bytes/bytearray/special-float/list/tuple/dict/set/range/slice representation subset recursively over supported container and slice-bound elements, `format` over the current string-conversion domain with literal or evaluated format-spec expressions over the current supported subset, including current string `s`/default width/alignment/precision subset using Unicode code-point length with sign/alternate/`=` alignment, `z` option, and incompatible numeric presentation-type `ValueError` diagnostics and current int-like `b`/`c`/`d`/`o`/`x`/`X` sign/alternate/width/alignment/zero-padding/grouping subset with integer-precision, `z` option, string-presentation-type, locale-independent `n` grouping/precision/`z`, signed-`c`, alternate-`c` `ValueError` diagnostics, and non-surrogate Unicode scalar-value `c` output, plus special float-value presentation rendering for `e`/`E`/`f`/`F`/`g`/`G`/`n`/`%` and default non-empty numeric specs over `inf`/`-inf`/`nan` with sign, `z`, `#`, precision-ignored special payloads, fill/alignment, zero-padding, width, width and fractional comma/underscore grouping for non-`n` float presentation types, grouped-`n` `ValueError` diagnostics, and missing-precision `ValueError` diagnostics, including CPython-compatible string quote selection, quote/backslash/tab/newline/carriage-return escape rendering, internal `\xhh` helper rules for other ASCII controls, CPython-compatible non-ASCII `ascii` escaping over adapter-emitted UTF-8 strings, bytes quote selection and byte escape rendering, plus special-float `inf`/`-inf`/`nan` spelling, `chr(x)`/`ord(x)` over current non-surrogate Unicode scalar-value strings/code points with current `ValueError`/`TypeError` diagnostics and bytes/bytearray `ord` over one-byte values, `all`, `any`, `sorted` over current concrete comparable iterables including memoryview over bytes/bytearray with fresh list results, reverse= truthiness, key= callable sorting over current order-comparable key results, and stable equal-key ordering, `sum` over current numeric/list/tuple/addable and memoryview byte values with unsupported-start/item `TypeError` diagnostics, int-like, memoryview byte, float-containing list/tuple, and string one-argument `min`/`max` over Unicode code-point items with empty iterable `ValueError`, `default=` support, and `key=` support for current int-like and float-compatible numeric key-result profiles, int-like and float-compatible numeric multi-argument `min`/`max` with `default=` `TypeError` diagnostics and `key=` support for current int-like and float-compatible numeric key-result profiles, `int`, numeric and simple string/bytes/bytearray `float`, including CPython-profile Unicode-whitespace-padded numeric strings and ASCII-whitespace-padded bytes/bytearray, valid integer-string/bytes/bytearray underscores, matching base prefixes, base-0 integer autodetection, finite decimal-point and small-exponent float strings/bytes/bytearray with valid underscore digit separators, and case-insensitive float `inf`/`infinity`/`nan` special strings/bytes/bytearray, numeric `complex()` positional, `real`/`imag` keyword, current concrete-dict keyword-unpacked, and simple real-only, imaginary-only, unparenthesized real-plus-imaginary, or parenthesized string construction with valid underscore separators, optional surrounding Python string whitespace, and `inf`/`infinity`/`nan` special strings in the supported numeric parts, numeric `real`/`imag` attributes, int-like `numerator`/`denominator` attributes, no-argument numeric `conjugate()`, `abs`, `divmod`, two-argument `pow`, and nonnegative-exponent plus invertible negative-exponent three-argument modular `pow` subsets and current int-like/finite-float `round` with half-even behavior exist, plus the adapter-backed `Ellipsis` singleton and shadowable builtins-name fallback, no-argument `list()`, `tuple()`, `dict()`, `set()`, and `bytes()`/`bytearray()` constructors, ordered-iterable `list(x)`/`tuple(x)` for current concrete values, with strings itemized by Unicode code point and including bytes, bytearray, memoryview, and set values through the current internal set iteration order profile, `dict(x)` for current dict values and list/tuple pair iterables, `dict(key=value)`/`dict(**d)`/`dict(x, key=value, **d)` for simple keyword arguments and current concrete-dict keyword unpacking, `set(x)` for current concrete iterables including bytes, bytearray, and memoryview, `bytes(source)` for current int-like lengths, existing bytes and bytearray values, current memoryview values over bytes/bytearray, bytes(string, encoding[, errors]) for current utf-8, ascii, and latin-1 string encoding with strict, ignore, replace, backslashreplace, and xmlcharrefreplace error handling plus current lazy handler lookup for encodable strings, and concrete byte-int list/tuple/dict-key/set/range iterables with TypeError/ValueError diagnostics, simple-name `bytes.count(sub[, start[, end]])`/`bytes.find(sub[, start[, end]])`/`bytes.rfind(sub[, start[, end]])`/`bytes.index(sub[, start[, end]])`/`bytes.rindex(sub[, start[, end]])` methods over current concrete bytes values with integer-byte or bytes-like needles, optional slice-style start/end windows, non-overlapping count semantics, `find`/`rfind` `-1` not-found results, `index`/`rindex` `ValueError` not-found diagnostics, and current `TypeError`/`ValueError` needle diagnostics, and `bytes.split([sep[, maxsplit]])`/`bytes.rsplit([sep[, maxsplit]])` returning lists of fresh bytes subsequences for explicit bytes-like separators or default ASCII-whitespace runs with directional `maxsplit`, and `bytes.partition(sep)`/`bytes.rpartition(sep)` returning 3-tuples of bytes subsequences split around first/last nonempty bytes-like separators, and `bytes.removeprefix(prefix)`/`bytes.removesuffix(suffix)` returning fresh bytes values after nonempty bytes-like affix removal, and `bytes.splitlines([keepends])` returning lists of fresh bytes line segments split at ASCII LF/CR/CRLF boundaries with optional kept line endings, and `bytes.isascii()`/`bytes.isdigit()`/`bytes.isalpha()`/`bytes.isalnum()`/`bytes.islower()`/`bytes.isupper()`/`bytes.isspace()`/`bytes.istitle()` returning booleans over ASCII byte classes, and `bytes.lower()`/`bytes.upper()`/`bytes.capitalize()`/`bytes.swapcase()`/`bytes.title()` returning ASCII case-converted fresh bytes values, and `bytes.lstrip([bytes])`/`bytes.rstrip([bytes])`/`bytes.strip([bytes])` returning fresh bytes values after default ASCII-whitespace or bytes-like strip-set removal, and `bytes.center(width[, fillbyte])`/`bytes.ljust(width[, fillbyte])`/`bytes.rjust(width[, fillbyte])` returning width-justified fresh bytes values with default-space padding and one-byte bytes/bytearray fill validation, and `bytes.expandtabs([tabsize])` returning fresh bytes values after ASCII tab expansion with LF/CR column resets, and `bytes.hex([sep[, bytes_per_sep]])` returning lowercase hexadecimal strings with optional ASCII string/bytes separators grouped by signed `bytes_per_sep`, and `bytes.fromhex(string)` decoding hexadecimal strings or ASCII bytes-like inputs with ASCII-whitespace separators between byte pairs, and `bytes.replace(old, new[, count])` returning fresh bytes values after left-to-right non-overlapping bytes-like substitution with optional count, and `bytes.join(iterable)` returning fresh bytes values from concrete iterables of bytes-like items separated by the receiver, and `bytes.startswith(prefix[, start[, end]])`/`bytes.endswith(suffix[, start[, end]])` returning affix booleans for bytes-like or tuple affix arguments, and `bytes.zfill(width)` returning width-padded fresh bytes values with leading-sign handling, and `bytearray(source)`/`bytearray(string, encoding[, errors])` for the same current source and encoding profile plus read-only binary sequence operations, simple-name `bytearray.append(x)`/`bytearray.extend(x)`/`bytearray.clear()`/`bytearray.copy()`/`bytearray.reverse()`/`bytearray.pop([index])`/`bytearray.remove(value)`/`bytearray.insert(index, value)`/`bytearray.count(sub)`/`bytearray.index(sub)`/`bytearray.find(sub)`/`bytearray.rfind(sub)`/`bytearray.rindex(sub)`/`bytearray.startswith(prefix)`/`bytearray.endswith(suffix)`/`bytearray.expandtabs([tabsize])`/`bytearray.hex([sep[, bytes_per_sep]])`/`bytearray.fromhex(string)`/`bytearray.removeprefix(prefix)`/`bytearray.removesuffix(suffix)`/`bytearray.replace(old, new[, count])`/`bytearray.join(iterable)`/`bytearray.split([sep[, maxsplit]])`/`bytearray.rsplit([sep[, maxsplit]])`/`bytearray.splitlines([keepends])`/`bytearray.partition(sep)`/`bytearray.rpartition(sep)`/`bytearray.isascii()`/`bytearray.isdigit()`/`bytearray.isalpha()`/`bytearray.isalnum()`/`bytearray.islower()`/`bytearray.isupper()`/`bytearray.isspace()`/`bytearray.istitle()`/`bytearray.lower()`/`bytearray.upper()`/`bytearray.capitalize()`/`bytearray.swapcase()`/`bytearray.title()`/`bytearray.center(width[, fillbyte])`/`bytearray.ljust(width[, fillbyte])`/`bytearray.rjust(width[, fillbyte])`/`bytearray.lstrip([bytes])`/`bytearray.rstrip([bytes])`/`bytearray.strip([bytes])`/`bytearray.zfill(width)` methods over current concrete bytearray values, with `None` returns for mutators, `copy()` returning a bytearray value, `pop()` returning and removing indexed byte values, `remove()` removing the first matching byte, `insert()` inserting clamped-index byte values, `count()` returning integer-byte or bytes-like subsequence occurrence counts, `index()`/`rindex()` returning first/last integer-byte or bytes-like subsequence positions, and `find()`/`rfind()` returning first/last integer-byte or bytes-like needle positions or `-1`, with optional slice-style `start`/`end` windows for all five search methods, and `startswith()`/`endswith()` returning affix booleans for bytes-like or tuple affix arguments over whole values or optional slice-style `start`/`end` windows, `expandtabs()` returning fresh bytearray values after ASCII tab expansion with LF/CR column resets, `hex()` returning lowercase hexadecimal strings with optional ASCII string/bytes separators grouped by signed `bytes_per_sep`, `fromhex()` decoding hexadecimal strings or ASCII bytes-like inputs with ASCII-whitespace separators between byte pairs into fresh bytearray values, and `removeprefix()`/`removesuffix()` returning fresh bytearray values with a matching bytes-like affix removed, `replace()` returning fresh bytearray values after left-to-right non-overlapping bytes-like substitution with optional count, `join(iterable)` returning fresh bytearray values from concrete iterables of bytes-like items separated by the receiver, `split()`/`rsplit()` returning lists of fresh bytearray subsequences for explicit bytes-like separators or default ASCII-whitespace runs with directional `maxsplit`, `splitlines()` returning lists of fresh bytearray line segments split at ASCII LF/CR/CRLF boundaries with optional kept line endings, partition/rpartition returning three-bytearray tuples split around first/last nonempty bytes-like separators, and ASCII classification methods returning booleans over ASCII byte classes, and ASCII case-conversion methods returning fresh bytearray values, center/ljust/rjust returning width-justified fresh bytearray values with default-space padding and one-byte bytes/bytearray fill validation, strip/lstrip/rstrip returning fresh bytearray values after default ASCII-whitespace or bytes-like strip-set removal, and zfill returning width-padded fresh bytearray values with leading-sign handling, and current `TypeError`/`ValueError`/`IndexError` diagnostics; `memoryview(source)` over current bytes/bytearray values participates in the concrete iteration/list/tuple/set/bytes/bytearray/sorted/all/any/sum/min/max profile over yielded integer bytes; no full builtins namespace, general object protocol, remaining bytearray mutation/methods beyond current simple-name , native parser coverage for control-character string escapes, surrogate code point string support, finite float and complex string conversion, recursive cycle handling, or full object `str`/`repr` dispatch yet. |

## Construct Families

| Family | Status | Required next evidence |
| --- | --- | --- |
| Source encodings and tokenizer behavior | partial | Current adapter-backed source-file frontend reads raw bytes and delegates documented source decoding to CPython `ast.parse`/`compile`, with a Latin-1 coding-cookie regression. Needs native K tokenizer-level encoding detection, UTF-8 BOM handling and BOM/cookie conflicts, invalid-encoding diagnostics, exact source spans, and non-adapter parser coverage. |
| Indentation and block structure | not-started | Parser tests for `INDENT`/`DEDENT`, mixed tabs/spaces diagnostics, blank/comment-only lines. |
| Names, keywords, soft keywords | partial | Initial `Id` handling only; needs Python identifier Unicode rules and keyword/soft-keyword separation. |
| Literals | partial | Int, simple finite float, adapter-backed imaginary/complex, simple string, CPython-AST-backed source string/bytes literal values for raw, adjacent, ignored-newline, named-character, hex, octal, and byte-boundary escape spellings, adapter-backed simple f-string interpolation plus current string-conversion-subset `!s`/`!r`/`!a` conversion, debug prefix, empty, literal non-empty, and dynamic nested format specs over the current format subset, adapter-backed bytes constants, bool, None, and adapter-backed ellipsis atom smoke coverage; native K parser coverage and diagnostics for full Python string/bytes prefixes and escapes, direct bytes parser syntax and diagnostics, full float lexical edge cases, object `__format__` dispatch, and t-strings remain. |
| Object model | partial | Minimal value model with singleton identity plus the current user-class profile: fresh class values, fresh user object values, class-attribute payloads from simple class-body assignments and simple method definitions, single-base user-class lineage, inherited class attributes and methods with child override precedence, current per-object instance attribute storage, exact-class and current single-base inherited runtime class attribute assignment/deletion over direct class aliases, env-bound instances, and current list/tuple/dict-value-held class and instance snapshots, already-imported fixed-module attribute assignment/deletion over direct module aliases and current list/tuple/dict-value-held module snapshots, instance-before-class fallback attribute lookup, simple bound methods, current identity/default equality, truthiness, callability, dynamic `isinstance(obj, ClassName)` checks, and dynamic user-class `issubclass(ClassName, BaseName)` checks. Needs full heap objects, real type objects, standard object/type hierarchy, broader mutability and lifetime behavior, descriptors, real module-cache identity, multiple inheritance, full MRO, metaclasses, and introspection. |
| Numeric operations | partial | Int/bool unary `+`/`-`/`~`, `+`, `-`, `*`, `/`, `%`, `//`, shifts, bitwise operators, `**`, float and mixed int/bool/float `+`, `-`, `*`, `/`, `//`, `%`, `**` with current `ZeroDivisionError` diagnostics for zero divisors in true division, floor division, modulo, and `divmod`, ordering comparisons, complex unary `+`/`-`, complex equality/truthiness, complex `+`/`-`/`*`/nonzero `/` plus current `ZeroDivisionError` diagnostics for zero complex divisors over current int/bool/float/complex values, complex-base integer-like exponentiation, numeric `real`/`imag` attributes, int-like `numerator`/`denominator` attributes, and no-argument numeric `conjugate()` over current int/bool/float/complex values, adapter-backed `int()`/`int(x)` for current int-like values and simple digit strings with CPython-profile Unicode numeric whitespace and bytes/bytearray with optional surrounding ASCII whitespace, optional sign, optional valid underscore digit separators, explicit bases 2 through 36, matching base prefixes, and base-0 autodetection, adapter-backed `float()` and `float(x)` for current int-like/float values plus simple signed ASCII integer strings with CPython-profile Unicode numeric whitespace and ASCII-whitespace-padded bytes/bytearray with optional valid underscore digit separators, finite decimal-point strings with valid underscore digit separators, finite exponent strings with valid underscore digit separators and small decimal exponents, and case-insensitive `inf`/`infinity`/`nan` special strings plus current `ValueError`/`TypeError` diagnostics for invalid current string/bytes/bytearray numeric inputs and explicit `int` base misuse, adapter-backed `complex()`/`complex(x)`/`complex(real, imag)` plus `complex(real=...)`, `complex(imag=...)`, `complex(x, imag=...)`, current concrete-dict `complex(**kwargs)`, and simple real-only, imaginary-only, unparenthesized real-plus-imaginary, or parenthesized `complex(str)` forms with valid underscore separators, optional surrounding Python string whitespace, and `inf`/`infinity`/`nan` special strings for current int/bool/float/complex values and numeric strings, with current `ValueError` diagnostics for malformed strings and current `TypeError` diagnostics for unsupported concrete operands, int-like/float and finite non-overflowing complex `abs(x)` plus complex infinity/NaN special cases, including special-float infinity and NaN behavior plus zero-value behavior with `TypeError` diagnostics for current concrete non-numeric operands, int-like/float `divmod(x, y)` with current `ZeroDivisionError` diagnostics for zero divisors and `TypeError` diagnostics for unsupported current operands, two-argument `pow(x, y)` including complex bases with int-like exponents, negative float bases with int-like exponents, negative real bases with integral float exponents, and the current finite positive-base real float subset with current `TypeError` diagnostics for concrete non-numeric operands, nonnegative-exponent and invertible negative-exponent three-argument modular `pow(x, y, mod)` with current `TypeError` diagnostics for non-int-like modular operands and current `ValueError` diagnostics for zero-modulus and non-invertible negative-exponent modular `pow`, current int-like/finite-float `round` with half-even behavior, concrete string/bytes/bytearray/list/tuple repetition and same-family concatenation with current invalid-repeat-count and cross-type concatenation `TypeError` diagnostics, concrete-iterable `sum(x)`/`sum(x, start)` over current numeric/list/tuple addable values with unsupported-start/item `TypeError` diagnostics, one-argument `min(x)`/`max(x)` over current concrete int-like, float-containing list/tuple, and string iterables with empty iterable `ValueError`, `default=` support, and `key=` support for current int-like and float-compatible numeric key-result profiles, and multi-argument `min(a, b, ...)`/`max(a, b, ...)` over current int-like and float-compatible numeric values with `default=` `TypeError` diagnostics and `key=` support for current int-like and float-compatible numeric key-result profiles, with current `ZeroDivisionError` diagnostics for zero-base negative integer/bool/float/complex power and `pow` exponents; adapter smoke now exercises real Python `//` and `/` spelling, bytearray numeric constructor sources, numeric constructor Unicode whitespace, numeric constructor error classes, complex constructor error classes, and matrix multiplication `@` with TypeError diagnostics for current built-in operands. Python numeric tower, general complex exponentiation with non-integer exponents, int object conversion and exact numeric-constructor diagnostic text, float object conversion and exact numeric-constructor diagnostic text, exponent overflow/underflow and decimal rounding-boundary/error behavior, NaN sign preservation, complex object conversion, complex keyword diagnostics/warnings, exact large-int/float comparison behavior, full coercions, complex abs() magnitude for overflow-scale edge cases, negative-base complex power results, and float edge/error behavior remain. |
| Truth-value testing | partial | Simple int/float/complex/string/bytes/bytearray/memoryview/list/tuple/dict/set/range/bool/None/Ellipsis truthiness, adapter-backed `bool()`/`bool(x)`, concrete-iterable `all(x)`/`any(x)` for those values, and short-circuit `and`/`or`; needs `__bool__`, `__len__`, `__iter__`, full containers, lazy iterator side effects, and user-defined objects. |
| Name binding and scope | partial | Single environment only, including simple-name assignment expressions in that current environment; needs module/function/class scopes, globals, nonlocals, comprehensions, annotation scopes. |
| Assignment | partial | Single-name assignment, adapter-backed simple-name assignment expressions, adapter-backed simple-name annotated assignment with annotation expressions erased in the current no-introspection profile, adapter-backed multi-target simple-name assignment, current user-object attribute assignment targets such as `self.x = expr` with RHS-before-target-object evaluation and per-object instance attribute updates, plus exact-class class attribute assignment targets such as `C.x = expr` over direct class aliases, env-bound instances, and current list/tuple/dict-value-held class and instance snapshots, including current single-base base-class propagation and subclass-own shadowing, plus already-imported fixed-module attribute assignment targets such as `math.x = expr` over direct module aliases and current list/tuple/dict-value-held module snapshots, simple named list/dict subscription assignment with current list-target `IndexError`/`TypeError` diagnostics, simple named list/dict subscription augmented assignment with current pre-RHS `IndexError`/`KeyError`/`TypeError` diagnostics, and two-level simple-name-rooted list/dict subscription assignment in the current value-store profile, simple named list extended slice assignment with `ValueError` for mismatched non-unit-step replacement lengths and zero-step targets, flat sequence unpacking assignment to simple names over current string/bytes/bytearray/list/tuple/dict/set/range RHS values with current arity `ValueError` diagnostics, non-starred nested sequence unpacking assignment to simple names over current string/bytes/bytearray/list/tuple/dict/set/range RHS values with current top-level arity `ValueError` diagnostics, starred and nested starred sequence unpacking assignment with simple-name starred captures over current string/bytes/bytearray/list/tuple/dict/set/range RHS values and top-level starred-too-few `ValueError` diagnostics, and simple-name augmented assignment for current integer-like operators, true/floor division, and current `@=` TypeError diagnostics, with bytes/bytearray yielding integers, dictionaries yielding keys, and sets using the current internal set iteration order profile; needs general iterable unpacking, attribute assignment beyond current user-object instance attributes, the exact/single-base direct-binding plus list/tuple/dict-value class mutation subset, and the current direct-binding plus list/tuple/dict-value fixed-module mutation subset, subscription assignment beyond current simple/two-level name-rooted list/dict value-store targets, subscript target diagnostics outside the current simple named list/dict subset, heap object identity/aliasing beyond the current user-object profile, annotation metadata/scopes, in-place/special-method dispatch, recursive unpacking diagnostics with Python's partial left-to-right binding behavior, non-name starred targets, and full target evaluation order. |
| Calls and functions | partial | Positional-argument lambda values and function definitions, positional lambda/function defaults evaluated at lambda/function definition time, function/lambda varargs parameters including suffix defaults and keyword binding for fixed parameters, function/lambda `*args` plus keyword-only signatures including sparse keyword-only defaults and kwargs collection, function/lambda `*args, **kwargs` signatures including suffix defaults, function/lambda `**kwargs` parameters including suffix defaults and without keyword-only parameters, function/lambda positional-only parameters including suffix defaults, varargs-only, kwargs-only, keyword-only, keyword-only-plus-kwargs, varargs-plus-kwargs, varargs-plus-keyword-only, and varargs-plus-keyword-only-plus-kwargs combinations, function/lambda keyword-only parameters without positional parameters including sparse defaults and kwargs collection, mixed positional-plus-keyword-only definitions including sparse keyword-only defaults and kwargs collection, adapter-backed function parameter/return annotations erased in the current no-introspection profile, adapter-backed decorators for current function-signature families with optional suffix and sparse keyword-only defaults, top-to-bottom decorator-expression evaluation, definition-time default evaluation before decorator calls, and bottom-to-top application, bare and value returns, recursive calls, zero/multi-positional calls, keyword-only calls without positional arguments, mixed positional/keyword calls, starred positional call unpacking over current ordered concrete string/bytes/bytearray/list/tuple/dict/set/range values, with strings itemized by Unicode code point, bytes/bytearray yielding integers and sets using the current internal set iteration order profile, keyword argument unpacking over current concrete dictionary values with string keys, and current wrong positional arity TypeError diagnostics for ordinary one-, zero-, and multi-positional function/lambda calls, plus adapter-backed simple materialized generator functions over top-level plain yield statements with fresh stateful iterator refs and current bound-method calls that prepend the receiver for simple class-stored function values; needs closures/cells, annotation metadata/scopes, type parameters, full descriptor method binding, remaining argument-binding diagnostics, and real frames. |
| Containers and comprehensions | partial | Minimal string/bytes/bytearray/list/tuple/dict/set value displays, adapter-backed expression evaluation for list/tuple/dict/set displays including starred list/tuple/set display unpacking over current ordered concrete string/bytes/bytearray/list/tuple/dict/set/range values, with sets using the current internal set iteration order profile, non-async one-or-more-generator list comprehensions over current concrete iterables with simple-name targets or current recursive sequence targets, zero or more filter clauses on each generator, and restored outer binding, non-async one-or-more-generator dict comprehensions over current concrete iterables with simple-name targets or current recursive sequence targets, zero or more filter clauses on each generator, duplicate-key replacement, and restored outer binding, non-async one-or-more-generator set comprehensions over current concrete iterables with simple-name targets or current recursive sequence targets, zero or more filter clauses on each generator, duplicate-element normalization, and restored outer binding, dictionary display unpacking over current concrete dict values, no-argument `list()`, `tuple()`, `dict()`, `set()`, `bytes()`, and `bytearray()` constructors, `list(x)`/`tuple(x)` over current ordered string/bytes/bytearray/list/tuple/dict/set/range/memoryview values, with strings itemized by Unicode code point, `dict(x)` over current dict values and concrete list/tuple pair iterables, `dict(key=value)`/`dict(**d)`/`dict(x, key=value, **d)` over current simple keyword arguments and current concrete-dict keyword unpacking with TypeError diagnostics for duplicate evaluated keyword names, `set(x)` over current string/bytes/bytearray/list/tuple/dict/set/range/memoryview values, `bytes(source)` over current int-like lengths, existing bytes/bytearray values, current memoryview values over bytes/bytearray, bytes(string, encoding[, errors]) for current utf-8, ascii, and latin-1 string encoding with strict, ignore, replace, backslashreplace, and xmlcharrefreplace error handling plus current lazy handler lookup for encodable strings, and concrete byte-int list/tuple/dict-key/set/range iterables, `bytearray(source)`/`bytearray(string, encoding[, errors])` over the same current source and encoding profile, simple-name `bytearray.append(x)`/`bytearray.extend(x)`/`bytearray.clear()`/`bytearray.copy()`/`bytearray.reverse()`/`bytearray.pop([index])`/`bytearray.remove(value)`/`bytearray.insert(index, value)`/`bytearray.count(sub)`/`bytearray.index(sub)`/`bytearray.find(sub)`/`bytearray.rfind(sub)`/`bytearray.rindex(sub)`/`bytearray.startswith(prefix)`/`bytearray.endswith(suffix)`/`bytearray.expandtabs([tabsize])`/`bytearray.hex([sep[, bytes_per_sep]])`/`bytearray.fromhex(string)`/`bytearray.removeprefix(prefix)`/`bytearray.removesuffix(suffix)`/`bytearray.replace(old, new[, count])`/`bytearray.join(iterable)`/`bytearray.split([sep[, maxsplit]])`/`bytearray.rsplit([sep[, maxsplit]])`/`bytearray.splitlines([keepends])`/`bytearray.partition(sep)`/`bytearray.rpartition(sep)`/`bytearray.isascii()`/`bytearray.isdigit()`/`bytearray.isalpha()`/`bytearray.isalnum()`/`bytearray.islower()`/`bytearray.isupper()`/`bytearray.isspace()`/`bytearray.istitle()`/`bytearray.lower()`/`bytearray.upper()`/`bytearray.capitalize()`/`bytearray.swapcase()`/`bytearray.title()`/`bytearray.center(width[, fillbyte])`/`bytearray.ljust(width[, fillbyte])`/`bytearray.rjust(width[, fillbyte])`/`bytearray.lstrip([bytes])`/`bytearray.rstrip([bytes])`/`bytearray.strip([bytes])`/`bytearray.zfill(width)` methods over current concrete bytearray values, with `None` returns for mutators, `copy()` returning a bytearray value, `pop()` returning and removing indexed byte values, `remove()` removing the first matching byte, `insert()` inserting clamped-index byte values, `count()` returning integer-byte or bytes-like subsequence occurrence counts, `index()`/`rindex()` returning first/last integer-byte or bytes-like subsequence positions, and `find()`/`rfind()` returning first/last integer-byte or bytes-like needle positions or `-1`, with optional slice-style `start`/`end` windows for all five search methods, and `startswith()`/`endswith()` returning affix booleans for bytes-like or tuple affix arguments over whole values or optional slice-style `start`/`end` windows, `expandtabs()` returning fresh bytearray values after ASCII tab expansion with LF/CR column resets, `hex()` returning lowercase hexadecimal strings with optional ASCII string/bytes separators grouped by signed `bytes_per_sep`, `fromhex()` decoding hexadecimal strings or ASCII bytes-like inputs with ASCII-whitespace separators between byte pairs into fresh bytearray values, and `removeprefix()`/`removesuffix()` returning fresh bytearray values with a matching bytes-like affix removed, `replace()` returning fresh bytearray values after left-to-right non-overlapping bytes-like substitution with optional count, `join(iterable)` returning fresh bytearray values from concrete iterables of bytes-like items separated by the receiver, `split()`/`rsplit()` returning lists of fresh bytearray subsequences for explicit bytes-like separators or default ASCII-whitespace runs with directional `maxsplit`, `splitlines()` returning lists of fresh bytearray line segments split at ASCII LF/CR/CRLF boundaries with optional kept line endings, partition/rpartition returning three-bytearray tuples split around first/last nonempty bytes-like separators, and ASCII classification methods returning booleans over ASCII byte classes, and ASCII case-conversion methods returning fresh bytearray values, center/ljust/rjust returning width-justified fresh bytearray values with default-space padding and one-byte bytes/bytearray fill validation, strip/lstrip/rstrip returning fresh bytearray values after default ASCII-whitespace or bytes-like strip-set removal, and zfill returning width-padded fresh bytearray values with leading-sign handling, and current `TypeError`/`ValueError`/`IndexError` diagnostics, `memoryview(source)` over current bytes/bytearray values with current truthiness, `len`, `isinstance(..., memoryview)`, `for` iteration, concrete iterable builtins, list/tuple/set conversion, bytes/bytearray constructor conversion, and comprehension iteration support, duplicate dictionary key replacement in the supported key-equality subset, duplicate set element normalization through the adapter, string/list/tuple/bytes/bytearray repetition with integer-like counts plus current invalid-repeat-count `TypeError` diagnostics, string nonzero-step slicing by Unicode code point plus list/tuple/bytes/bytearray nonzero-step slicing with current zero-step `ValueError` diagnostics, slice-object subscriptions over current string/list/tuple/bytes/bytearray/range values, string substring membership, bytes/bytearray integer and subsequence membership with current ValueError diagnostics for out-of-range integer needles and TypeError diagnostics for invalid non-bytes-like needles, string iteration by Unicode code point and bytes/bytearray/memoryview iteration, lexicographic ordering, and positive/negative indexing with current out-of-range `IndexError` and invalid-index `TypeError` diagnostics, string/bytes/bytearray/list/tuple same-family concatenation with current cross-type sequence concatenation `TypeError` diagnostics, list/tuple/dict/set/bytes/bytearray equality, list/tuple/bytes/bytearray lexicographic ordering, set-to-set ordering comparisons, membership, truthiness, positive/negative indexing with current out-of-range `IndexError` and invalid-index `TypeError` diagnostics, simple named list subscription mutation/deletion with current out-of-range `IndexError` and invalid-index `TypeError` diagnostics, dict key membership, dict key lookup with current missing-key `KeyError` and unsupported-key `TypeError` diagnostics, simple named dict subscription augmented assignment/deletion with current missing-key `KeyError` and unsupported-key `TypeError` diagnostics, dict key iteration, set iteration in current `for` loops using the current internal set iteration order profile, and adapter-backed `range(stop)`/`range(start, stop)`/`range(start, stop, step)` values for `for` loops, equality, integer-like and float-equality membership, positive/negative indexing with current out-of-range `IndexError` and invalid-index `TypeError` diagnostics, nonzero-step slicing, current zero-step `ValueError` diagnostics, and `len`; direct K smoke uses trailing-comma input while adapter smoke accepts ordinary Python displays for the supported subset. Needs broader constructor arguments/iterable conversion, broader bytes constructor encodings, remaining namereplace/custom error handlers, buffer behavior, mapping protocol, remaining bytes methods beyond current simple-name  and remaining bytearray mutation/methods beyond current simple-name  plus full memoryview behavior, async list comprehensions, async dict comprehensions, async set comprehensions, full lazy generator expression object/frame semantics, nonempty set unpacking/order profiles, mutation beyond the current simple subscript/slice/bytearray append/extend/clear/copy/reverse/pop/remove/insert/count/index value-store subset, broader slice object behavior, repeated-list mutable-element aliasing behavior, subscription IndexError/KeyError/TypeError behavior outside the current concrete expression/simple mutation subset, full `__index__` and hash protocol behavior, dictionary views/iterator mutation behavior, full range object behavior, iterators, and full comprehensions. |
| Attribute access and descriptors | partial | Adapter-backed `range.start`, `range.stop`, `range.step`, `slice.start`, `slice.stop`, `slice.step`, numeric `real`/`imag`, and int-like `numerator`/`denominator` attribute references exist for current concrete range, slice, int/bool/float, and complex values, plus current user-class class attributes, user-object instance attributes, class fallback, inherited single-base class attributes and methods, simple bound methods including current suffix defaults, plain `*args`, plain `*args` plus keyword-only parameters, plain `*args` plus keyword-only plus `**kwargs` parameters, plain `*args, **kwargs`, and `**kwargs`, staticmethod unwrapping, classmethod owner-class binding, property getter invocation on instance direct lookup and literal/dynamic `getattr`, current same-name class-body property getter/setter/deleter chaining, current property setter invocation/read-only `AttributeError` on instance assignment, current property deleter invocation/read-only `AttributeError` on attribute deletion, current user-object instance attribute deletion with class fallback, exact-class class attribute assignment/deletion through direct dot targets and `setattr`/`delattr` over direct class aliases, env-bound instances, and current list/tuple/dict-value-held class and instance snapshots, including current single-base base-class propagation, inherited-only subclass deletion errors, and subclass-own shadowing, already-imported fixed-module attribute assignment/deletion through direct dot targets and `setattr`/`delattr` over direct module aliases and current list/tuple/dict-value-held module snapshots, and adapter-backed `getattr`/`hasattr` with literal and evaluated string names over that current attribute profile, including known missing-attribute behavior and non-string-name `TypeError`. Needs `object.__getattribute__`, full descriptor protocol, property constructor calls and property method chaining beyond the current same-name class-body decorator profile, exact property object identity/introspection/doc behavior, full data-descriptor precedence, method binding beyond the current class-stored user-function/staticmethod/classmethod/property profile, special method lookup, module attributes beyond the current already-imported fixed-module subset, real `sys.modules` identity, multiple inheritance/MRO, class attribute mutation/deletion beyond the current exact/single-base direct-binding plus list/tuple/dict-value subset, broader instance deletion, and full attribute diagnostics. |
| Classes and metaclasses | partial | Adapter-backed current-profile class definitions initialize `__module__`, top-level `__qualname__`, and `__doc__` as ordinary class attributes; `class C: pass` and class bodies containing `pass` plus simple `NAME = expr` assignments lower to `#classAttrs(C, ...)`, simple ordinary-positional class-body method definitions without or with suffix defaults, plain `*args` with optional suffix defaults, plain `*args` plus keyword-only parameters with optional positional suffix defaults and sparse keyword-only defaults, plain `*args` plus keyword-only plus `**kwargs` parameters with optional positional suffix defaults and sparse keyword-only defaults, plain `*args, **kwargs` with optional suffix defaults, or `**kwargs`, positional-only parameters without or with suffix defaults, ordinary-positional-plus-keyword-only parameters without or with positional suffix defaults and sparse keyword-only defaults, ordinary-positional-plus-keyword-only-plus-`**kwargs` parameters without or with positional suffix defaults and sparse keyword-only defaults, positional-only-plus-keyword-only parameters without or with positional suffix defaults and sparse keyword-only defaults, or positional-only combinations with `*args`, keyword-only parameters, and `**kwargs` without or with supported defaults lower to `#classMethod(...)`/`#classMethodDefaults(...)`/`#classMethodVarArgs(...)`/`#classMethodVarArgsDefaults(...)`/`#classMethodVarArgsKwOnly(...)`/`#classMethodVarArgsKwDefaults(...)`/`#classMethodVarArgsKwOnlyKwArgs(...)`/`#classMethodVarArgsKwDefaultsKwArgs(...)`/`#classMethodVarKwArgs(...)`/`#classMethodVarKwArgsDefaults(...)`/`#classMethodKwArgs(...)`/`#classMethodKwArgsDefaults(...)`/`#classMethodPosOnly(...)`/`#classMethodPosOnlyDefaults(...)`/`#classMethodKwOnly(...)`/`#classMethodKwDefaults(...)`/`#classMethodKwOnlyKwArgs(...)`/`#classMethodKwDefaultsKwArgs(...)`/`#classMethodPosOnlyKwOnly(...)`/`#classMethodPosOnlyKwOnlyDefaults(...)`, `@staticmethod` and `@classmethod` methods without or with suffix defaults, plain `*args` with optional suffix defaults, plain `*args` plus keyword-only parameters with optional positional suffix defaults and sparse keyword-only defaults, plain `*args` plus keyword-only plus `**kwargs` parameters with optional positional suffix defaults and sparse keyword-only defaults, plain `*args, **kwargs` with optional suffix defaults, or `**kwargs`, positional-only parameters without or with suffix defaults, ordinary-positional-plus-keyword-only parameters without or with positional suffix defaults and sparse keyword-only defaults, ordinary-positional-plus-keyword-only-plus-`**kwargs` parameters without or with positional suffix defaults and sparse keyword-only defaults, positional-only-plus-keyword-only parameters without or with positional suffix defaults and sparse keyword-only defaults, or positional-only combinations with `*args`, keyword-only parameters, and `**kwargs` without or with supported defaults lower to `#classStaticMethod(...)`/`#classStaticMethodDefaults(...)`/`#classStaticMethodVarArgs(...)`/`#classStaticMethodVarArgsDefaults(...)`/`#classStaticMethodVarArgsKwOnly(...)`/`#classStaticMethodVarArgsKwDefaults(...)`/`#classStaticMethodVarArgsKwOnlyKwArgs(...)`/`#classStaticMethodVarArgsKwDefaultsKwArgs(...)`/`#classStaticMethodVarKwArgs(...)`/`#classStaticMethodVarKwArgsDefaults(...)`/`#classStaticMethodKwArgs(...)`/`#classStaticMethodKwArgsDefaults(...)`/`#classStaticMethodPosOnly(...)`/`#classStaticMethodPosOnlyDefaults(...)`/`#classStaticMethodKwOnly(...)`/`#classStaticMethodKwDefaults(...)`/`#classStaticMethodKwOnlyKwArgs(...)`/`#classStaticMethodKwDefaultsKwArgs(...)`/`#classStaticMethodPosOnlyKwOnly(...)`/`#classStaticMethodPosOnlyKwOnlyDefaults(...)` and `#classClassMethod(...)`/`#classClassMethodDefaults(...)`/`#classClassMethodVarArgs(...)`/`#classClassMethodVarArgsDefaults(...)`/`#classClassMethodVarArgsKwOnly(...)`/`#classClassMethodVarArgsKwDefaults(...)`/`#classClassMethodVarArgsKwOnlyKwArgs(...)`/`#classClassMethodVarArgsKwDefaultsKwArgs(...)`/`#classClassMethodVarKwArgs(...)`/`#classClassMethodVarKwArgsDefaults(...)`/`#classClassMethodKwArgs(...)`/`#classClassMethodKwArgsDefaults(...)`/`#classClassMethodPosOnly(...)`/`#classClassMethodPosOnlyDefaults(...)`/`#classClassMethodKwOnly(...)`/`#classClassMethodKwDefaults(...)`/`#classClassMethodKwOnlyKwArgs(...)`/`#classClassMethodKwDefaultsKwArgs(...)`/`#classClassMethodPosOnlyKwOnly(...)`/`#classClassMethodPosOnlyKwOnlyDefaults(...)`, and current `@property`, simple `@name.getter`, simple `@name.setter`, and simple `@name.deleter` methods lower to `#classProperty(...)`/`#classPropertyGetter(...)`/`#classPropertySetter(...)`/`#classPropertyDeleter(...)`, one simple non-builtin user-class base lowers to `#classBase(C, Base, ...)`, and decorators over those current class profiles lower to `#classDecorated(...)`/`#classBaseDecorated(...)`; the semantics binds a fresh class value or final decorator result, evaluates decorator expressions top to bottom, applies decorators bottom to top, evaluates simple class-body assignments and ordinary-positional, plain-`*args`, plain-`*args`-plus-keyword-only, plain-`*args`-plus-keyword-only-plus-`**kwargs`, plain-`*args, **kwargs`, `**kwargs`, ordinary-positional-plus-keyword-only-plus-`**kwargs`, positional-only, keyword-only, and positional-only-plus-keyword-only method defaults in a temporary class namespace with global and prior class-attribute lookup before restoring the outer environment and final binding, supports construction of fresh instances, dispatches current `__init__` methods including suffix defaults, plain `*args` with optional suffix defaults, plain `*args` plus keyword-only parameters with optional positional suffix defaults and sparse keyword-only defaults, plain `*args` plus keyword-only plus `**kwargs` parameters with optional positional suffix defaults and sparse keyword-only defaults, plain `*args, **kwargs` with optional suffix defaults, `**kwargs`, ordinary-positional-plus-keyword-only-plus-`**kwargs` parameters with optional positional suffix defaults and sparse keyword-only defaults, positional-only parameters with optional suffix defaults, keyword-only parameters with optional defaults, and positional-only-plus-keyword-only parameters with optional defaults and requires `None` return including inherited `__init__`, stores per-object instance attributes, supports class and instance truthiness, class callability, current identity/default equality including class-name rebinding to a fresh class object, direct class lookup, instance lookup with class fallback, inherited class attributes/methods, method overriding, simple bound methods including suffix defaults, plain `*args` with optional suffix defaults, plain `*args` plus keyword-only parameters with optional positional suffix defaults and sparse keyword-only defaults, plain `*args` plus keyword-only plus `**kwargs` parameters with optional positional suffix defaults and sparse keyword-only defaults, plain `*args, **kwargs` with optional suffix defaults, `**kwargs`, ordinary-positional-plus-keyword-only-plus-`**kwargs` parameters with optional positional suffix defaults and sparse keyword-only defaults, positional-only parameters with optional suffix defaults, keyword-only parameters with optional defaults, positional-only-plus-keyword-only parameters with optional defaults, and positional-only combinations with `*args`, keyword-only parameters, and `**kwargs` with supported defaults, staticmethod unwrapping including suffix defaults, plain `*args` with optional suffix defaults, plain `*args` plus keyword-only parameters with optional positional suffix defaults and sparse keyword-only defaults, plain `*args` plus keyword-only plus `**kwargs` parameters with optional positional suffix defaults and sparse keyword-only defaults, plain `*args, **kwargs` with optional suffix defaults, `**kwargs`, ordinary-positional-plus-keyword-only-plus-`**kwargs` parameters with optional positional suffix defaults and sparse keyword-only defaults, positional-only parameters with optional suffix defaults, keyword-only parameters with optional defaults, positional-only-plus-keyword-only parameters with optional defaults, and positional-only combinations with `*args`, keyword-only parameters, and `**kwargs` with supported defaults, classmethod owner-class binding including suffix defaults, plain `*args` with optional suffix defaults, plain `*args` plus keyword-only parameters with optional positional suffix defaults and sparse keyword-only defaults, plain `*args` plus keyword-only plus `**kwargs` parameters with optional positional suffix defaults and sparse keyword-only defaults, plain `*args, **kwargs` with optional suffix defaults, `**kwargs`, ordinary-positional-plus-keyword-only-plus-`**kwargs` parameters with optional positional suffix defaults and sparse keyword-only defaults, positional-only parameters with optional suffix defaults, keyword-only parameters with optional defaults, and positional-only-plus-keyword-only parameters with optional defaults, same-name class-body property getter/setter/deleter chaining, property getter invocation, property setter invocation/read-only `AttributeError` on instance assignment, property deleter invocation/read-only `AttributeError` on attribute deletion, `getattr`/`hasattr`, dynamic `isinstance`, dynamic user-class `issubclass` for those instances/classes, and exact-class plus current single-base inherited runtime class attribute mutation/deletion over direct class aliases, env-bound instances, and current list/tuple/dict-value-held class and instance snapshots. Needs general class body execution, namespace mapping behavior beyond current metadata initialization, nested/function-local `__qualname__` values, modules beyond the current `__main__` profile, multiple bases, builtin bases, full MRO/C3 linearization, metaclass protocol, type parameters, class decorator coverage beyond the current class-body/base profile, method decorators beyond staticmethod/classmethod/property, method-default coverage beyond the current directly enumerated class-body method profiles, property constructor calls and property method chaining beyond the current same-name class-body decorator profile, exact property object identity/introspection, full descriptor protocol, `__new__`, broader subclassing, class patterns over user classes, full class attribute mutation/deletion beyond the current exact/single-base direct-binding plus list/tuple/dict-value subset, broader instance deletion, and exact diagnostics. |
| Exceptions | partial | Adapter-backed `raise`, `raise Name`, `raise Name(args...)`, `raise Name from None`, and `raise Name from Cause` plus one or more named, tuple-named, or bare `except` handlers exist over internal exception values, including constructor-call syntax with evaluated positional arguments, active-exception tracking for bare re-raise, `RuntimeError` sentinel raising when no active exception exists, `except as` binding/cleanup, normal try-body completion, first-match handler order, current Python 3.14 built-in exception hierarchy name matching for internal sentinels plus CPython's runtime-only `_IncompleteInputError` sentinel including tuple handlers, nested mismatching-handler propagation, bare catch-all handlers, `try` `else`, `finally` execution/override for active exceptions, and combined `try`/`except`/`else`/`finally` composition in the current handler subset. `from None` and `from Cause` are preserved as distinct K constructs but have no observable cause/context object in the current exception-value profile. Needs real exception objects and hierarchy, keyword/non-name exception construction, traceback, propagation through all continuations, full active-exception/traceback/context behavior, cause/context attachment beyond construct preservation, groups, `except*`, and user-defined type-hierarchy matching. |
| Control flow | partial | Adapter-backed `if`, `while`, string/bytes/bytearray/list/tuple/dict/set/range/memoryview `for`, current stateful iterator-ref `for` target loops, flat, non-starred nested, starred, and nested starred unpacking `for` targets, loop `else`, one-or-more-manager `with memoryview(bytes-or-bytearray)` and `with ... as target` over current simple-name, sequence, and starred sequence targets with left-to-right entry, nested/reverse exit, body normal completion, body exception propagation, pre-entry exception propagation, target arity failure propagation after exit, and return/break/continue propagation through the current non-suppressing exit profile, normal-completion and return/break/continue/exception-propagating `try/finally`, including finalizer override of pending exceptions by return/break/continue/new exception, named/tuple/bare `try/except` handler lists plus `else` and `finally` over exception sentinels, `break`, `continue`, and function bare/value `return` subsets are covered, along with `pass`, current non-optimized `assert` success/failure control flow, and expression-level conditionals, with bytes/bytearray/memoryview yielding integers, dictionaries yielding keys, sets using the current internal set iteration order profile, and current concrete/iterator-ref item unpacking in the supported target profiles; needs arbitrary/user iteration, real exceptions, full suspended `yield`/`yield from` control flow, general context-manager protocol, exception suppression, user-defined/library context managers, and async control flow. |
| Import system | partial | Current adapter-backed slice covers single-module `import math`/`import keyword`/`import importlib`, `import ... as name`, arbitrary simple-name `from math`/`from keyword`/`from importlib` imports over the current fixed-module profile, alias forms over math `__name__`/`e`/`pi`/`tau`, keyword `__name__`/`__all__`/`kwlist`/`softkwlist`/`iskeyword`/`issoftkeyword`, and importlib `__name__`/`import_module`, and explicit relative `from` import forms in the current top-level no-package context raising `ImportError`; repeated supported imports compare identical in the current structural identity model, `importlib.import_module` returns fixed supported modules or current sentinel errors for the supported failure profile including missing, empty, relative, non-string, and wrong-arity calls, already-imported fixed-module attribute mutation/deletion is visible across current direct aliases and list/tuple/dict-held module values, and later fixed-module import/importlib lookups reuse current env-reachable mutated/deleted module snapshots. Needs real `sys.modules`, find/load protocol, module specs, positive packages and relative imports, star and dotted imports, user module execution, broader importlib integration, deterministic cache identity independent of current env reachability, host/filesystem profile boundaries, and `__main__`. |
| Pattern matching | partial | Basic adapter-backed `match` supports value patterns over current expression values, singleton `True`/`False`/`None` patterns with identity-style matching, wildcard patterns, simple capture patterns that bind the subject in the current environment, OR patterns over current non-binding subpatterns and current capture-bearing sequence/mapping/builtin-class alternatives with first-matching-alternative binding, AS patterns over current non-binding subpatterns and current capture-bearing sequence/mapping/builtin-class subpatterns that bind subpattern captures and then the alias after a successful subpattern match, zero-argument builtin class patterns over current concrete bool/int/float/complex/str/bytes/list/tuple/dict/set/range/slice values including CPython-compatible bool-as-int matching, single-positional builtin class patterns for current bool/bytes/dict/float/int/list/set/str/tuple whole-object matching with non-binding or capture-bearing subpatterns, keyword and positional-plus-keyword builtin class patterns over numeric `real`/`imag`, int-like `numerator`/`denominator`, and range/slice `start`/`stop`/`step` attributes with non-binding or capture-bearing nested attribute subpatterns, fixed-length and starred sequence patterns over current concrete list/tuple values with non-binding or capture-bearing nested element/prefix/suffix patterns, `*name` binding for top-level and nested starred sequence patterns, mapping patterns over current concrete dicts with constant keys and non-binding or capture-bearing nested value patterns, `**rest` binding for top-level and nested mapping patterns, CPython-compatible no-leak behavior for failed capture-bearing compound subpatterns, guarded cases over the current pattern subset with guard-false fallthrough, guard exception propagation, and current CPython-compatible top-level/OR-selected/AS/nested sequence/starred-sequence/mapping/rest/builtin-class capture visibility after false/raising guards, ordered case selection, and single subject evaluation. Needs OR and AS patterns beyond current sequence/mapping/builtin-class capture-bearing alternatives, class patterns beyond current builtin whole-object captures, general sequence protocol matching beyond current concrete list/tuple values, broader mapping protocol behavior, full class patterns including name_or_attr evaluation/shadowing, user classes, keyword subpatterns beyond the current builtin attribute subset, general positional subpatterns via `__match_args__`, duplicate keyword diagnostics, and TypeError behavior, group patterns/provenance if needed, binding rollback for broader refutable subpatterns, broader value-pattern evaluation, irrefutability diagnostics, and PEP 634 underspecification triage. |
| Coroutines and async | partial | Adapter-backed undecorated ordinary-positional `async def` declarations now preserve a distinct async function value with current callable and function metadata observations; needs coroutine object allocation/execution, `await`, async generators, `async for`, async comprehensions, `async with`, decorators, full signatures, annotation/type-parameter metadata, and exact diagnostics. |
| Annotations and type syntax | partial | Adapter-backed simple-name non-generic `type Alias = expr` binds a fresh lazy alias object with current metadata, object identity, and cached direct/`getattr` `__value__` evaluation. Needs deferred annotations, generic type aliases, type parameters, function/class type-parameter syntax, full annotation scopes, `hasattr` lazy-evaluation behavior, and exact diagnostics. |
| Standard library interactions | partial | Internal `range(stop)`/`range(start, stop)`/`range(start, stop, step)` loop, equality, integer-like and float-equality membership, indexing, nonzero-step slicing, current zero-step `ValueError` diagnostics, truthiness, length, and `start`/`stop`/`step` attribute support exist; `len`, `bool`, current scalar/string-escape/Unicode-ascii/bytes/bytearray/special-float/container/range/slice `str`/`repr`/`ascii`, `format` over the current string-conversion domain with literal or evaluated format-spec expressions over the current supported subset, including current string `s`/default width/alignment/precision subset with sign/alternate/`=` alignment, `z` option, and incompatible numeric presentation-type `ValueError` diagnostics and current int-like `b`/`c`/`d`/`o`/`x`/`X` sign/alternate/width/alignment/zero-padding/grouping subset with integer-precision, `z` option, string-presentation-type, locale-independent `n` grouping/precision/`z`, signed-`c`, and alternate-`c` `ValueError` diagnostics plus special float-value presentation rendering for `e`/`E`/`f`/`F`/`g`/`G`/`n`/`%` and default non-empty numeric specs over `inf`/`-inf`/`nan` with sign, `z`, `#`, precision-ignored special payloads, fill/alignment, zero-padding, width, width and fractional comma/underscore grouping for non-`n` float presentation types, grouped-`n` `ValueError` diagnostics, and missing-precision `ValueError` diagnostics, `chr` over current int-like code points, concrete-iterable `all`/`any`, `sorted` over current concrete comparable iterables including memoryview over bytes/bytearray with fresh list results, reverse= truthiness, key= callable sorting over current order-comparable key results, and stable equal-key ordering, `sum` over current numeric/list/tuple addable values and memoryview byte values with unsupported-start/item `TypeError` diagnostics, int-like, memoryview byte, float-containing list/tuple, and string one-argument `min`/`max` over Unicode code-point items with empty iterable `ValueError`, `default=` support, and `key=` support for current int-like and float-compatible numeric key-result profiles, int-like and float-compatible numeric multi-argument `min`/`max` with `default=` `TypeError` diagnostics and `key=` support for current int-like and float-compatible numeric key-result profiles, int-like and CPython-profile Unicode-whitespace-padded string plus ASCII-whitespace-padded bytes/bytearray/base `int` with valid underscore digit separators, matching base prefixes, and base-0 autodetection, numeric `real`/`imag` attributes, int-like `numerator`/`denominator` attributes, no-argument numeric `conjugate()`, numeric, simple signed ASCII integer-string, finite decimal-point string, small-exponent string, and case-insensitive `inf`/`infinity`/`nan` special string `float` with CPython-profile Unicode numeric whitespace plus ASCII-whitespace-padded bytes/bytearray float forms and valid underscore digit separators, `abs`, `divmod`, two-argument `pow` including complex bases with int-like exponents, negative float bases with int-like exponents, and negative real bases with integral float exponents, with current `TypeError` diagnostics for concrete non-numeric operands, nonnegative-exponent and invertible negative-exponent three-argument modular `pow` with current `TypeError` diagnostics for non-int-like modular operands and current `ValueError` diagnostics for zero-modulus and non-invertible negative-exponent modular `pow`, and `round` over current int-like values plus finite floats in the current concrete scaling subset with half-even behavior, current special-float `OverflowError`/`ValueError` diagnostics for integer-converting forms, and special-float preservation for integer `ndigits`, adapter-backed complex literal values with equality/truthiness and `+`/`-`/`*`/nonzero `/` plus current `ZeroDivisionError` diagnostics for zero complex divisors and integer-like `**`, adapter-backed `complex()` construction including `real`/`imag` keyword arguments, current concrete-dict keyword unpacking, and simple real-only, imaginary-only, unparenthesized real-plus-imaginary, or parenthesized strings over current numeric values, adapter-backed `Ellipsis` literal and shadowable builtins-name fallback, and adapter-backed `__debug__` as `True` in the current non-optimized profile work for current concrete values including bytes/bytearray integer iteration, with current `ZeroDivisionError` diagnostics for zero-base negative two-argument integer/bool/float/complex power and `pow` exponents; no-argument `list()`, `tuple()`, `dict()`, `set()`, `bytes()`, and `bytearray()` constructors build current empty concrete values, `list(x)`/`tuple(x)` convert current ordered concrete string/bytes/bytearray/list/tuple/dict/set/range/memoryview values, with sets using the current internal set iteration order profile, `dict(x)` copies current dict values or consumes current list/tuple pair iterables, `dict(key=value)`/`dict(**d)`/`dict(x, key=value, **d)` consumes current simple keyword arguments and current concrete-dict keyword unpacking with TypeError diagnostics for duplicate evaluated keyword names, `set(x)` consumes current concrete iterables including memoryview with duplicate normalization, `bytes(source)` consumes current int-like lengths, existing bytes/bytearray values, current memoryview values over bytes/bytearray, bytes(string, encoding[, errors]) for current utf-8, ascii, and latin-1 string encoding with strict, ignore, replace, backslashreplace, and xmlcharrefreplace error handling plus current lazy handler lookup for encodable strings, and concrete byte-int list/tuple/dict-key/set/range iterables with TypeError/ValueError diagnostics, simple-name `bytes.count(sub[, start[, end]])`/`bytes.find(sub[, start[, end]])`/`bytes.rfind(sub[, start[, end]])`/`bytes.index(sub[, start[, end]])`/`bytes.rindex(sub[, start[, end]])` methods over current concrete bytes values with integer-byte or bytes-like needles, optional slice-style start/end windows, non-overlapping count semantics, `find`/`rfind` `-1` not-found results, `index`/`rindex` `ValueError` not-found diagnostics, and current `TypeError`/`ValueError` needle diagnostics, and `bytes.split([sep[, maxsplit]])`/`bytes.rsplit([sep[, maxsplit]])` returning lists of fresh bytes subsequences for explicit bytes-like separators or default ASCII-whitespace runs with directional `maxsplit`, and `bytes.partition(sep)`/`bytes.rpartition(sep)` returning 3-tuples of bytes subsequences split around first/last nonempty bytes-like separators, and `bytes.removeprefix(prefix)`/`bytes.removesuffix(suffix)` returning fresh bytes values after nonempty bytes-like affix removal, and `bytes.splitlines([keepends])` returning lists of fresh bytes line segments split at ASCII LF/CR/CRLF boundaries with optional kept line endings, and `bytes.isascii()`/`bytes.isdigit()`/`bytes.isalpha()`/`bytes.isalnum()`/`bytes.islower()`/`bytes.isupper()`/`bytes.isspace()`/`bytes.istitle()` returning booleans over ASCII byte classes, and `bytes.lower()`/`bytes.upper()`/`bytes.capitalize()`/`bytes.swapcase()`/`bytes.title()` returning ASCII case-converted fresh bytes values, and `bytes.lstrip([bytes])`/`bytes.rstrip([bytes])`/`bytes.strip([bytes])` returning fresh bytes values after default ASCII-whitespace or bytes-like strip-set removal, and `bytes.center(width[, fillbyte])`/`bytes.ljust(width[, fillbyte])`/`bytes.rjust(width[, fillbyte])` returning width-justified fresh bytes values with default-space padding and one-byte bytes/bytearray fill validation, and `bytes.expandtabs([tabsize])` returning fresh bytes values after ASCII tab expansion with LF/CR column resets, and `bytes.hex([sep[, bytes_per_sep]])` returning lowercase hexadecimal strings with optional ASCII string/bytes separators grouped by signed `bytes_per_sep`, and `bytes.fromhex(string)` decoding hexadecimal strings or ASCII bytes-like inputs with ASCII-whitespace separators between byte pairs, and `bytes.replace(old, new[, count])` returning fresh bytes values after left-to-right non-overlapping bytes-like substitution with optional count, and `bytes.join(iterable)` returning fresh bytes values from concrete iterables of bytes-like items separated by the receiver, and `bytes.startswith(prefix[, start[, end]])`/`bytes.endswith(suffix[, start[, end]])` returning affix booleans for bytes-like or tuple affix arguments, and `bytes.zfill(width)` returning width-padded fresh bytes values with leading-sign handling, and `bytearray(source)`/`bytearray(string, encoding[, errors])` consumes the same current source and encoding profile while preserving bytearray result type for read-only binary sequence operations, simple-name `bytearray.append(x)`/`bytearray.extend(x)`/`bytearray.clear()`/`bytearray.copy()`/`bytearray.reverse()`/`bytearray.pop([index])`/`bytearray.remove(value)`/`bytearray.insert(index, value)`/`bytearray.count(sub)`/`bytearray.index(sub)`/`bytearray.find(sub)`/`bytearray.rfind(sub)`/`bytearray.rindex(sub)`/`bytearray.startswith(prefix)`/`bytearray.endswith(suffix)`/`bytearray.expandtabs([tabsize])`/`bytearray.hex([sep[, bytes_per_sep]])`/`bytearray.fromhex(string)`/`bytearray.removeprefix(prefix)`/`bytearray.removesuffix(suffix)`/`bytearray.replace(old, new[, count])`/`bytearray.join(iterable)`/`bytearray.split([sep[, maxsplit]])`/`bytearray.rsplit([sep[, maxsplit]])`/`bytearray.splitlines([keepends])`/`bytearray.partition(sep)`/`bytearray.rpartition(sep)`/`bytearray.isascii()`/`bytearray.isdigit()`/`bytearray.isalpha()`/`bytearray.isalnum()`/`bytearray.islower()`/`bytearray.isupper()`/`bytearray.isspace()`/`bytearray.istitle()`/`bytearray.lower()`/`bytearray.upper()`/`bytearray.capitalize()`/`bytearray.swapcase()`/`bytearray.title()`/`bytearray.center(width[, fillbyte])`/`bytearray.ljust(width[, fillbyte])`/`bytearray.rjust(width[, fillbyte])`/`bytearray.lstrip([bytes])`/`bytearray.rstrip([bytes])`/`bytearray.strip([bytes])`/`bytearray.zfill(width)` methods cover current concrete bytearray values, with `None` returns for mutators, `copy()` returning a bytearray value, `pop()` returning and removing indexed byte values, `remove()` removing the first matching byte, `insert()` inserting clamped-index byte values, `count()` returning integer-byte or bytes-like subsequence occurrence counts, `index()`/`rindex()` returning first/last integer-byte or bytes-like subsequence positions, and `find()`/`rfind()` returning first/last integer-byte or bytes-like needle positions or `-1`, with optional slice-style `start`/`end` windows for all five search methods, and `startswith()`/`endswith()` returning affix booleans for bytes-like or tuple affix arguments over whole values or optional slice-style `start`/`end` windows, `expandtabs()` returning fresh bytearray values after ASCII tab expansion with LF/CR column resets, `hex()` returning lowercase hexadecimal strings with optional ASCII string/bytes separators grouped by signed `bytes_per_sep`, `fromhex()` decoding hexadecimal strings or ASCII bytes-like inputs with ASCII-whitespace separators between byte pairs into fresh bytearray values, and `removeprefix()`/`removesuffix()` returning fresh bytearray values with a matching bytes-like affix removed, `replace()` returning fresh bytearray values after left-to-right non-overlapping bytes-like substitution with optional count, `join(iterable)` returning fresh bytearray values from concrete iterables of bytes-like items separated by the receiver, `split()`/`rsplit()` returning lists of fresh bytearray subsequences for explicit bytes-like separators or default ASCII-whitespace runs with directional `maxsplit`, `splitlines()` returning lists of fresh bytearray line segments split at ASCII LF/CR/CRLF boundaries with optional kept line endings, partition/rpartition returning three-bytearray tuples split around first/last nonempty bytes-like separators, and ASCII classification methods returning booleans over ASCII byte classes, and ASCII case-conversion methods returning fresh bytearray values, center/ljust/rjust returning width-justified fresh bytearray values with default-space padding and one-byte bytes/bytearray fill validation, strip/lstrip/rstrip returning fresh bytearray values after default ASCII-whitespace or bytes-like strip-set removal, and zfill returning width-padded fresh bytearray values with leading-sign handling, and current `TypeError`/`ValueError`/`IndexError` diagnostics; `memoryview(source)` over current bytes/bytearray values participates in the concrete iteration/list/tuple/set/sorted/all/any/sum/min/max profile over yielded integer bytes. Needs staged builtins/core library coverage, broader constructor arguments/iterable conversion, mapping protocol, broader bytes constructor encodings, remaining namereplace/custom error handlers, buffer behavior, remaining bytes methods beyond current simple-name  and buffer protocol behavior, int object conversion and exact numeric-constructor diagnostic text, float object conversion and exact numeric-constructor diagnostic text, remaining bytearray mutation/methods beyond current simple-name , exponent overflow/underflow and decimal rounding-boundary/error behavior, broader range object behavior, general complex exponentiation with non-integer exponents and object construction, complex keyword diagnostics/warnings, general method binding, general builtins namespace binding beyond the `Ellipsis` singleton, optimization profile behavior for `-O`, native parser coverage for control-character string escapes, finite float presentation rendering, integer formatting through floating-point presentation types, finite float and complex string conversion, recursive cycle handling, general `__bool__`/`__len__`/`__iter__`/`__next__`/`__int__`/`__float__`/`__abs__`/`__divmod__`/`__pow__`, and host profile boundaries. |
| CPython profile | partial | Current diagnostics profile records exception-class normalization for existing adapter diagnostic tests and classifies CPython `test_syntax`/`test_traceback` as exact-diagnostic upstream suites; current resource-limits profile records implementation-note, optional-extension/environment, recursion-limit, integer-string-conversion-limit, and Unix `resource` module boundaries with CPython `test_sys`/`test_int`/`test_resource` provenance; current bytecode-internals profile records the portable source-semantics boundary and future bytecode, code-object, compiler, marshal, `.pyc`, import-cache, and eval-loop implementation-profile surfaces with CPython `test_dis`/`test_compile`/`test_codeop`/`test_compiler_codegen`/`test_compiler_assemble`/`test_marshal`/`test_py_compile`/`test_compileall` provenance. Needs exact message text, source locations, traceback object/rendering behavior, warnings, executable recursion-depth and `int_max_str_digits` runtime cells/rules, process/host resource APIs, executable CPython bytecode/code-object profile if selected, and broader `Lib/test` extraction. |

## Completeness Rule

Do not mark a construct complete merely because CPython accepts a test. Completion requires matching the official docs, a K rule story, and an explicit decision about CPython-specific behavior.
