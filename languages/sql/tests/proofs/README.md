# SQL Proof Tests

Reachability claims and symbolic sanity checks will live here after the executable SQL/Foundation core exists.

Early proof candidates include three-valued logic identities, predicate normalization properties, and invariants over table/bag operations.
