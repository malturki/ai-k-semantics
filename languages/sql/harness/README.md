# SQL Harness

The harness will eventually run:

- K parser and execution tests.
- Standard conformance tests with section provenance.
- Differential tests against selected vendor profiles.

Because SQL has no single global reference implementation, every differential run must name the selected engine, version, configuration, dialect/profile, and normalization rules.
