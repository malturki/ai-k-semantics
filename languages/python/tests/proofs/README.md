# Python Proof Tests

Reachability claims and symbolic sanity checks will live here after the executable Python core exists.

Early proof candidates include evaluation-order invariants, truth-value identities for built-in constants, exception propagation properties, and frame/name-binding invariants.
