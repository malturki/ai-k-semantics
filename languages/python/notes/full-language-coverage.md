# Python Full-Language Coverage Map

This file is the completeness ledger for Python 3.14.6. A construct is not considered complete until it has:

- direct syntax in K or a documented reason for a faithful desugaring
- semantic rules linked to the official docs
- local positive and negative tests
- CPython differential coverage, unless classified as intentionally non-CPython
- known ambiguity/divergence notes where relevant

Status values:

- `not-started`: no K syntax or semantics yet
- `syntax`: syntax exists, semantics incomplete
- `partial`: executable semantics exists for a documented subset
- `profile`: only covered in an implementation/profile module
- `complete`: docs-linked semantics and tests are in place

## Source Chapters

| Source area | Reference | Status | Notes |
| --- | --- | --- | --- |
| Lexical analysis | Language Reference, chapter 2 | partial | Initial kernel uses semicolon-separated simple statements with trailing semicolons and parses integers, simple K strings, CPython-AST-backed source string/bytes literal values for raw, adjacent, ignored-newline, named-character, hex, octal, and byte-boundary escape spellings, adapter-backed simple f-string text/formatted-expression/conversion/debug/empty-format parts plus literal and dynamic nested format-spec parts over the current format subset, `True`, `False`, `None`, and the adapter-backed `...` atom, bytes literal constants, and imaginary literal constants. Full source encoding, physical/logical lines, indentation, comments, token classes, native K parser coverage and diagnostics for full Python string/bytes prefixes and escapes, t-string details, and numeric literal edge cases remain. |
| Data model | Language Reference, chapter 3 | partial | Initial kernel has minimal integers, floats, complex values, strings, bytes, list/tuple/dict/set values, booleans, `None`, and `Ellipsis`. Full object identity, real bool-as-int subtype behavior, type hierarchy, attributes, descriptors, special methods, GC/lifetime notes, coroutines, and async generators remain. |
| Execution model | Language Reference, chapter 4 | partial | Initial kernel has a single environment. Full code blocks, frames, binding, scopes, exceptions, annotation scopes, and runtime components remain. |
| Import system | Language Reference, chapter 5 | not-started | Needs controlled module table and host profile. |
| Expressions | Language Reference, chapter 6 | partial | Initial kernel covers integer/float/imaginary/string/bytes/list/tuple/dict/set/bool/None/Ellipsis atoms, adapter-backed simple f-string interpolation over the current string-conversion value subset with left-to-right expression evaluation, `!s`/`!r`/`!a` conversions, debug literal prefixes, empty, literal non-empty, and dynamic nested format specs over the current format subset, and exception propagation, names, parentheses, expression-backed list/tuple/dict/set displays through the adapter including starred unpacking over current ordered concrete iterables, unary `+`/`-`/`~`, arithmetic and bitwise integer operators, complex unary `+`/`-` plus complex addition/subtraction/multiplication/nonzero true division for current int/bool/float/complex values, string concatenation/equality/order, string/list/tuple/bytes/bytearray sequence repetition with integer-like counts plus current invalid-repeat-count `TypeError` diagnostics, string nonzero-step slicing by Unicode code point plus list/tuple/bytes/bytearray nonzero-step slicing plus `ValueError` for zero-step direct and slice-object subscriptions in the current subset, adapter-backed slice objects with start/stop/step attributes and slice-object subscriptions over current string/list/tuple/bytes/bytearray/range values, numeric `real`/`imag` and int-like `numerator`/`denominator` attributes, string substring membership and string indexing by Unicode code point and bytes/bytearray integer indexing with current out-of-range `IndexError` and invalid-index `TypeError` diagnostics, bytes/bytearray subsequence membership with current ValueError diagnostics for out-of-range integer needles and TypeError diagnostics for invalid non-bytes-like needles, list/tuple same-type concatenation, bytes/bytearray same-family concatenation, current cross-type sequence concatenation `TypeError` diagnostics over string/bytes/bytearray/list/tuple, list/tuple/dict/set/bytes/bytearray equality, list/tuple/bytes/bytearray lexicographic ordering, set-to-set ordering comparisons, current concrete unsupported ordering `TypeError` diagnostics, list/tuple membership/indexing with current out-of-range `IndexError` and invalid-index `TypeError` diagnostics, dict key membership/lookup with current missing-key `KeyError` and unsupported-key `TypeError` diagnostics, set membership, range equality/int-like and float-equality membership/indexing/nonzero-step slicing/start-stop-step attributes with current out-of-range `IndexError` and invalid-index `TypeError` diagnostics, singleton identity comparisons, chained comparisons over supported operators including current unsupported-ordering `TypeError` diagnostics, truthiness for simple values, short-circuit `and`/`or`, `not`, conditional expressions, simple-name assignment expressions, positional-argument lambdas/calls including suffix defaults, positional-only lambdas including suffix defaults, varargs-only, kwargs-only, keyword-only, keyword-only-plus-kwargs, varargs-plus-kwargs, varargs-plus-keyword-only, and varargs-plus-keyword-only-plus-kwargs combinations, varargs parameters including suffix defaults and keyword binding for fixed parameters, `*args` plus keyword-only lambda signatures including suffix and sparse keyword-only defaults and kwargs collection, `*args, **kwargs` lambda signatures including suffix defaults, keyword-only parameters in the no-positional subset including suffix defaults and kwargs collection, mixed positional-plus-keyword-only parameters including suffix defaults and kwargs collection, and starred positional call unpacking over current ordered concrete iterables including bytes and bytearray, and a binary comparison subset. All other expression constructs remain. |
| Simple statements | Language Reference, chapter 7 | partial | Initial kernel covers expression statements, `pass`, `assert` in the current non-optimized profile including success-path message skipping and false-path `AssertionError` sentinel raising after optional message evaluation, module-scope single-name and multi-name `global`, simple assignment, simple named list/dict subscription assignment and augmented assignment with current simple-target `IndexError`/`KeyError`/`TypeError` diagnostics, two-level simple-name-rooted list/dict subscription assignment, simple named list slice assignment including explicit step-1 resizing, fixed-length non-unit-step replacement over current concrete iterable RHS values, and `ValueError` for mismatched non-unit-step replacement lengths, simple-name annotated assignment in the current no-introspection profile, flat, non-starred nested, starred, and nested starred sequence unpacking assignment over current string/bytes/bytearray/list/tuple/dict/set/range/memoryview RHS values with current `ValueError` arity checks for flat, top-level starred-too-few, and top-level nested target mismatches, simple-name `+=`/`-=`/`*=`/`@=`/`/=`/`%=`/`**=`/`//=`/`<<=`/`>>=`/`&=`/`^=`/`|=`, simple-name/multi-target simple-name `del`, simple named list/dict subscription `del` with current `IndexError`/`KeyError`/`TypeError` diagnostics, current two-level simple-name-rooted list/dict subscription `del`, simple named list slice `del`, and adapter-backed `raise`, `raise Name`, `raise Name(args...)`, `raise Name from None`, and `raise Name from Cause` over the current internal exception-value subset, including positional constructor-argument evaluation, with bytes/bytearray/memoryview yielding integers, dictionaries yielding keys, and sets using the current internal set iteration order profile. Remaining statements are not started. |
| Compound statements | Language Reference, chapter 8 | partial | Adapter-backed `if`, `while`, string/bytes/bytearray/list/tuple/dict/set/range/memoryview `for`, flat, non-starred nested, starred, and nested starred unpacking `for` targets, normal-completion and return/break/continue/exception-propagating `try/finally`, including `break`/`continue`/`return`/new exception issued by `finally` bodies overriding a pending exception, one or more named, tuple-named, or bare `except` handlers over the current internal exception-value subset, including `except as` binding/cleanup, normal completion, first-match handler order, nested mismatch propagation, bare catch-all handlers, `else` clauses, and combined `try`/`except`/`else`/`finally` forms by modular composition, basic `match` statements over value, singleton, wildcard, simple capture, non-binding OR, non-binding AS, zero-argument builtin class patterns over current concrete value families, single-positional builtin class patterns over documented whole-object matching types with non-binding subpatterns, non-binding fixed-length sequence patterns, non-binding starred sequence `*_` patterns, top-level starred sequence `*name` capture patterns, non-binding mapping patterns over current concrete dicts with constant keys, top-level mapping `**rest` capture patterns, and guarded cases over the current pattern subset, positional-parameter `def` subsets, decorators on current function-signature families with optional suffix and sparse keyword-only defaults, and one-or-more-manager `with` statements over the current `memoryview(bytes-or-bytearray)` context-manager profile with optional simple-name, sequence, and starred sequence `as` targets exist, including `break`, `continue`, bare/value `return` propagation, exception propagation through current while/for/with header and body continuations, and loop `else` for the current while/for subsets, with bytes/bytearray/memoryview yielding integers, dictionaries yielding keys, and sets using the current internal set iteration order profile, and with unpacking support for current concrete string/bytes/bytearray/list/tuple/dict/set/range item values in the supported target profiles. General iteration, broader `except` matching forms, `except*`, real exception objects, general context-manager protocol, broader `match` patterns/protocol behavior, full function objects/frames, classes, coroutines, type parameter lists, annotations remain. |
| Top-level components | Language Reference, chapter 9 | syntax | Initial kernel parses a small file-input subset. Interactive/eval modes remain. |
| Full grammar | Language Reference, chapter 10 | syntax | A first construct-preserving CPython-AST adapter exists for the current executable subset, including ordinary `//`, list displays, tuple displays, and simple `JoinedStr`/`FormattedValue` f-string nodes with conversion, empty format-spec markers, literal non-empty format-spec markers, and dynamic nested format-spec part markers over the current format subset. The full grammar must still be mirrored construct-by-construct or covered by documented parser front ends that preserve construct identity. |
| Built-ins and core library | Standard Library reference | partial | Internal `range`, `len` with strings counted by Unicode code points, `bool`, `str`/`repr`/`ascii` over the current int/bool/None/Ellipsis/simple-string/bytes/bytearray/special-float/list/tuple/dict/set/range/slice representation subset recursively over supported container and slice-bound elements, `format` over the current string-conversion domain with literal or evaluated format-spec expressions over the current supported subset, including current string `s`/default width/alignment/precision subset using Unicode code-point length with sign/alternate/`=` alignment, `z` option, and incompatible numeric presentation-type `ValueError` diagnostics and current int-like `b`/`c`/`d`/`o`/`x`/`X` sign/alternate/width/alignment/zero-padding/grouping subset with integer-precision, `z` option, string-presentation-type, locale-independent `n` grouping/precision/`z`, signed-`c`, alternate-`c` `ValueError` diagnostics, and non-surrogate Unicode scalar-value `c` output, plus special float-value presentation rendering for `e`/`E`/`f`/`F`/`g`/`G`/`n`/`%` and default non-empty numeric specs over `inf`/`-inf`/`nan` with sign, `z`, `#`, precision-ignored special payloads, fill/alignment, zero-padding, width, width and fractional comma/underscore grouping for non-`n` float presentation types, grouped-`n` `ValueError` diagnostics, and missing-precision `ValueError` diagnostics, including CPython-compatible string quote selection, quote/backslash/tab/newline/carriage-return escape rendering, internal `\xhh` helper rules for other ASCII controls, CPython-compatible non-ASCII `ascii` escaping over adapter-emitted UTF-8 strings, bytes quote selection and byte escape rendering, plus special-float `inf`/`-inf`/`nan` spelling, `chr(x)`/`ord(x)` over current non-surrogate Unicode scalar-value strings/code points with current `ValueError`/`TypeError` diagnostics and bytes/bytearray `ord` over one-byte values, `all`, `any`, `sorted` over current concrete comparable iterables including memoryview over bytes/bytearray with fresh list results, reverse= truthiness, key= callable sorting over current order-comparable key results, and stable equal-key ordering, `sum` over current numeric/list/tuple/addable and memoryview byte values with unsupported-start/item `TypeError` diagnostics, int-like, memoryview byte, float-containing list/tuple, and string one-argument `min`/`max` over Unicode code-point items with empty iterable `ValueError`, `default=` support, and `key=` support for current int-like and float-compatible numeric key-result profiles, int-like and float-compatible numeric multi-argument `min`/`max` with `default=` `TypeError` diagnostics and `key=` support for current int-like and float-compatible numeric key-result profiles, `int`, numeric and simple string/bytes/bytearray `float`, including CPython-profile Unicode-whitespace-padded numeric strings and ASCII-whitespace-padded bytes/bytearray, valid integer-string/bytes/bytearray underscores, matching base prefixes, base-0 integer autodetection, finite decimal-point and small-exponent float strings/bytes/bytearray with valid underscore digit separators, and case-insensitive float `inf`/`infinity`/`nan` special strings/bytes/bytearray, numeric `complex()` positional, `real`/`imag` keyword, current concrete-dict keyword-unpacked, and simple real-only, imaginary-only, unparenthesized real-plus-imaginary, or parenthesized string construction with valid underscore separators, optional surrounding Python string whitespace, and `inf`/`infinity`/`nan` special strings in the supported numeric parts, numeric `real`/`imag` attributes, int-like `numerator`/`denominator` attributes, no-argument numeric `conjugate()`, `abs`, `divmod`, two-argument `pow`, and nonnegative-exponent plus invertible negative-exponent three-argument modular `pow` subsets and current int-like/finite-float `round` with half-even behavior exist, plus the adapter-backed `Ellipsis` singleton and shadowable builtins-name fallback, no-argument `list()`, `tuple()`, `dict()`, `set()`, and `bytes()`/`bytearray()` constructors, ordered-iterable `list(x)`/`tuple(x)` for current concrete values, with strings itemized by Unicode code point and including bytes, bytearray, memoryview, and set values through the current internal set iteration order profile, `dict(x)` for current dict values and list/tuple pair iterables, `dict(key=value)`/`dict(**d)`/`dict(x, key=value, **d)` for simple keyword arguments and current concrete-dict keyword unpacking, `set(x)` for current concrete iterables including bytes, bytearray, and memoryview, `bytes(source)` for current int-like lengths, existing bytes and bytearray values, current memoryview values over bytes/bytearray, bytes(string, encoding[, errors]) for current utf-8, ascii, and latin-1 string encoding with strict, ignore, replace, backslashreplace, and xmlcharrefreplace error handling plus current lazy handler lookup for encodable strings, and concrete byte-int list/tuple/dict-key/set/range iterables with TypeError/ValueError diagnostics, and `bytearray(source)`/`bytearray(string, encoding[, errors])` for the same current source and encoding profile plus read-only binary sequence operations, simple-name `bytearray.append(x)`/`bytearray.extend(x)`/`bytearray.clear()`/`bytearray.copy()`/`bytearray.reverse()`/`bytearray.pop([index])`/`bytearray.remove(value)`/`bytearray.insert(index, value)`/`bytearray.count(sub)`/`bytearray.index(sub)`/`bytearray.find(sub)`/`bytearray.rfind(sub)`/`bytearray.rindex(sub)`/`bytearray.startswith(prefix)`/`bytearray.endswith(suffix)`/`bytearray.expandtabs([tabsize])`/`bytearray.hex([sep[, bytes_per_sep]])`/`bytearray.removeprefix(prefix)`/`bytearray.removesuffix(suffix)`/`bytearray.replace(old, new[, count])`/`bytearray.split([sep[, maxsplit]])`/`bytearray.rsplit([sep[, maxsplit]])`/`bytearray.splitlines([keepends])`/`bytearray.partition(sep)`/`bytearray.rpartition(sep)`/`bytearray.isascii()`/`bytearray.isdigit()`/`bytearray.isalpha()`/`bytearray.isalnum()`/`bytearray.islower()`/`bytearray.isupper()`/`bytearray.isspace()`/`bytearray.istitle()`/`bytearray.lower()`/`bytearray.upper()`/`bytearray.capitalize()`/`bytearray.swapcase()`/`bytearray.title()`/`bytearray.center(width[, fillbyte])`/`bytearray.ljust(width[, fillbyte])`/`bytearray.rjust(width[, fillbyte])`/`bytearray.lstrip([bytes])`/`bytearray.rstrip([bytes])`/`bytearray.strip([bytes])`/`bytearray.zfill(width)` methods over current concrete bytearray values, with `None` returns for mutators, `copy()` returning a bytearray value, `pop()` returning and removing indexed byte values, `remove()` removing the first matching byte, `insert()` inserting clamped-index byte values, `count()` returning integer-byte or bytes-like subsequence occurrence counts, `index()`/`rindex()` returning first/last integer-byte or bytes-like subsequence positions, and `find()`/`rfind()` returning first/last integer-byte or bytes-like needle positions or `-1`, with optional slice-style `start`/`end` windows for all five search methods, and `startswith()`/`endswith()` returning affix booleans for bytes-like or tuple affix arguments over whole values or optional slice-style `start`/`end` windows, `expandtabs()` returning fresh bytearray values after ASCII tab expansion with LF/CR column resets, `hex()` returning lowercase hexadecimal strings with optional ASCII string/bytes separators grouped by signed `bytes_per_sep`, and `removeprefix()`/`removesuffix()` returning fresh bytearray values with a matching bytes-like affix removed, `replace()` returning fresh bytearray values after left-to-right non-overlapping bytes-like substitution with optional count, `split()`/`rsplit()` returning lists of fresh bytearray subsequences for explicit bytes-like separators or default ASCII-whitespace runs with directional `maxsplit`, `splitlines()` returning lists of fresh bytearray line segments split at ASCII LF/CR/CRLF boundaries with optional kept line endings, partition/rpartition returning three-bytearray tuples split around first/last nonempty bytes-like separators, and ASCII classification methods returning booleans over ASCII byte classes, and ASCII case-conversion methods returning fresh bytearray values, center/ljust/rjust returning width-justified fresh bytearray values with default-space padding and one-byte bytes/bytearray fill validation, strip/lstrip/rstrip returning fresh bytearray values after default ASCII-whitespace or bytes-like strip-set removal, and zfill returning width-padded fresh bytearray values with leading-sign handling, and current `TypeError`/`ValueError`/`IndexError` diagnostics; `memoryview(source)` over current bytes/bytearray values participates in the concrete iteration/list/tuple/set/bytes/bytearray/sorted/all/any/sum/min/max profile over yielded integer bytes; no full builtins namespace, general object protocol, remaining bytearray mutation/methods beyond current simple-name , native parser coverage for control-character string escapes, surrogate code point string support, finite float and complex string conversion, recursive cycle handling, or full object `str`/`repr` dispatch yet. |

## Construct Families

| Family | Status | Required next evidence |
| --- | --- | --- |
| Source encodings and tokenizer behavior | not-started | CPython tokenizer cases and docs-linked K lexer/token model. |
| Indentation and block structure | not-started | Parser tests for `INDENT`/`DEDENT`, mixed tabs/spaces diagnostics, blank/comment-only lines. |
| Names, keywords, soft keywords | partial | Initial `Id` handling only; needs Python identifier Unicode rules and keyword/soft-keyword separation. |
| Literals | partial | Int, simple finite float, adapter-backed imaginary/complex, simple string, CPython-AST-backed source string/bytes literal values for raw, adjacent, ignored-newline, named-character, hex, octal, and byte-boundary escape spellings, adapter-backed simple f-string interpolation plus current string-conversion-subset `!s`/`!r`/`!a` conversion, debug prefix, empty, literal non-empty, and dynamic nested format specs over the current format subset, adapter-backed bytes constants, bool, None, and adapter-backed ellipsis atom smoke coverage; native K parser coverage and diagnostics for full Python string/bytes prefixes and escapes, direct bytes parser syntax and diagnostics, full float lexical edge cases, object `__format__` dispatch, and t-strings remain. |
| Object model | partial | Minimal value model with singleton identity only; needs heap objects, general identity, type hierarchy, attributes, descriptors. |
| Numeric operations | partial | Int/bool unary `+`/`-`/`~`, `+`, `-`, `*`, `/`, `%`, `//`, shifts, bitwise operators, `**`, float and mixed int/bool/float `+`, `-`, `*`, `/`, `//`, `%`, `**` with current `ZeroDivisionError` diagnostics for zero divisors in true division, floor division, modulo, and `divmod`, ordering comparisons, complex unary `+`/`-`, complex equality/truthiness, complex `+`/`-`/`*`/nonzero `/` plus current `ZeroDivisionError` diagnostics for zero complex divisors over current int/bool/float/complex values, complex-base integer-like exponentiation, numeric `real`/`imag` attributes, int-like `numerator`/`denominator` attributes, and no-argument numeric `conjugate()` over current int/bool/float/complex values, adapter-backed `int()`/`int(x)` for current int-like values and simple digit strings with CPython-profile Unicode numeric whitespace and bytes/bytearray with optional surrounding ASCII whitespace, optional sign, optional valid underscore digit separators, explicit bases 2 through 36, matching base prefixes, and base-0 autodetection, adapter-backed `float()` and `float(x)` for current int-like/float values plus simple signed ASCII integer strings with CPython-profile Unicode numeric whitespace and ASCII-whitespace-padded bytes/bytearray with optional valid underscore digit separators, finite decimal-point strings with valid underscore digit separators, finite exponent strings with valid underscore digit separators and small decimal exponents, and case-insensitive `inf`/`infinity`/`nan` special strings plus current `ValueError`/`TypeError` diagnostics for invalid current string/bytes/bytearray numeric inputs and explicit `int` base misuse, adapter-backed `complex()`/`complex(x)`/`complex(real, imag)` plus `complex(real=...)`, `complex(imag=...)`, `complex(x, imag=...)`, current concrete-dict `complex(**kwargs)`, and simple real-only, imaginary-only, unparenthesized real-plus-imaginary, or parenthesized `complex(str)` forms with valid underscore separators, optional surrounding Python string whitespace, and `inf`/`infinity`/`nan` special strings for current int/bool/float/complex values and numeric strings, with current `ValueError` diagnostics for malformed strings and current `TypeError` diagnostics for unsupported concrete operands, int-like/float and finite non-overflowing complex `abs(x)` plus complex infinity/NaN special cases, including special-float infinity and NaN behavior plus zero-value behavior with `TypeError` diagnostics for current concrete non-numeric operands, int-like/float `divmod(x, y)` with current `ZeroDivisionError` diagnostics for zero divisors and `TypeError` diagnostics for unsupported current operands, two-argument `pow(x, y)` including complex bases with int-like exponents, negative float bases with int-like exponents, negative real bases with integral float exponents, and the current finite positive-base real float subset with current `TypeError` diagnostics for concrete non-numeric operands, nonnegative-exponent and invertible negative-exponent three-argument modular `pow(x, y, mod)` with current `TypeError` diagnostics for non-int-like modular operands and current `ValueError` diagnostics for zero-modulus and non-invertible negative-exponent modular `pow`, current int-like/finite-float `round` with half-even behavior, concrete string/bytes/bytearray/list/tuple repetition and same-family concatenation with current invalid-repeat-count and cross-type concatenation `TypeError` diagnostics, concrete-iterable `sum(x)`/`sum(x, start)` over current numeric/list/tuple addable values with unsupported-start/item `TypeError` diagnostics, one-argument `min(x)`/`max(x)` over current concrete int-like, float-containing list/tuple, and string iterables with empty iterable `ValueError`, `default=` support, and `key=` support for current int-like and float-compatible numeric key-result profiles, and multi-argument `min(a, b, ...)`/`max(a, b, ...)` over current int-like and float-compatible numeric values with `default=` `TypeError` diagnostics and `key=` support for current int-like and float-compatible numeric key-result profiles, with current `ZeroDivisionError` diagnostics for zero-base negative integer/bool/float/complex power and `pow` exponents; adapter smoke now exercises real Python `//` and `/` spelling, bytearray numeric constructor sources, numeric constructor Unicode whitespace, numeric constructor error classes, complex constructor error classes, and matrix multiplication `@` with TypeError diagnostics for current built-in operands. Python numeric tower, general complex exponentiation with non-integer exponents, int object conversion and exact numeric-constructor diagnostic text, float object conversion and exact numeric-constructor diagnostic text, exponent overflow/underflow and decimal rounding-boundary/error behavior, NaN sign preservation, complex object conversion, complex keyword diagnostics/warnings, exact large-int/float comparison behavior, full coercions, complex abs() magnitude for overflow-scale edge cases, negative-base complex power results, and float edge/error behavior remain. |
| Truth-value testing | partial | Simple int/float/complex/string/bytes/bytearray/memoryview/list/tuple/dict/set/range/bool/None/Ellipsis truthiness, adapter-backed `bool()`/`bool(x)`, concrete-iterable `all(x)`/`any(x)` for those values, and short-circuit `and`/`or`; needs `__bool__`, `__len__`, `__iter__`, full containers, lazy iterator side effects, and user-defined objects. |
| Name binding and scope | partial | Single environment only, including simple-name assignment expressions in that current environment; needs module/function/class scopes, globals, nonlocals, comprehensions, annotation scopes. |
| Assignment | partial | Single-name assignment, adapter-backed simple-name assignment expressions, adapter-backed simple-name annotated assignment with annotation expressions erased in the current no-introspection profile, adapter-backed multi-target simple-name assignment, simple named list/dict subscription assignment with current list-target `IndexError`/`TypeError` diagnostics, simple named list/dict subscription augmented assignment with current pre-RHS `IndexError`/`KeyError`/`TypeError` diagnostics, and two-level simple-name-rooted list/dict subscription assignment in the current value-store profile, simple named list extended slice assignment with `ValueError` for mismatched non-unit-step replacement lengths and zero-step targets, flat sequence unpacking assignment to simple names over current string/bytes/bytearray/list/tuple/dict/set/range RHS values with current arity `ValueError` diagnostics, non-starred nested sequence unpacking assignment to simple names over current string/bytes/bytearray/list/tuple/dict/set/range RHS values with current top-level arity `ValueError` diagnostics, starred and nested starred sequence unpacking assignment with simple-name starred captures over current string/bytes/bytearray/list/tuple/dict/set/range RHS values and top-level starred-too-few `ValueError` diagnostics, and simple-name augmented assignment for current integer-like operators, true/floor division, and current `@=` TypeError diagnostics, with bytes/bytearray yielding integers, dictionaries yielding keys, and sets using the current internal set iteration order profile; needs general iterable unpacking, attributes, subscription assignment beyond current simple/two-level name-rooted list/dict value-store targets, subscript target diagnostics outside the current simple named list/dict subset, heap object identity/aliasing, annotation metadata/scopes, in-place/special-method dispatch, recursive unpacking diagnostics with Python's partial left-to-right binding behavior, non-name starred targets, and full target evaluation order. |
| Calls and functions | partial | Positional-argument lambda values and function definitions, positional lambda/function defaults evaluated at lambda/function definition time, function/lambda varargs parameters including suffix defaults and keyword binding for fixed parameters, function/lambda `*args` plus keyword-only signatures including sparse keyword-only defaults and kwargs collection, function/lambda `*args, **kwargs` signatures including suffix defaults, function/lambda `**kwargs` parameters including suffix defaults and without keyword-only parameters, function/lambda positional-only parameters including suffix defaults, varargs-only, kwargs-only, keyword-only, keyword-only-plus-kwargs, varargs-plus-kwargs, varargs-plus-keyword-only, and varargs-plus-keyword-only-plus-kwargs combinations, function/lambda keyword-only parameters without positional parameters including sparse defaults and kwargs collection, mixed positional-plus-keyword-only definitions including sparse keyword-only defaults and kwargs collection, adapter-backed function parameter/return annotations erased in the current no-introspection profile, adapter-backed decorators for current function-signature families with optional suffix and sparse keyword-only defaults, top-to-bottom decorator-expression evaluation, definition-time default evaluation before decorator calls, and bottom-to-top application, bare and value returns, recursive calls, zero/multi-positional calls, keyword-only calls without positional arguments, mixed positional/keyword calls, starred positional call unpacking over current ordered concrete string/bytes/bytearray/list/tuple/dict/set/range values, with strings itemized by Unicode code point, bytes/bytearray yielding integers and sets using the current internal set iteration order profile, keyword argument unpacking over current concrete dictionary values with string keys, and current wrong positional arity TypeError diagnostics for ordinary one-, zero-, and multi-positional function/lambda calls; needs closures/cells, annotation metadata/scopes, type parameters, methods, remaining argument-binding diagnostics, and real frames. |
| Containers and comprehensions | partial | Minimal string/bytes/bytearray/list/tuple/dict/set value displays, adapter-backed expression evaluation for list/tuple/dict/set displays including starred list/tuple/set display unpacking over current ordered concrete string/bytes/bytearray/list/tuple/dict/set/range values, with sets using the current internal set iteration order profile, non-async one-or-more-generator list comprehensions over current concrete iterables with simple-name targets or current recursive sequence targets, zero or more filter clauses on each generator, and restored outer binding, non-async one-or-more-generator dict comprehensions over current concrete iterables with simple-name targets or current recursive sequence targets, zero or more filter clauses on each generator, duplicate-key replacement, and restored outer binding, non-async one-or-more-generator set comprehensions over current concrete iterables with simple-name targets or current recursive sequence targets, zero or more filter clauses on each generator, duplicate-element normalization, and restored outer binding, dictionary display unpacking over current concrete dict values, no-argument `list()`, `tuple()`, `dict()`, `set()`, `bytes()`, and `bytearray()` constructors, `list(x)`/`tuple(x)` over current ordered string/bytes/bytearray/list/tuple/dict/set/range/memoryview values, with strings itemized by Unicode code point, `dict(x)` over current dict values and concrete list/tuple pair iterables, `dict(key=value)`/`dict(**d)`/`dict(x, key=value, **d)` over current simple keyword arguments and current concrete-dict keyword unpacking with TypeError diagnostics for duplicate evaluated keyword names, `set(x)` over current string/bytes/bytearray/list/tuple/dict/set/range/memoryview values, `bytes(source)` over current int-like lengths, existing bytes/bytearray values, current memoryview values over bytes/bytearray, bytes(string, encoding[, errors]) for current utf-8, ascii, and latin-1 string encoding with strict, ignore, replace, backslashreplace, and xmlcharrefreplace error handling plus current lazy handler lookup for encodable strings, and concrete byte-int list/tuple/dict-key/set/range iterables, `bytearray(source)`/`bytearray(string, encoding[, errors])` over the same current source and encoding profile, simple-name `bytearray.append(x)`/`bytearray.extend(x)`/`bytearray.clear()`/`bytearray.copy()`/`bytearray.reverse()`/`bytearray.pop([index])`/`bytearray.remove(value)`/`bytearray.insert(index, value)`/`bytearray.count(sub)`/`bytearray.index(sub)`/`bytearray.find(sub)`/`bytearray.rfind(sub)`/`bytearray.rindex(sub)`/`bytearray.startswith(prefix)`/`bytearray.endswith(suffix)`/`bytearray.expandtabs([tabsize])`/`bytearray.hex([sep[, bytes_per_sep]])`/`bytearray.removeprefix(prefix)`/`bytearray.removesuffix(suffix)`/`bytearray.replace(old, new[, count])`/`bytearray.split([sep[, maxsplit]])`/`bytearray.rsplit([sep[, maxsplit]])`/`bytearray.splitlines([keepends])`/`bytearray.partition(sep)`/`bytearray.rpartition(sep)`/`bytearray.isascii()`/`bytearray.isdigit()`/`bytearray.isalpha()`/`bytearray.isalnum()`/`bytearray.islower()`/`bytearray.isupper()`/`bytearray.isspace()`/`bytearray.istitle()`/`bytearray.lower()`/`bytearray.upper()`/`bytearray.capitalize()`/`bytearray.swapcase()`/`bytearray.title()`/`bytearray.center(width[, fillbyte])`/`bytearray.ljust(width[, fillbyte])`/`bytearray.rjust(width[, fillbyte])`/`bytearray.lstrip([bytes])`/`bytearray.rstrip([bytes])`/`bytearray.strip([bytes])`/`bytearray.zfill(width)` methods over current concrete bytearray values, with `None` returns for mutators, `copy()` returning a bytearray value, `pop()` returning and removing indexed byte values, `remove()` removing the first matching byte, `insert()` inserting clamped-index byte values, `count()` returning integer-byte or bytes-like subsequence occurrence counts, `index()`/`rindex()` returning first/last integer-byte or bytes-like subsequence positions, and `find()`/`rfind()` returning first/last integer-byte or bytes-like needle positions or `-1`, with optional slice-style `start`/`end` windows for all five search methods, and `startswith()`/`endswith()` returning affix booleans for bytes-like or tuple affix arguments over whole values or optional slice-style `start`/`end` windows, `expandtabs()` returning fresh bytearray values after ASCII tab expansion with LF/CR column resets, `hex()` returning lowercase hexadecimal strings with optional ASCII string/bytes separators grouped by signed `bytes_per_sep`, and `removeprefix()`/`removesuffix()` returning fresh bytearray values with a matching bytes-like affix removed, `replace()` returning fresh bytearray values after left-to-right non-overlapping bytes-like substitution with optional count, `split()`/`rsplit()` returning lists of fresh bytearray subsequences for explicit bytes-like separators or default ASCII-whitespace runs with directional `maxsplit`, `splitlines()` returning lists of fresh bytearray line segments split at ASCII LF/CR/CRLF boundaries with optional kept line endings, partition/rpartition returning three-bytearray tuples split around first/last nonempty bytes-like separators, and ASCII classification methods returning booleans over ASCII byte classes, and ASCII case-conversion methods returning fresh bytearray values, center/ljust/rjust returning width-justified fresh bytearray values with default-space padding and one-byte bytes/bytearray fill validation, strip/lstrip/rstrip returning fresh bytearray values after default ASCII-whitespace or bytes-like strip-set removal, and zfill returning width-padded fresh bytearray values with leading-sign handling, and current `TypeError`/`ValueError`/`IndexError` diagnostics, `memoryview(source)` over current bytes/bytearray values with current truthiness, `len`, `isinstance(..., memoryview)`, `for` iteration, concrete iterable builtins, list/tuple/set conversion, bytes/bytearray constructor conversion, and comprehension iteration support, duplicate dictionary key replacement in the supported key-equality subset, duplicate set element normalization through the adapter, string/list/tuple/bytes/bytearray repetition with integer-like counts plus current invalid-repeat-count `TypeError` diagnostics, string nonzero-step slicing by Unicode code point plus list/tuple/bytes/bytearray nonzero-step slicing with current zero-step `ValueError` diagnostics, slice-object subscriptions over current string/list/tuple/bytes/bytearray/range values, string substring membership, bytes/bytearray integer and subsequence membership with current ValueError diagnostics for out-of-range integer needles and TypeError diagnostics for invalid non-bytes-like needles, string iteration by Unicode code point and bytes/bytearray/memoryview iteration, lexicographic ordering, and positive/negative indexing with current out-of-range `IndexError` and invalid-index `TypeError` diagnostics, string/bytes/bytearray/list/tuple same-family concatenation with current cross-type sequence concatenation `TypeError` diagnostics, list/tuple/dict/set/bytes/bytearray equality, list/tuple/bytes/bytearray lexicographic ordering, set-to-set ordering comparisons, membership, truthiness, positive/negative indexing with current out-of-range `IndexError` and invalid-index `TypeError` diagnostics, simple named list subscription mutation/deletion with current out-of-range `IndexError` and invalid-index `TypeError` diagnostics, dict key membership, dict key lookup with current missing-key `KeyError` and unsupported-key `TypeError` diagnostics, simple named dict subscription augmented assignment/deletion with current missing-key `KeyError` and unsupported-key `TypeError` diagnostics, dict key iteration, set iteration in current `for` loops using the current internal set iteration order profile, and adapter-backed `range(stop)`/`range(start, stop)`/`range(start, stop, step)` values for `for` loops, equality, integer-like and float-equality membership, positive/negative indexing with current out-of-range `IndexError` and invalid-index `TypeError` diagnostics, nonzero-step slicing, current zero-step `ValueError` diagnostics, and `len`; direct K smoke uses trailing-comma input while adapter smoke accepts ordinary Python displays for the supported subset. Needs broader constructor arguments/iterable conversion, broader bytes constructor encodings, remaining namereplace/custom error handlers, buffer behavior, mapping protocol, bytes methods and remaining bytearray mutation/methods beyond current simple-name  plus full memoryview behavior, async list comprehensions, async dict comprehensions, async set comprehensions, generator comprehensions, nonempty set unpacking/order profiles, mutation beyond the current simple subscript/slice/bytearray append/extend/clear/copy/reverse/pop/remove/insert/count/index value-store subset, broader slice object behavior, repeated-list mutable-element aliasing behavior, subscription IndexError/KeyError/TypeError behavior outside the current concrete expression/simple mutation subset, full `__index__` and hash protocol behavior, dictionary views/iterator mutation behavior, full range object behavior, iterators, and full comprehensions. |
| Attribute access and descriptors | partial | Adapter-backed `range.start`, `range.stop`, `range.step`, `slice.start`, `slice.stop`, `slice.step`, numeric `real`/`imag`, and int-like `numerator`/`denominator` attribute references exist for current concrete range, slice, int/bool/float, and complex values, plus adapter-backed `getattr`/`hasattr` with literal and evaluated string names over that current attribute profile, including known missing-attribute behavior and non-string-name `TypeError`. Needs `object.__getattribute__`, arbitrary attribute names, descriptors, method binding, special method lookup, user objects, modules, classes, and full attribute diagnostics. |
| Classes and metaclasses | not-started | Needs class body execution, namespace, MRO, descriptors, metaclass protocol. |
| Exceptions | partial | Adapter-backed `raise`, `raise Name`, `raise Name(args...)`, `raise Name from None`, and `raise Name from Cause` plus one or more named, tuple-named, or bare `except` handlers exist over internal exception values, including constructor-call syntax with evaluated positional arguments, active-exception tracking for bare re-raise, `RuntimeError` sentinel raising when no active exception exists, `except as` binding/cleanup, normal try-body completion, first-match handler order, current Python 3.14 built-in exception hierarchy name matching for internal sentinels plus CPython's runtime-only `_IncompleteInputError` sentinel including tuple handlers, nested mismatching-handler propagation, bare catch-all handlers, `try` `else`, `finally` execution/override for active exceptions, and combined `try`/`except`/`else`/`finally` composition in the current handler subset. `from None` and `from Cause` are preserved as distinct K constructs but have no observable cause/context object in the current exception-value profile. Needs real exception objects and hierarchy, keyword/non-name exception construction, traceback, propagation through all continuations, full active-exception/traceback/context behavior, cause/context attachment beyond construct preservation, groups, `except*`, and user-defined type-hierarchy matching. |
| Control flow | partial | Adapter-backed `if`, `while`, string/bytes/bytearray/list/tuple/dict/set/range/memoryview `for`, flat, non-starred nested, starred, and nested starred unpacking `for` targets, loop `else`, one-or-more-manager `with memoryview(bytes-or-bytearray)` and `with ... as target` over current simple-name, sequence, and starred sequence targets with left-to-right entry, nested/reverse exit, body normal completion, body exception propagation, pre-entry exception propagation, target arity failure propagation after exit, and return/break/continue propagation through the current non-suppressing exit profile, normal-completion and return/break/continue/exception-propagating `try/finally`, including finalizer override of pending exceptions by return/break/continue/new exception, named/tuple/bare `try/except` handler lists plus `else` and `finally` over exception sentinels, `break`, `continue`, and function bare/value `return` subsets are covered, along with `pass`, current non-optimized `assert` success/failure control flow, and expression-level conditionals, with bytes/bytearray/memoryview yielding integers, dictionaries yielding keys, sets using the current internal set iteration order profile, and current concrete item unpacking in the supported target profiles; needs general iteration, real exceptions, `yield`, general context-manager protocol, exception suppression, user-defined/library context managers, and async control flow. |
| Import system | not-started | Needs module cache, find/load protocol, packages, relative imports, `__main__`. |
| Pattern matching | partial | Basic adapter-backed `match` supports value patterns over current expression values, singleton `True`/`False`/`None` patterns with identity-style matching, wildcard patterns, simple capture patterns that bind the subject in the current environment, OR patterns over current non-binding subpatterns and current capture-bearing sequence/mapping/builtin-class alternatives with first-matching-alternative binding, AS patterns over current non-binding subpatterns and current capture-bearing sequence/mapping/builtin-class subpatterns that bind subpattern captures and then the alias after a successful subpattern match, zero-argument builtin class patterns over current concrete bool/int/float/complex/str/bytes/list/tuple/dict/set/range/slice values including CPython-compatible bool-as-int matching, single-positional builtin class patterns for current bool/bytes/dict/float/int/list/set/str/tuple whole-object matching with non-binding or capture-bearing subpatterns, keyword and positional-plus-keyword builtin class patterns over numeric `real`/`imag`, int-like `numerator`/`denominator`, and range/slice `start`/`stop`/`step` attributes with non-binding or capture-bearing nested attribute subpatterns, fixed-length and starred sequence patterns over current concrete list/tuple values with non-binding or capture-bearing nested element/prefix/suffix patterns, `*name` binding for top-level and nested starred sequence patterns, mapping patterns over current concrete dicts with constant keys and non-binding or capture-bearing nested value patterns, `**rest` binding for top-level and nested mapping patterns, CPython-compatible no-leak behavior for failed capture-bearing compound subpatterns, guarded cases over the current pattern subset with guard-false fallthrough, guard exception propagation, and current CPython-compatible top-level/OR-selected/AS/nested sequence/starred-sequence/mapping/rest/builtin-class capture visibility after false/raising guards, ordered case selection, and single subject evaluation. Needs OR and AS patterns beyond current sequence/mapping/builtin-class capture-bearing alternatives, class patterns beyond current builtin whole-object captures, general sequence protocol matching beyond current concrete list/tuple values, broader mapping protocol behavior, full class patterns including name_or_attr evaluation/shadowing, user classes, keyword subpatterns beyond the current builtin attribute subset, general positional subpatterns via `__match_args__`, duplicate keyword diagnostics, and TypeError behavior, group patterns/provenance if needed, binding rollback for broader refutable subpatterns, broader value-pattern evaluation, irrefutability diagnostics, and PEP 634 underspecification triage. |
| Coroutines and async | not-started | Needs awaitables, async functions/generators, async with/for. |
| Annotations and type syntax | not-started | Needs deferred annotations, type parameters, `type` statement, annotation scopes. |
| Standard library interactions | partial | Internal `range(stop)`/`range(start, stop)`/`range(start, stop, step)` loop, equality, integer-like and float-equality membership, indexing, nonzero-step slicing, current zero-step `ValueError` diagnostics, truthiness, length, and `start`/`stop`/`step` attribute support exist; `len`, `bool`, current scalar/string-escape/Unicode-ascii/bytes/bytearray/special-float/container/range/slice `str`/`repr`/`ascii`, `format` over the current string-conversion domain with literal or evaluated format-spec expressions over the current supported subset, including current string `s`/default width/alignment/precision subset with sign/alternate/`=` alignment, `z` option, and incompatible numeric presentation-type `ValueError` diagnostics and current int-like `b`/`c`/`d`/`o`/`x`/`X` sign/alternate/width/alignment/zero-padding/grouping subset with integer-precision, `z` option, string-presentation-type, locale-independent `n` grouping/precision/`z`, signed-`c`, and alternate-`c` `ValueError` diagnostics plus special float-value presentation rendering for `e`/`E`/`f`/`F`/`g`/`G`/`n`/`%` and default non-empty numeric specs over `inf`/`-inf`/`nan` with sign, `z`, `#`, precision-ignored special payloads, fill/alignment, zero-padding, width, width and fractional comma/underscore grouping for non-`n` float presentation types, grouped-`n` `ValueError` diagnostics, and missing-precision `ValueError` diagnostics, `chr` over current int-like code points, concrete-iterable `all`/`any`, `sorted` over current concrete comparable iterables including memoryview over bytes/bytearray with fresh list results, reverse= truthiness, key= callable sorting over current order-comparable key results, and stable equal-key ordering, `sum` over current numeric/list/tuple addable values and memoryview byte values with unsupported-start/item `TypeError` diagnostics, int-like, memoryview byte, float-containing list/tuple, and string one-argument `min`/`max` over Unicode code-point items with empty iterable `ValueError`, `default=` support, and `key=` support for current int-like and float-compatible numeric key-result profiles, int-like and float-compatible numeric multi-argument `min`/`max` with `default=` `TypeError` diagnostics and `key=` support for current int-like and float-compatible numeric key-result profiles, int-like and CPython-profile Unicode-whitespace-padded string plus ASCII-whitespace-padded bytes/bytearray/base `int` with valid underscore digit separators, matching base prefixes, and base-0 autodetection, numeric `real`/`imag` attributes, int-like `numerator`/`denominator` attributes, no-argument numeric `conjugate()`, numeric, simple signed ASCII integer-string, finite decimal-point string, small-exponent string, and case-insensitive `inf`/`infinity`/`nan` special string `float` with CPython-profile Unicode numeric whitespace plus ASCII-whitespace-padded bytes/bytearray float forms and valid underscore digit separators, `abs`, `divmod`, two-argument `pow` including complex bases with int-like exponents, negative float bases with int-like exponents, and negative real bases with integral float exponents, with current `TypeError` diagnostics for concrete non-numeric operands, nonnegative-exponent and invertible negative-exponent three-argument modular `pow` with current `TypeError` diagnostics for non-int-like modular operands and current `ValueError` diagnostics for zero-modulus and non-invertible negative-exponent modular `pow`, and `round` over current int-like values plus finite floats in the current concrete scaling subset with half-even behavior, current special-float `OverflowError`/`ValueError` diagnostics for integer-converting forms, and special-float preservation for integer `ndigits`, adapter-backed complex literal values with equality/truthiness and `+`/`-`/`*`/nonzero `/` plus current `ZeroDivisionError` diagnostics for zero complex divisors and integer-like `**`, adapter-backed `complex()` construction including `real`/`imag` keyword arguments, current concrete-dict keyword unpacking, and simple real-only, imaginary-only, unparenthesized real-plus-imaginary, or parenthesized strings over current numeric values, adapter-backed `Ellipsis` literal and shadowable builtins-name fallback, and adapter-backed `__debug__` as `True` in the current non-optimized profile work for current concrete values including bytes/bytearray integer iteration, with current `ZeroDivisionError` diagnostics for zero-base negative two-argument integer/bool/float/complex power and `pow` exponents; no-argument `list()`, `tuple()`, `dict()`, `set()`, `bytes()`, and `bytearray()` constructors build current empty concrete values, `list(x)`/`tuple(x)` convert current ordered concrete string/bytes/bytearray/list/tuple/dict/set/range/memoryview values, with sets using the current internal set iteration order profile, `dict(x)` copies current dict values or consumes current list/tuple pair iterables, `dict(key=value)`/`dict(**d)`/`dict(x, key=value, **d)` consumes current simple keyword arguments and current concrete-dict keyword unpacking with TypeError diagnostics for duplicate evaluated keyword names, `set(x)` consumes current concrete iterables including memoryview with duplicate normalization, `bytes(source)` consumes current int-like lengths, existing bytes/bytearray values, current memoryview values over bytes/bytearray, bytes(string, encoding[, errors]) for current utf-8, ascii, and latin-1 string encoding with strict, ignore, replace, backslashreplace, and xmlcharrefreplace error handling plus current lazy handler lookup for encodable strings, and concrete byte-int list/tuple/dict-key/set/range iterables with TypeError/ValueError diagnostics, and `bytearray(source)`/`bytearray(string, encoding[, errors])` consumes the same current source and encoding profile while preserving bytearray result type for read-only binary sequence operations, simple-name `bytearray.append(x)`/`bytearray.extend(x)`/`bytearray.clear()`/`bytearray.copy()`/`bytearray.reverse()`/`bytearray.pop([index])`/`bytearray.remove(value)`/`bytearray.insert(index, value)`/`bytearray.count(sub)`/`bytearray.index(sub)`/`bytearray.find(sub)`/`bytearray.rfind(sub)`/`bytearray.rindex(sub)`/`bytearray.startswith(prefix)`/`bytearray.endswith(suffix)`/`bytearray.expandtabs([tabsize])`/`bytearray.hex([sep[, bytes_per_sep]])`/`bytearray.removeprefix(prefix)`/`bytearray.removesuffix(suffix)`/`bytearray.replace(old, new[, count])`/`bytearray.split([sep[, maxsplit]])`/`bytearray.rsplit([sep[, maxsplit]])`/`bytearray.splitlines([keepends])`/`bytearray.partition(sep)`/`bytearray.rpartition(sep)`/`bytearray.isascii()`/`bytearray.isdigit()`/`bytearray.isalpha()`/`bytearray.isalnum()`/`bytearray.islower()`/`bytearray.isupper()`/`bytearray.isspace()`/`bytearray.istitle()`/`bytearray.lower()`/`bytearray.upper()`/`bytearray.capitalize()`/`bytearray.swapcase()`/`bytearray.title()`/`bytearray.center(width[, fillbyte])`/`bytearray.ljust(width[, fillbyte])`/`bytearray.rjust(width[, fillbyte])`/`bytearray.lstrip([bytes])`/`bytearray.rstrip([bytes])`/`bytearray.strip([bytes])`/`bytearray.zfill(width)` methods cover current concrete bytearray values, with `None` returns for mutators, `copy()` returning a bytearray value, `pop()` returning and removing indexed byte values, `remove()` removing the first matching byte, `insert()` inserting clamped-index byte values, `count()` returning integer-byte or bytes-like subsequence occurrence counts, `index()`/`rindex()` returning first/last integer-byte or bytes-like subsequence positions, and `find()`/`rfind()` returning first/last integer-byte or bytes-like needle positions or `-1`, with optional slice-style `start`/`end` windows for all five search methods, and `startswith()`/`endswith()` returning affix booleans for bytes-like or tuple affix arguments over whole values or optional slice-style `start`/`end` windows, `expandtabs()` returning fresh bytearray values after ASCII tab expansion with LF/CR column resets, `hex()` returning lowercase hexadecimal strings with optional ASCII string/bytes separators grouped by signed `bytes_per_sep`, and `removeprefix()`/`removesuffix()` returning fresh bytearray values with a matching bytes-like affix removed, `replace()` returning fresh bytearray values after left-to-right non-overlapping bytes-like substitution with optional count, `split()`/`rsplit()` returning lists of fresh bytearray subsequences for explicit bytes-like separators or default ASCII-whitespace runs with directional `maxsplit`, `splitlines()` returning lists of fresh bytearray line segments split at ASCII LF/CR/CRLF boundaries with optional kept line endings, partition/rpartition returning three-bytearray tuples split around first/last nonempty bytes-like separators, and ASCII classification methods returning booleans over ASCII byte classes, and ASCII case-conversion methods returning fresh bytearray values, center/ljust/rjust returning width-justified fresh bytearray values with default-space padding and one-byte bytes/bytearray fill validation, strip/lstrip/rstrip returning fresh bytearray values after default ASCII-whitespace or bytes-like strip-set removal, and zfill returning width-padded fresh bytearray values with leading-sign handling, and current `TypeError`/`ValueError`/`IndexError` diagnostics; `memoryview(source)` over current bytes/bytearray values participates in the concrete iteration/list/tuple/set/sorted/all/any/sum/min/max profile over yielded integer bytes. Needs staged builtins/core library coverage, broader constructor arguments/iterable conversion, mapping protocol, broader bytes constructor encodings, remaining namereplace/custom error handlers, buffer behavior, bytes methods and buffer protocol behavior, int object conversion and exact numeric-constructor diagnostic text, float object conversion and exact numeric-constructor diagnostic text, remaining bytearray mutation/methods beyond current simple-name , exponent overflow/underflow and decimal rounding-boundary/error behavior, broader range object behavior, general complex exponentiation with non-integer exponents and object construction, complex keyword diagnostics/warnings, general method binding, general builtins namespace binding beyond the `Ellipsis` singleton, optimization profile behavior for `-O`, native parser coverage for control-character string escapes, finite float presentation rendering, integer formatting through floating-point presentation types, finite float and complex string conversion, recursive cycle handling, general `__bool__`/`__len__`/`__iter__`/`__next__`/`__int__`/`__float__`/`__abs__`/`__divmod__`/`__pow__`, and host profile boundaries. |
| CPython profile | not-started | Needs implementation notes, diagnostics, limits, and `Lib/test` classification. |

## Completeness Rule

Do not mark a construct complete merely because CPython accepts a test. Completion requires matching the official docs, a K rule story, and an explicit decision about CPython-specific behavior.
