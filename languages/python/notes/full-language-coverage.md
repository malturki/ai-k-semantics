# Python Full-Language Coverage Map

This file is the completeness ledger for Python 3.14.6. A construct is not considered complete until it has:

- direct syntax in K or a documented reason for a faithful desugaring
- semantic rules linked to the official docs
- local positive and negative tests
- CPython differential coverage, unless classified as intentionally non-CPython
- known ambiguity/divergence notes where relevant

Status values:

- `not-started`: no K syntax or semantics yet
- `syntax`: syntax exists, semantics incomplete
- `partial`: executable semantics exists for a documented subset
- `profile`: only covered in an implementation/profile module
- `complete`: docs-linked semantics and tests are in place

## Source Chapters

| Source area | Reference | Status | Notes |
| --- | --- | --- | --- |
| Lexical analysis | Language Reference, chapter 2 | partial | Initial kernel uses semicolon-separated simple statements with trailing semicolons and parses integers, simple K strings, `True`, `False`, `None`, and the adapter-backed `...` atom and bytes literal constants. Full source encoding, physical/logical lines, indentation, comments, token classes, f/t-string details, direct bytes parser syntax and escape diagnostics, and numeric literal edge cases remain. |
| Data model | Language Reference, chapter 3 | partial | Initial kernel has minimal integers, strings, bytes, list/tuple/dict/set values, booleans, `None`, and `Ellipsis`. Full object identity, real bool-as-int subtype behavior, type hierarchy, attributes, descriptors, special methods, GC/lifetime notes, coroutines, and async generators remain. |
| Execution model | Language Reference, chapter 4 | partial | Initial kernel has a single environment. Full code blocks, frames, binding, scopes, exceptions, annotation scopes, and runtime components remain. |
| Import system | Language Reference, chapter 5 | not-started | Needs controlled module table and host profile. |
| Expressions | Language Reference, chapter 6 | partial | Initial kernel covers integer/string/bytes/list/tuple/dict/set/bool/None/Ellipsis atoms, names, parentheses, expression-backed list/tuple/dict/set displays through the adapter including starred unpacking over current ordered concrete iterables, unary `+`/`-`/`~`, arithmetic and bitwise integer operators, string concatenation/equality/order, string/list/tuple/bytes sequence repetition with integer-like counts, string/list/tuple/bytes nonzero-step slicing, adapter-backed slice objects with start/stop/step attributes and slice-object subscriptions over current string/list/tuple/bytes/range values, string substring membership and bytes integer membership plus indexing, list/tuple same-type concatenation, bytes same-type concatenation, list/tuple/dict/set/bytes equality, list/tuple/bytes lexicographic ordering, set-to-set ordering comparisons, list/tuple membership/indexing, dict key membership/lookup, set membership, range equality/int-like and float-equality membership/indexing/nonzero-step slicing/start-stop-step attributes, singleton identity comparisons, chained comparisons over supported operators, truthiness for simple values, short-circuit `and`/`or`, `not`, conditional expressions, simple-name assignment expressions, positional-argument lambdas/calls including suffix defaults, positional-only lambdas including suffix defaults, varargs-only, kwargs-only, keyword-only, keyword-only-plus-kwargs, varargs-plus-kwargs, varargs-plus-keyword-only, and varargs-plus-keyword-only-plus-kwargs combinations, varargs parameters including suffix defaults and keyword binding for fixed parameters, `*args` plus keyword-only lambda signatures including suffix and sparse keyword-only defaults and kwargs collection, `*args, **kwargs` lambda signatures including suffix defaults, keyword-only parameters in the no-positional subset including suffix defaults and kwargs collection, mixed positional-plus-keyword-only parameters including suffix defaults and kwargs collection, and starred positional call unpacking over current ordered concrete iterables including bytes, and a binary comparison subset. All other expression constructs remain. |
| Simple statements | Language Reference, chapter 7 | partial | Initial kernel covers expression statements, `pass`, truthy `assert` including optional messages that are not evaluated on success, module-scope single-name and multi-name `global`, simple assignment, simple-name annotated assignment in the current no-introspection profile, flat, non-starred nested, starred, and nested starred sequence unpacking assignment over current string/bytes/list/tuple/dict/set/range RHS values, simple-name `+=`/`-=`/`*=`, and simple-name/multi-target simple-name `del`, with bytes yielding integers, dictionaries yielding keys, and sets using the current internal set iteration order profile. Remaining statements are not started. |
| Compound statements | Language Reference, chapter 8 | partial | Adapter-backed `if`, `while`, string/bytes/list/tuple/dict/set/range `for`, flat, non-starred nested, starred, and nested starred unpacking `for` targets, normal-completion and return/break/continue-propagating `try/finally`, including `break`/`continue` issued directly by `finally` bodies, positional-parameter `def` subsets, and decorators on current function-signature families with optional suffix and sparse keyword-only defaults exist, including `break`, `continue`, bare/value `return` propagation, and loop `else` for the current while/for subsets, with bytes yielding integers, dictionaries yielding keys, and sets using the current internal set iteration order profile, and with unpacking support for current concrete string/bytes/list/tuple/dict/set/range item values in the supported target profiles. General iteration, `except`, `try` `else`, exceptions, `finally` over exceptions, `with`, `match`, full function objects/frames, classes, coroutines, type parameter lists, annotations remain. |
| Top-level components | Language Reference, chapter 9 | syntax | Initial kernel parses a small file-input subset. Interactive/eval modes remain. |
| Full grammar | Language Reference, chapter 10 | syntax | A first construct-preserving CPython-AST adapter exists for the current executable subset, including ordinary `//`, list displays, and tuple displays. The full grammar must still be mirrored construct-by-construct or covered by documented parser front ends that preserve construct identity. |
| Built-ins and core library | Standard Library reference | partial | Internal `range`, `len`, `bool`, `all`, `any`, int-like `sum`, nonempty int-like one-argument `min`/`max`, int-like multi-argument `min`/`max`, `int`, `abs`, `divmod`, two-argument `pow`, and nonnegative-exponent three-argument modular `pow` subsets exist, plus the adapter-backed `Ellipsis` singleton, no-argument `list()`, `tuple()`, `dict()`, and `set()` constructors, ordered-iterable `list(x)`/`tuple(x)` for current concrete values, including bytes and set values through the current internal set iteration order profile, `dict(x)` for current dict values and list/tuple pair iterables, `dict(key=value)`/`dict(**d)`/`dict(x, key=value, **d)` for simple keyword arguments and current concrete-dict keyword unpacking, and `set(x)` for current concrete iterables including bytes; no full builtins namespace or general object protocol yet. |

## Construct Families

| Family | Status | Required next evidence |
| --- | --- | --- |
| Source encodings and tokenizer behavior | not-started | CPython tokenizer cases and docs-linked K lexer/token model. |
| Indentation and block structure | not-started | Parser tests for `INDENT`/`DEDENT`, mixed tabs/spaces diagnostics, blank/comment-only lines. |
| Names, keywords, soft keywords | partial | Initial `Id` handling only; needs Python identifier Unicode rules and keyword/soft-keyword separation. |
| Literals | partial | Int, simple finite float, simple string, adapter-backed bytes constants, bool, None, and adapter-backed ellipsis atom smoke coverage; direct bytes parser syntax and diagnostics, full float lexical edge cases, imaginary numbers, full Python string prefixes/escapes, f-strings, t-strings remain. |
| Object model | partial | Minimal value model with singleton identity only; needs heap objects, general identity, type hierarchy, attributes, descriptors. |
| Numeric operations | partial | Int/bool unary `+`/`-`/`~`, `+`, `-`, `*`, `/`, `%`, `//`, shifts, bitwise operators, `**`, float and mixed int/bool/float `+`, `-`, `*`, `/`, `//`, `%`, `**`, ordering comparisons, adapter-backed `int()`/`int(x)`, adapter-backed `float()`/`float(x)` for current int-like/float values, int-like/float `abs(x)`, int-like/float `divmod(x, y)`, two-argument `pow(x, y)` including the current finite positive-base real float subset, nonnegative-exponent three-argument modular `pow(x, y, mod)`, concrete-iterable `sum(x)`/`sum(x, start)`, one-argument `min(x)`/`max(x)` over current nonempty concrete int-like iterables, and multi-argument `min(a, b, ...)`/`max(a, b, ...)` over current int-like values, with negative integer exponents requiring a nonzero base and producing floats; adapter smoke now exercises real Python `//` and `/` spelling. Python numeric tower, zero-base negative-exponent diagnostics, modular inverse for negative-exponent three-argument pow, string/base int conversion, float string/object conversion, exact large-int/float comparison behavior, full coercions, non-int sum starts/items, negative-base complex power results, float edge/error behavior, key/default min/max, and error behavior remain. |
| Truth-value testing | partial | Simple int/float/string/bytes/list/tuple/dict/set/range/bool/None/Ellipsis truthiness, adapter-backed `bool()`/`bool(x)`, concrete-iterable `all(x)`/`any(x)` for those values, and short-circuit `and`/`or`; needs `__bool__`, `__len__`, `__iter__`, full containers, lazy iterator side effects, and user-defined objects. |
| Name binding and scope | partial | Single environment only, including simple-name assignment expressions in that current environment; needs module/function/class scopes, globals, nonlocals, comprehensions, annotation scopes. |
| Assignment | partial | Single-name assignment, adapter-backed simple-name assignment expressions, adapter-backed simple-name annotated assignment with annotation expressions erased in the current no-introspection profile, adapter-backed multi-target simple-name assignment, flat sequence unpacking assignment to simple names over current string/bytes/list/tuple/dict/set/range RHS values, non-starred nested sequence unpacking assignment to simple names over current string/bytes/list/tuple/dict/set/range RHS values, starred and nested starred sequence unpacking assignment with simple-name starred captures over current string/bytes/list/tuple/dict/set/range RHS values, and simple-name augmented assignment for current integer-like operators plus true division, with bytes yielding integers, dictionaries yielding keys, and sets using the current internal set iteration order profile; needs general iterable unpacking, attributes, subscriptions, annotation metadata/scopes, `@=`, in-place/special-method dispatch, unpacking diagnostics, non-name starred targets, and full target evaluation order. |
| Calls and functions | partial | Positional-argument lambda values and function definitions, positional lambda/function defaults evaluated at lambda/function definition time, function/lambda varargs parameters including suffix defaults and keyword binding for fixed parameters, function/lambda `*args` plus keyword-only signatures including sparse keyword-only defaults and kwargs collection, function/lambda `*args, **kwargs` signatures including suffix defaults, function/lambda `**kwargs` parameters including suffix defaults and without keyword-only parameters, function/lambda positional-only parameters including suffix defaults, varargs-only, kwargs-only, keyword-only, keyword-only-plus-kwargs, varargs-plus-kwargs, varargs-plus-keyword-only, and varargs-plus-keyword-only-plus-kwargs combinations, function/lambda keyword-only parameters without positional parameters including sparse defaults and kwargs collection, mixed positional-plus-keyword-only definitions including sparse keyword-only defaults and kwargs collection, adapter-backed function parameter/return annotations erased in the current no-introspection profile, adapter-backed decorators for current function-signature families with optional suffix and sparse keyword-only defaults, top-to-bottom decorator-expression evaluation, definition-time default evaluation before decorator calls, and bottom-to-top application, bare and value returns, recursive calls, zero/multi-positional calls, keyword-only calls without positional arguments, mixed positional/keyword calls, starred positional call unpacking over current ordered concrete string/bytes/list/tuple/dict/set/range values, with bytes yielding integers and sets using the current internal set iteration order profile, and keyword argument unpacking over current concrete dictionary values with string keys; needs closures/cells, annotation metadata/scopes, type parameters, methods, argument-binding diagnostics, and real frames. |
| Containers and comprehensions | partial | Minimal string/bytes/list/tuple/dict/set value displays, adapter-backed expression evaluation for list/tuple/dict/set displays including starred list/tuple/set display unpacking over current ordered concrete string/bytes/list/tuple/dict/set/range values, with sets using the current internal set iteration order profile, one-generator list comprehensions over current concrete iterables with simple-name targets, zero or more filter clauses, and restored outer binding plus no-filter one-generator list comprehensions with current recursive sequence targets, two-generator list comprehensions over current concrete iterables with simple-name targets, zero or more filter clauses on each generator, and restored outer binding, three-generator list comprehensions over current concrete iterables with simple-name targets, zero or more filter clauses on each generator, and restored outer binding, one-generator dict comprehensions over current concrete iterables with simple-name targets, zero or more filter clauses, duplicate-key replacement, and restored outer binding plus no-filter one-generator dict comprehensions with current recursive sequence targets, two-generator dict comprehensions over current concrete iterables with simple-name targets, zero or more filter clauses on each generator, duplicate-key replacement, and restored outer binding, three-generator dict comprehensions over current concrete iterables with simple-name targets, zero or more filter clauses on each generator, duplicate-key replacement, and restored outer binding, one-generator set comprehensions over current concrete iterables with simple-name targets, zero or more filter clauses, duplicate-element normalization, and restored outer binding plus no-filter one-generator set comprehensions with current recursive sequence targets, two-generator set comprehensions over current concrete iterables with simple-name targets, zero or more filter clauses on each generator, duplicate-element normalization, and restored outer binding, three-generator set comprehensions over current concrete iterables with simple-name targets, zero or more filter clauses on each generator, duplicate-element normalization, and restored outer binding, dictionary display unpacking over current concrete dict values, no-argument `list()`, `tuple()`, `dict()`, and `set()` constructors, `list(x)`/`tuple(x)` over current ordered string/bytes/list/tuple/dict/set/range values, `dict(x)` over current dict values and concrete list/tuple pair iterables, `dict(key=value)`/`dict(**d)`/`dict(x, key=value, **d)` over current simple keyword arguments and current concrete-dict keyword unpacking, `set(x)` over current string/bytes/list/tuple/dict/set/range values, duplicate dictionary key replacement in the supported key-equality subset, duplicate set element normalization through the adapter, string/list/tuple/bytes repetition with integer-like counts, string/list/tuple/bytes nonzero-step slicing, slice-object subscriptions over current string/list/tuple/bytes/range values, string substring membership, bytes integer membership, string and bytes iteration, lexicographic ordering, and positive/negative indexing, list/tuple/bytes same-type concatenation, list/tuple/dict/set/bytes equality, list/tuple/bytes lexicographic ordering, set-to-set ordering comparisons, membership, truthiness, positive/negative indexing, dict key membership, dict key lookup, dict key iteration, set iteration in current `for` loops using the current internal set iteration order profile, and adapter-backed `range(stop)`/`range(start, stop)`/`range(start, stop, step)` values for `for` loops, equality, integer-like and float-equality membership, positive/negative indexing, nonzero-step slicing, and `len`; direct K smoke uses trailing-comma input while adapter smoke accepts ordinary Python displays for the supported subset. Needs broader constructor arguments/iterable conversion, mapping protocol, dictionary-constructor duplicate-keyword diagnostics, bytes-subsequence membership, bytes methods and bytearray/memoryview behavior, filtered and multi-generator unpacking comprehension targets, more-than-three/async list comprehensions, more-than-three/async dict comprehensions, more-than-three/async set comprehensions, generator comprehensions, nonempty set unpacking/order profiles, mutation, zero-step slice diagnostics, broader slice object behavior, non-integer repetition diagnostics, repeated-list mutable-element aliasing behavior, cross-type concatenation diagnostics, out-of-range IndexError behavior, dictionary views/iterator mutation behavior, full range object behavior, iterators, and full comprehensions. |
| Attribute access and descriptors | partial | Adapter-backed `range.start`, `range.stop`, and `range.step` attribute references exist for current internal range values. Needs `object.__getattribute__`, descriptors, method binding, special method lookup, user objects, modules, classes, and full attribute diagnostics. |
| Classes and metaclasses | not-started | Needs class body execution, namespace, MRO, descriptors, metaclass protocol. |
| Exceptions | not-started | Needs exception objects, raise/propagation, chaining, groups, handlers, finally. |
| Control flow | partial | Adapter-backed `if`, `while`, string/bytes/list/tuple/dict/set/range `for`, flat, non-starred nested, starred, and nested starred unpacking `for` targets, loop `else`, normal-completion and return/break/continue-propagating `try/finally`, including `finally`-body `break`/`continue`, `break`, `continue`, and function bare/value `return` subsets are covered, along with `pass`, truthy `assert` with success-path optional messages, and expression-level conditionals, with bytes yielding integers, dictionaries yielding keys, sets using the current internal set iteration order profile, and current concrete item unpacking in the supported target profiles; needs general iteration, exceptions, finally-over-exceptions, `yield`, context managers. |
| Import system | not-started | Needs module cache, find/load protocol, packages, relative imports, `__main__`. |
| Pattern matching | not-started | Needs PEP 634/635/636 plus current docs mapping. |
| Coroutines and async | not-started | Needs awaitables, async functions/generators, async with/for. |
| Annotations and type syntax | not-started | Needs deferred annotations, type parameters, `type` statement, annotation scopes. |
| Standard library interactions | partial | Internal `range(stop)`/`range(start, stop)`/`range(start, stop, step)` loop, equality, integer-like and float-equality membership, indexing, nonzero-step slicing, truthiness, length, and `start`/`stop`/`step` attribute support exist; `len`, `bool`, concrete-iterable `all`/`any`, int-like `sum`, nonempty int-like one-argument `min`/`max`, int-like multi-argument `min`/`max`, int-like `int`, `abs`, `divmod`, two-argument `pow`, nonnegative-exponent three-argument modular `pow`, adapter-backed `Ellipsis`, and adapter-backed `__debug__` as `True` in the current non-optimized profile work for current concrete values including bytes integer iteration, with negative two-argument `pow` exponents requiring a nonzero base; no-argument `list()`, `tuple()`, `dict()`, and `set()` constructors build current empty concrete values, `list(x)`/`tuple(x)` convert current ordered concrete string/bytes/list/tuple/dict/set/range values, with sets using the current internal set iteration order profile, `dict(x)` copies current dict values or consumes current list/tuple pair iterables, `dict(key=value)`/`dict(**d)`/`dict(x, key=value, **d)` consumes current simple keyword arguments and current concrete-dict keyword unpacking, and `set(x)` consumes current concrete iterables with duplicate normalization. Needs staged builtins/core library coverage, broader constructor arguments/iterable conversion, mapping protocol, dictionary-constructor duplicate-keyword diagnostics, bytes methods and buffer protocol behavior, string/base int conversion, broader range object behavior, zero-step diagnostics, builtins namespace binding for the assignable `Ellipsis` name, optimization profile behavior for `-O`, general `__bool__`/`__len__`/`__iter__`/`__next__`/`__int__`/`__abs__`/`__divmod__`/`__pow__`, modular inverse for negative-exponent three-argument pow, non-int sum starts/items, key/default min/max, and host profile boundaries. |
| CPython profile | not-started | Needs implementation notes, diagnostics, limits, and `Lib/test` classification. |

## Completeness Rule

Do not mark a construct complete merely because CPython accepts a test. Completion requires matching the official docs, a K rule story, and an explicit decision about CPython-specific behavior.
