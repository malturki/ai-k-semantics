# Python Semantics

K definitions live here.

Current entry points:

```sh
kompile python.k --main-module PYTHON --syntax-module PYTHON-SYNTAX
krun ../tests/examples/simple.py
```

The base modules should model portable Python semantics from the official docs. CPython-specific behavior belongs in a profile module.

## Current Slice

`python.k` is an executable seed, not a full semantics. It currently covers:

- semicolon-separated simple statements with a trailing semicolon
- expression statements
- `pass`
- single-name assignment
- adapter-backed flat sequence unpacking assignment to simple names over current string/list/tuple/dict/set/range RHS values
- adapter-backed non-starred nested sequence unpacking assignment to simple names over current string/list/tuple/dict/set/range RHS values
- adapter-backed starred sequence unpacking assignment, including nested starred sequence targets with simple-name starred captures, over current string/list/tuple/dict/set/range RHS values, with dictionaries yielding keys and sets using the current internal set iteration order profile
- integer literals, simple finite float literals, and adapter-backed imaginary/complex literals
- adapter-backed `int()` and `int(x)` for the current int-like subset plus simple digit strings with CPython-profile Unicode numeric whitespace and bytes/bytearray with optional surrounding ASCII whitespace, optional sign, optional valid underscore digit separators, explicit bases 2 through 36, matching base prefixes, and base-0 autodetection
- adapter-backed `float()` and `float(x)` for the current int-like/float subset plus simple signed ASCII integer strings with CPython-profile Unicode numeric whitespace and ASCII-whitespace-padded bytes/bytearray with optional valid underscore digit separators, finite decimal-point strings with valid underscore digit separators, finite exponent strings with valid underscore digit separators and small decimal exponents, and case-insensitive `inf`/`infinity`/`nan` special strings plus current `ValueError`/`TypeError` diagnostics for invalid current string/bytes/bytearray numeric inputs and explicit `int` base misuse
- adapter-backed `complex()`, `complex(x)`, and `complex(real, imag)` for the current int/bool/float/complex subset plus simple string construction and current `ValueError`/`TypeError` diagnostics for invalid strings and unsupported concrete operands
- adapter-backed `abs(x)` for the current int-like/float subset
- adapter-backed `divmod(x, y)` for the current int-like subset
- adapter-backed `pow(x, y)` for the current int-like subset, with negative exponents requiring a nonzero base and producing floats
- adapter-backed `pow(x, y, mod)` for current int-like values with nonnegative exponents and nonzero modulus
- adapter-backed true division `/` for the current int-like subset, producing float values
- `True`, `False`, `None`, adapter-backed ellipsis `...` and shadowable builtins-name `Ellipsis` as the `Ellipsis` singleton, and adapter-backed `__debug__` as `True` in the current non-optimized profile
- simple string literals as K `String` tokens
- adapter-backed bytes literals as integer byte-sequence values, with truthiness, equality, lexicographic ordering, concatenation, repetition, positive/negative indexing, nonzero-step slicing, integer-byte and bytes-subsequence membership, `len`, `list`/`tuple` conversion, starred display/call unpacking, assignment and `for`-target unpacking, simple `for` loops, and `all`/`any`/`sum`/`min`/`max` over yielded integer bytes
- adapter-backed `bytes.maketrans`, `bytearray.maketrans`, `bytes.translate`, and `bytearray.translate` over current concrete bytes-like values, including deletion-before-translation behavior, 256-byte table validation, `None` delete-only translation, keyword `delete=`, and bytes-vs-bytearray result types
- adapter-backed `bytes`/`bytearray` `%` formatting for the current literal `%%`, `%b`, `%s`, `%a`, `%r`, `%c`, `%d`, `%i`, `%u`, `%o`, `%x`, and `%X` conversion subset, with literal and dynamic `*` precision plus literal and dynamic `*` minimum field width for `%b`/`%s`/`%a`/`%r`/`%c`, nonnumeric conversion flags with `-` left-adjustment and other flags ignored for `%b`/`%s`/`%a`/`%r`/`%c`, numeric conversion flags, literal and dynamic `*` numeric width and precision, ignored `h`/`l`/`L` length modifiers, concrete-dict mapping-key lookup, zero padding, alternate octal/hex forms, tuple and single-argument consumption, bool-as-int formatting, finite-float truncation for decimal conversions, bytearray result preservation, and current TypeError/KeyError/OverflowError/ValueError diagnostics
- name lookup in a single environment, including current unbound-name `NameError` behavior
- unary `+`, unary `-`, and integer/bool `+`, `-`, and `*`
- integer/bool `%`, `//`, `/`, shifts, bitwise operators, bitwise inversion, and exponentiation
- float and mixed int/bool/float `+`, `-`, `*`, `/`, `//`, `%`, `**`, `divmod`, `pow`, ordering comparisons, and `abs`
- adapter-backed complex values from imaginary literals, with truth-value testing, equality against complex/int/bool/float zero-imaginary values, unary `+`/`-`, complex addition/subtraction/multiplication/nonzero true division with current int/bool/float/complex values, and integer-like exponentiation for complex bases
- string concatenation with `+`, repetition with integer/bool `*`, nonzero-step slicing, substring membership, lexicographic ordering, and positive/negative integer indexing
- truth-value testing for integers, floats, complex values, booleans, `None`, and `Ellipsis`
- truth-value testing for strings
- adapter-backed `bool()` and `bool(x)` for the current truth-value subset
- adapter-backed `format(value)`, `format(value, "")`, and `format(value, spec)` for literal or evaluated string specs in the current supported string/int/special-float format subset, including non-string spec `TypeError` and argument-evaluation-order exception propagation
- adapter-backed `all(x)` and `any(x)` for current concrete string/container/range/memoryview values
- adapter-backed `sorted(x)`, `sorted(x, reverse=expr)`, `sorted(x, key=expr)`, and combined `key=`/`reverse=` forms for current concrete string/bytes/bytearray/list/tuple/dict-key/set/range/memoryview iterables with current ordering-comparison support, current order-comparable key results, reverse truthiness, stable equal-key ordering, and fresh list results
- adapter-backed `sum(x)` and `sum(x, start)` for current concrete int-like container/range/memoryview values
- adapter-backed one-argument `min(x)` and `max(x)` for current nonempty concrete int-like container/range/memoryview values, plus multi-argument `min(a, b, ...)` and `max(a, b, ...)` for current int-like values
- short-circuiting `and` and `or`
- `not`
- binary comparisons for int-like values and equality over integers, booleans, `None`, and `Ellipsis`
- string equality
- singleton identity comparisons with `is` and `is not` for `None`, `True`, `False`, and `Ellipsis`
- adapter-backed chained comparisons over the currently supported comparison operators, with short-circuiting
- conditional expressions
- adapter-backed assignment expressions `NAME := expr` for simple-name targets in the current environment
- adapter-backed simple-name annotated assignment in the current no-introspection profile, with value assignments evaluated and annotation-only statements leaving the current binding unchanged
- simple-name `+=`, `-=`, `*=`, `/=`, `%=`, `**=`, `//=`, `<<=`, `>>=`, `&=`, `^=`, and `|=`
- multi-target simple-name assignment through the AST adapter
- simple-name `del` and adapter-backed multi-target `del` for simple names
- truthy `assert`, including adapter-backed optional assertion messages that are not evaluated on success
- module-scope single-name and multi-name `global` declarations as no-ops
- list literals in a trailing-comma value-element subset
- adapter-backed list displays with supported expression elements, starred unpacking over current ordered concrete iterables, and non-async one-or-more-generator list comprehensions over current concrete iterables with simple-name targets or current recursive sequence targets and zero or more filter clauses on each generator
- adapter-backed arbitrary-many non-async list comprehensions materialize supported live iterator references at outer and nested generator sources before recursive traversal, preserving one-shot iterator consumption, recursive sequence targets, and filter order in the current iterator profile
- adapter-backed non-async generator expressions over the current list-comprehension subset, represented as a distinct `#genExp(...)` expression that materializes the finite stream and returns a fresh one-shot `iteratorRef`
- list truthiness, equality, lexicographic ordering, same-type concatenation, repetition with integer/bool `*`, nonzero-step slicing, membership, and positive/negative integer indexing
- tuple literals in a trailing-comma value-element subset
- adapter-backed tuple displays with supported expression elements and starred unpacking over current ordered concrete iterables
- tuple truthiness, equality, lexicographic ordering, same-type concatenation, repetition with integer/bool `*`, nonzero-step slicing, membership, and positive/negative integer indexing
- dictionary literals in a trailing-comma key/value subset
- adapter-backed dictionary displays with supported key/value expressions, including duplicate key replacement in the supported key-equality subset
- adapter-backed non-async one-or-more-generator dictionary comprehensions over current concrete iterables with simple-name targets or current recursive sequence targets and zero or more filter clauses on each generator, with duplicate key replacement in the supported key-equality subset
- adapter-backed arbitrary-many non-async dictionary comprehensions materialize supported live iterator references at outer and nested generator sources before recursive traversal, preserving one-shot iterator consumption, recursive sequence targets, filter order, and key-before-value evaluation in the current iterator profile
- dictionary truthiness, equality, key membership, key subscription lookup, and key iteration in `for` loops
- nonempty set literals in a trailing-comma value subset
- adapter-backed set displays with supported expression elements and starred unpacking over current ordered concrete iterables, including duplicate element normalization, plus empty `set()`
- adapter-backed non-async one-or-more-generator set comprehensions over current concrete iterables with simple-name targets or current recursive sequence targets and zero or more filter clauses on each generator, with duplicate element normalization
- adapter-backed arbitrary-many non-async set comprehensions materialize supported live iterator references at outer and nested generator sources before recursive traversal, preserving one-shot iterator consumption, recursive sequence targets, filter order, and duplicate element normalization in the current iterator profile
- set truthiness, equality, set-to-set ordering comparisons, membership, and length
- adapter-backed no-argument `list()`, `tuple()`, `dict()`, and `set()` constructors as explicit internal constructor expressions
- adapter-backed `list(x)` and `tuple(x)` for current ordered concrete string/bytes/list/tuple/dict/set/range values, with sets using the current internal set iteration order profile, with dictionaries yielding keys
- adapter-backed `dict(x)` for current concrete dictionaries and current concrete list/tuple iterables of length-two list/tuple pairs, plus `dict(key=value)`, `dict(**d)`, and `dict(x, key=value, **d)` over current simple keyword arguments and current concrete-dict keyword unpacking
- adapter-backed `set(x)` for current concrete string/bytes/list/tuple/dict/set/range values, with dictionaries yielding keys and duplicate elements normalized
- single-argument `lambda` expressions
- single-positional-argument calls to lambda closure values
- adapter-backed zero- and multi-positional-argument calls, keyword-only calls, mixed positional/keyword calls, starred positional call arguments over current ordered concrete iterables, functions, and lambdas, plus current non-callable and ordinary positional wrong-arity `TypeError` behavior after argument evaluation
- adapter-backed keyword argument unpacking over current concrete dictionary values with string keys
- adapter-backed positional lambda default values, evaluated when the lambda expression is evaluated
- adapter-backed positional function default values, evaluated at function definition time
- adapter-backed function and lambda varargs parameters, including suffix defaults for fixed parameters, keyword-only parameters with sparse defaults, and `*args, **kwargs` combinations with optional suffix defaults, collecting extra positional arguments into a tuple
- adapter-backed function and lambda `**kwargs` parameters in the no-keyword-only subset, including suffix defaults for fixed parameters, collecting extra keyword arguments into a dictionary keyed by strings
- adapter-backed function and lambda positional-only parameters, including suffix defaults, varargs-only, kwargs-only, keyword-only, keyword-only-plus-kwargs, varargs-plus-kwargs, varargs-plus-keyword-only, and varargs-plus-keyword-only-plus-kwargs combinations
- adapter-backed function and lambda keyword-only parameters in the no-positional subset, including sparse defaults and `**kwargs` collection
- adapter-backed function and lambda mixed positional-plus-keyword-only parameters including sparse keyword-only defaults and `**kwargs` collection
- adapter-backed function parameter and return annotations in the current no-introspection profile, erased as lazy metadata while preserving call behavior and default-value evaluation
- adapter-backed function decorators for zero/multi-positional, positional-only, varargs, keyword-only, and kwargs function signatures with optional positional suffix defaults and sparse keyword-only defaults, evaluating decorator expressions top to bottom, evaluating defaults next, applying decorator callables bottom to top, and binding the final result to the function name
- adapter-backed simple generator functions for undecorated ordinary positional functions whose bodies are only plain `yield`/`yield expr` statements, represented as distinct `#genDef`/`#yield` internal forms that materialize yielded values and return fresh one-shot `iteratorRef` values
- adapter-backed user class definitions for pass-only bodies, simple `NAME = expr` class-namespace assignments, simple undecorated ordinary-positional method definitions including current `@staticmethod`/`@classmethod`/`@property`/`@name.getter`/`@name.setter`/`@name.deleter`, one simple user-class base, and decorators over those current class profiles, represented as distinct `#class(Name)`/`#classAttrs(Name, ...)`/`#classMethod(...)`/`#classStaticMethod(...)`/`#classClassMethod(...)`/`#classProperty(...)`/`#classPropertyGetter(...)`/`#classPropertySetter(...)`/`#classPropertyDeleter(...)`/`#classBase(Name, Base, ...)`/`#classDecorated(...)`/`#classBaseDecorated(...)` internal statements that bind fresh class values or the final decorator result, support class truthiness/callability, construction of fresh user object values with current `__init__` dispatch including inherited `__init__` in the single-base profile, per-object instance attributes from `self.x = expr`-style assignment and current `del obj.attr`, current identity/default equality including class-name rebinding to a fresh class object, direct class lookup, instance lookup with class fallback, inherited class attributes and methods, bound methods, method overriding, staticmethod unwrapping, classmethod owner-class binding, same-name class-body property getter/setter/deleter chaining, property getter invocation on instance lookup and `getattr`, property setter invocation or read-only `AttributeError` on instance assignment, property deleter invocation or read-only `AttributeError` on attribute deletion, decorator expression evaluation/application order, decorator replacement results, `getattr`/`hasattr`, dynamic `isinstance(obj, ClassName)` checks, and dynamic `issubclass(ClassName, BaseName)` checks for current user classes
- an internal `#floorDiv(E1, E2)` parser bridge form emitted by the AST adapter
- adapter-backed `if` statements with optional `else`
- adapter-backed `while` loops with `break`, `continue`, and loop `else`
- adapter-backed `for` loops over the current string/bytes/bytearray/list/tuple/dict/set/range/memoryview value subset, including `break`, `continue`, and `return` propagation, with bytes/bytearray/memoryview yielding integers, dictionaries yielding keys, and sets using the current internal set iteration order profile
- adapter-backed flat, non-starred nested, starred, and nested starred sequence unpacking targets in `for` loops over current concrete string/bytes/bytearray/list/tuple/dict/set/range outer iterables and item values
- adapter-backed flat, nested, and starred sequence unpacking targets in `for` loops over current stateful iterator refs, including `for ... else`, `break` preservation of unconsumed items, and `continue`/`return`/exception propagation
- adapter-backed `while`/`for` loop `else` clauses for the current loop subsets
- adapter-backed one-or-more-manager `with` statements over the current `memoryview(bytes-or-bytearray)` context-manager profile, with optional simple-name, sequence, and starred sequence `as` targets plus abrupt-completion propagation
- adapter-backed `try/finally`, `try/except`, `try/except/else`, and combined `try/except/else/finally` subsets over current internal exception sentinels, including handler order, `except as` cleanup, bare and tuple handlers, current Python 3.14 built-in hierarchy name matching for sentinels, and finalizer override of pending abrupt completions
- adapter-backed `range(stop)`, `range(start, stop)`, and `range(start, stop, step)` values in `for` loops
- adapter-backed range truthiness, equality, integer-like membership, indexing, nonzero-step slicing, length, and `start`/`stop`/`step` attributes
- adapter-backed `len(...)` for strings, bytes, lists, tuples, dictionaries, sets, and current range values
- adapter-backed `iter(map(...))` and `iter(filter(...))` over current finite concrete map/filter values, yielding stateful iterator refs consumable by `next`, `next(default)`, `list`, `tuple`, and `for`
- adapter-backed `getattr` and `hasattr` over the current range/slice/numeric attribute profile, including literal and evaluated string attribute names, missing-attribute behavior, non-string-name `TypeError`, and `getattr` default-argument evaluation order
- adapter-backed single-parameter `def` functions, `return` with and without an expression, return-expression exception propagation, fallthrough to `None`, and recursive calls in the single-environment subset

The coverage ledger in `../notes/full-language-coverage.md` is the source of truth for what remains.

Current parser caveat: the K frontend treats `//` in direct K input files as a comment before it can be parsed as Python floor division. The `floorDivExp` syntax and semantics are present, and `harness/python_to_k_input.py` now translates real Python `//` nodes to the internal `#floorDiv(E1, E2)` form for adapter smoke tests.

Current container caveat: direct concrete list, tuple, dict, and set smoke tests use Python-valid trailing commas, such as `[1, 2,]`, `(1, 2,)`, `{"x": 1,}`, and `{1, 2,}`, because the first executable K container grammar avoids ambiguities caused by un-delimited element productions. The AST adapter now accepts ordinary Python list, tuple, dict, set displays, starred list/tuple/set display unpacking over current ordered concrete string/list/tuple/dict/set/range values, with sets using the current internal set iteration order profile, non-async one-or-more-generator list comprehensions over current concrete iterables with a simple-name target or current recursive sequence target and zero or more filter clauses on each generator, non-async one-or-more-generator dict comprehensions over current concrete iterables with a simple-name target or current recursive sequence target and zero or more filter clauses on each generator, non-async one-or-more-generator set comprehensions over current concrete iterables with a simple-name target or current recursive sequence target and zero or more filter clauses on each generator, non-async generator expressions over the current comprehension subset as materialized one-shot iterator refs, dictionary display unpacking over current concrete dict values, no-argument `list()`, `tuple()`, `dict()`, and `set()` constructors, `list(x)`/`tuple(x)` over current ordered concrete string/bytes/list/tuple/dict/set/range values, with sets using the current internal set iteration order profile, `dict(x)` over current concrete dict values and current concrete list/tuple pair iterables, `dict(key=value)`, `dict(**d)`, and `dict(x, key=value, **d)` over current simple keyword arguments and current concrete-dict keyword unpacking, and `set(x)` over current concrete string/bytes/list/tuple/dict/set/range values, plus adapter-backed `slice(stop)`/`slice(start, stop)`/`slice(start, stop, step)` objects with `start`/`stop`/`step` attributes and slice-object subscriptions over current string/bytes/list/tuple/range values for the supported expression subset and emits explicit internal forms. Full displays still need mutation, async list comprehensions, async dict comprehensions, async set comprehensions, lazy generator object/frame semantics, nonempty set unpacking/order profiles, zero-step slice diagnostics, non-integer repetition diagnostics, mutable-element aliasing behavior for repeated lists, cross-type ordering/concatenation diagnostics, hashability/error behavior, and complete error behavior.

Current compound-statement caveat: the K parser does not yet accept Python indentation syntax directly. The AST adapter emits internal `#if`, `#while`, `#whileElse`, `#for`, `#forElse`, unpacking-`for`, `#with`, `#withAs`, `#withAsTarget`, `#withMany`, `#tryFinally`, `#tryExcept`, `#tryExceptElse`, `#tryExceptCases`, `#tryExceptCasesElse`, `#def`, `#defDecorated`, `#class`, `#classAttrs`, `#classBase`, `#classDecorated`, and `#classBaseDecorated` statements with explicit blocks for the supported subset. `for` iteration is defined only for current concrete string/bytes/bytearray/list/tuple/dict/set/range/memoryview values and current stateful iterator refs produced by adapter-backed `iter(...)`, with bytes/bytearray/memoryview yielding integers, dictionaries iterating over keys, and sets using the current internal set iteration order profile; supported unpacking loop targets bind current concrete item values and current iterator-ref item values left to right. `with` currently supports one or more `memoryview(bytes-or-bytearray)` context managers with optional simple-name, sequence, and starred sequence `as` targets, left-to-right entry, nested/reverse exit through the current non-suppressing exit profile, body exception propagation, pre-entry exception propagation, target arity failure propagation after exit, and return/break/continue propagation through exit. `try/finally` currently runs the `finally` block after normal completion and when `return`, `break`, `continue`, or exception completion leaves the `try` block; abrupt completion from the `finally` block overrides pending `return`/`break`/`continue`/exception completion in the current subset. `try/except` supports named, tuple, bare, and `except as` handlers over current internal exception sentinels, with first-match ordering, else clauses, and current Python 3.14 built-in hierarchy name matching plus CPython's runtime-only `_IncompleteInputError` for sentinels. Class statements currently cover pass-only class definitions, simple class-body name assignments, simple undecorated ordinary-positional method definitions including current `__init__`, `@staticmethod`, `@classmethod`, `@property`, simple `@name.getter`, simple `@name.setter`, and simple `@name.deleter`, one simple user-class base with inherited class attributes/methods and dynamic user-class `isinstance`/`issubclass`, and decorators over those current class profiles with top-to-bottom expression evaluation, bottom-to-top application, and final-result binding; multiple bases, builtin bases, full MRO, keywords/metaclass, type parameters, arbitrary class-body statements, method defaults/varargs/keyword-only/positional-only signatures, method decorators beyond staticmethod/classmethod/property, broader descriptor behavior, property constructor calls and property method chaining beyond the current same-name class-body decorator profile, and full namespace mapping behavior remain unsupported. Arbitrary/user iterator protocol objects, general context-manager protocol, full classes, type-parameterized functions, real exception objects/groups, user-defined exception classes, `except*`, and async compound statements remain unsupported.

Current range caveat: adapter-backed `range` support is an internal `rangeVal` subset used by `for` loops, `len`, equality, truthiness, integer-like and float-equality containment, indexing, nonzero-step slicing, and `start`/`stop`/`step` attribute access, including positive and negative integer steps. Zero-step `ValueError`, broader user-object containment fallback, out-of-range `IndexError`, broader range object behavior, and the general iterator protocol remain unsupported.

Current attribute caveat: adapter-backed `getattr` and `hasattr` evaluate object and attribute-name expressions in Python argument order and support the current concrete range, slice, int/bool/float, complex, and user-class class/instance-attribute profile for literal or evaluated string names. User class attributes are limited to values captured from simple class-body name assignments and simple method definitions, with single-base user-class inheritance appending inherited class attributes after child attributes so child definitions override base definitions; current user objects store explicit instance attributes in `<objectAttrs>`, consult them before class fallback, bind class-stored function values as simple bound methods, unwrap current `staticmethod` values, bind current `classmethod` values to the owner class/subclass, invoke current property getters including same-name class-body getter replacement, route current property setters or read-only property `AttributeError` through attribute assignment, route current property deleters or read-only property `AttributeError` through `del obj.attr`, and support current user-object instance attribute deletion with class fallback. Full descriptor protocol, property constructor calls and property method chaining beyond the current same-name class-body decorator profile, exact property identity/introspection, modules, multiple inheritance, full MRO, class attribute mutation/deletion, broader instance deletion, arbitrary protocol dispatch, and full `AttributeError` object details remain unsupported.

Current builtin caveat: `len` is defined only for the current concrete string/bytes/container/range/memoryview values, `bool` is defined only through the current truth-value subset, `format` is defined for the current string-conversion domain with empty specs and for literal or evaluated string specs in the current supported string/int/special-float format subset, `all` and `any` are defined only for current concrete string/bytes/container/range/memoryview values after the argument value is materialized, `sorted` is defined only for current concrete string/bytes/bytearray/list/tuple/dict-key/set/range/memoryview iterables with the current ordering-comparison subset, `reverse` truthiness, and `key` callable sorting over current order-comparable key results, `sum` is defined only for current concrete int-like bytes/container/range/memoryview values with optional int-like `start`, one-argument `min` and `max` are defined only for current nonempty concrete int-like bytes/container/range/memoryview values, multi-argument `min` and `max` are defined only for current int-like argument values, `int` is defined only for no argument, current int-like values, and simple digit strings with CPython-profile Unicode numeric whitespace and bytes/bytearray with optional surrounding ASCII whitespace, optional sign, optional valid underscore digit separators, explicit bases 2 through 36, matching base prefixes, and base-0 autodetection, `float` is defined only for no argument, current int-like/float values, simple signed ASCII integer strings with CPython-profile Unicode numeric whitespace and ASCII-whitespace-padded bytes/bytearray with optional valid underscore digit separators, finite decimal-point strings with valid underscore digit separators, finite exponent strings with valid underscore digit separators and decimal exponents from -9 through 9, and case-insensitive `inf`/`infinity`/`nan` special strings plus current `ValueError`/`TypeError` diagnostics for invalid current string/bytes/bytearray numeric inputs and explicit `int` base misuse, `complex` construction is defined for no argument, current int/bool/float/complex values, supported string forms, supported real/imag keyword forms, and current `ValueError`/`TypeError` diagnostics for malformed strings and unsupported concrete operands, `abs` is defined over current int-like and float values, `divmod` is defined over current int-like/float values, two-argument `pow` is defined over current int-like values, complex bases with int-like exponents, and the finite positive-base real float subset, three-argument modular `pow` is defined only for current int-like values with nonnegative exponents and nonzero modulus, with negative two-argument integer and complex `pow` exponents requiring a nonzero base, and complex values currently cover adapter-backed imaginary literals plus equality, truthiness, unary `+`/`-`, addition/subtraction/multiplication, nonzero true division, and integer-like exponentiation. `__debug__` is defined as `True` for the current non-optimized profile, the builtins name `Ellipsis` has a narrow shadowable fallback to the singleton, and the current container type constructors cover no-argument `list()`, `tuple()`, `dict()`, and `set()`, one-argument `list(x)`/`tuple(x)` over current ordered concrete iterables including bytes, bytearray, memoryview, and set values through the current internal set iteration order profile, `dict(x)` over current dict values and list/tuple pair iterables, `dict(key=value)`/`dict(**d)`/`dict(x, key=value, **d)` over current simple keyword arguments and current concrete-dict keyword unpacking, one-argument `set(x)` over current concrete iterables including bytes, bytearray, and memoryview, `bytes(source)` and `bytearray(source)` over current memoryview values backed by bytes/bytearray, simple-name `bytearray.append(x)`/`bytearray.extend(x)`/`bytearray.clear()`/`bytearray.copy()`/`bytearray.reverse()`/`bytearray.pop([index])`/`bytearray.remove(value)`/`bytearray.insert(index, value)`/`bytearray.count(sub)`/`bytearray.index(sub)`/`bytearray.find(sub)`/`bytearray.rfind(sub)`/`bytearray.rindex(sub)`/`bytearray.startswith(prefix)`/`bytearray.endswith(suffix)`/`bytearray.removeprefix(prefix)`/`bytearray.removesuffix(suffix)`/`bytearray.split([sep[, maxsplit]])`/`bytearray.rsplit([sep[, maxsplit]])`/`bytearray.partition(sep)`/`bytearray.rpartition(sep)`/`bytearray.isascii()`/`bytearray.isdigit()`/`bytearray.isalpha()`/`bytearray.isalnum()`/`bytearray.islower()`/`bytearray.isupper()`/`bytearray.isspace()`/`bytearray.istitle()`/`bytearray.lower()`/`bytearray.upper()`/`bytearray.capitalize()`/`bytearray.swapcase()`/`bytearray.title()`/`bytearray.center(width[, fillbyte])`/`bytearray.ljust(width[, fillbyte])`/`bytearray.rjust(width[, fillbyte])`/`bytearray.lstrip([bytes])`/`bytearray.rstrip([bytes])`/`bytearray.strip([bytes])`/`bytearray.zfill(width)` methods over current concrete bytearray values, with `None` returns for mutators, `copy()` returning a bytearray value, and `pop()` returning and removing indexed byte values, `remove()` removing the first matching byte, `insert()` inserting clamped-index byte values, `count()` returning integer-byte or bytes-like subsequence occurrence counts, `index()`/`rindex()` returning first/last integer-byte or bytes-like subsequence positions, and `find()`/`rfind()` returning first/last integer-byte or bytes-like needle positions or `-1`, with optional slice-style `start`/`end` windows for all five search methods, and `startswith()`/`endswith()` returning affix booleans for bytes-like or tuple affix arguments, and `removeprefix()`/`removesuffix()` returning fresh bytearray values with a matching bytes-like affix removed, `split()`/`rsplit()` returning lists of fresh bytearray subsequences for explicit bytes-like separators or default ASCII-whitespace runs with directional `maxsplit`, partition/rpartition returning three-bytearray tuples split around first/last nonempty bytes-like separators, and ASCII classification methods returning booleans over ASCII byte classes, and ASCII case-conversion methods returning fresh bytearray values, center/ljust/rjust returning width-justified fresh bytearray values with default-space padding and one-byte bytes/bytearray fill validation, strip/lstrip/rstrip returning fresh bytearray values after default ASCII-whitespace or bytes-like strip-set removal, and zfill returning width-padded fresh bytearray values with leading-sign handling, `memoryview(x)` over current bytes/bytearray values with `isinstance(..., memoryview)` support plus iteration and bytes/bytearray constructor conversion through the current integer byte profile, and `slice(stop)`/`slice(start, stop)`/`slice(start, stop, step)` objects with `start`/`stop`/`step` attributes and slice-object subscriptions over current string/bytes/list/tuple/range values. General builtins namespace lookup beyond this named singleton, optimization profile behavior for `-O`, broader constructor arguments, mapping protocol, dictionary-constructor duplicate-keyword diagnostics, set iteration order profiles, full format-spec mini-language and `__format__` dispatch, broader `sorted` lazy iterable side effects and object-protocol dispatch, int object conversion and exact numeric-constructor diagnostic text, float object conversion and exact numeric-constructor diagnostic text, exponent overflow/underflow and decimal rounding-boundary/error behavior, complex object coercion and exact diagnostic text, complex zero-division diagnostics, general complex exponentiation with non-integer exponents, conjugation and negative-base complex power results, zero-base negative-exponent diagnostics, modular inverse for negative-exponent `pow`, non-int sum starts/items, empty/default/key `min`/`max`, `__bool__`/`__len__`/`__iter__`/`__next__`/`__int__`/`__float__`/`__complex__`/`__abs__`/`__divmod__`/`__pow__` dispatch, overflow/error behavior, and user-defined objects remain unsupported.

Current map/filter iterator caveat: `iter(map(...))` and `iter(filter(...))` allocate stateful iterator refs by collecting the current finite concrete map/filter stream first, then reusing the existing iterator-ref consumers. Exact map/filter object identity, direct `next(map_obj)`/`next(filter_obj)` without an explicit `iter(...)` wrapper, per-`next` callable/predicate side-effect interleaving, strict-map mismatch timing under `next(default)`, iterator inputs to map/filter, mutation-sensitive behavior, and arbitrary user iterators remain unsupported.

Current bytes caveat: adapter-backed bytes literals are represented as immutable integer byte sequences. The current executable subset covers core sequence behavior, many bytes/bytearray methods, narrow memoryview interoperation, UTF-8/ASCII/Latin-1 encode/decode profiles, `maketrans`/`translate`, and conversion-only `bytes`/`bytearray` `%` formatting for `%%`, `%b`, `%s`, `%a`, `%r`, `%c`, `%d`, `%i`, `%u`, `%o`, `%x`, and `%X`, including literal and dynamic `*` precision, literal and dynamic `*` minimum field width for `%b`/`%s`/`%a`/`%r`/`%c`, nonnumeric conversion flags with `-` left-adjustment and other flags ignored for `%b`/`%s`/`%a`/`%r`/`%c`, ignored `h`/`l`/`L` length modifiers, concrete-dict mapping-key lookup with current missing-key and incomplete-key diagnostics, and numeric flags, literal and dynamic `*` width and precision, zero padding, and alternate octal/hex forms for `%d`/`%i`/`%u`/`%o`/`%x`/`%X`. Remaining bytes and bytearray work includes unimplemented methods beyond the listed current slice, full memoryview and buffer protocol behavior, the rest of the `%` formatting mini-language (dynamic `*` specifiers with mapping keys and floating-point presentation conversions), broader decoding/encoding APIs, decode handlers beyond `strict`/`ignore`/`replace`/`backslashreplace`, and complete TypeError/ValueError diagnostics.

Current bytes-format validation caveat: the local Haskell backend currently crashes on bytes `%` float-valued paths because its runtime is missing the `FLOAT.eq` hook. The semantic rules for decimal float truncation and special-float diagnostics remain present, but concrete validation of those paths needs a backend/toolchain fix or an LLVM validation lane.

Current assignment-expression caveat: adapter-backed `NAME := expr` evaluates `expr`, binds the resulting value to `NAME` in the current single environment, and yields that value. Full named-expression support still needs concrete parser integration, grammar-position restrictions and diagnostics, comprehension scope behavior, and interaction with real module/function/class scopes.

Current annotated-assignment caveat: adapter-backed simple-name annotated assignments use internal `#annAssign` and `#annOnly` forms in the current no-introspection profile. The assigned value expression is evaluated and bound for `x: annotation = value`; annotation-only `x: annotation` leaves the current binding unchanged. Annotation expressions are erased as lazy metadata because the current environment has no `__annotations__` mapping or annotation scopes. Attribute/subscript annotated targets and annotation introspection remain unsupported.

Current unpacking caveat: flat, non-starred nested, starred, and nested starred assignment targets are adapter-backed for simple names and current concrete string/bytes/list/tuple/dict/set/range RHS values plus current stateful iterator refs, with bytes yielding integers, dictionaries yielding keys, and sets using the current internal set iteration order profile, and flat, non-starred nested, starred, and nested starred `for` targets are adapter-backed over current concrete string/bytes/list/tuple/dict/set/range outer iterables, current stateful iterator refs, and their current item values. A starred target receives a list of remaining values, possibly empty, and prefix/star/suffix name binding follows left-to-right target order recursively at each target-list level. General iterable unpacking, unpacking diagnostics, non-name starred targets, and attribute/subscript targets remain unsupported.

Current function caveat: adapter-backed `#def`, `#defArgs`, `#defDefaults`, `#defVarArgs`, `#defVarArgsDefaults`, `#defVarArgsKwOnly`, `#defVarArgsKwDefaults`, `#defVarArgsKwOnlyKwArgs`, `#defVarArgsKwDefaultsKwArgs`, `#defVarKwArgs`, `#defVarKwArgsDefaults`, `#defKwArgs`, `#defKwArgsDefaults`, `#defPosOnly`, `#defPosOnlyDefaults`, `#defPosOnlyVarArgs`, `#defPosOnlyVarArgsDefaults`, `#defPosOnlyKwArgs`, `#defPosOnlyKwArgsDefaults`, `#defPosOnlyVarKwArgs`, `#defPosOnlyVarKwArgsDefaults`, `#defPosOnlyVarArgsKwOnly`, `#defPosOnlyVarArgsKwDefaults`, `#defPosOnlyVarArgsKwOnlyKwArgs`, `#defPosOnlyVarArgsKwDefaultsKwArgs`, `#defPosOnlyKwOnly`, `#defPosOnlyKwOnlyDefaults`, `#defPosOnlyKwOnlyKwArgs`, `#defPosOnlyKwDefaultsKwArgs`, `#defKwOnly`, `#defKwDefaults`, `#defKwOnlyKwArgs`, `#defKwDefaultsKwArgs`, `#defPosKwOnly`, `#defPosKwDefaults`, `#defPosKwOnlyKwArgs`, `#defPosKwDefaultsKwArgs`, `#lambdaArgs`, `#lambdaDefaults`, `#lambdaVarArgs`, `#lambdaVarArgsDefaults`, `#lambdaVarArgsKwOnly`, `#lambdaVarArgsKwDefaults`, `#lambdaVarArgsKwOnlyKwArgs`, `#lambdaVarArgsKwDefaultsKwArgs`, `#lambdaVarKwArgs`, `#lambdaVarKwArgsDefaults`, `#lambdaKwArgs`, `#lambdaKwArgsDefaults`, `#lambdaPosOnly`, `#lambdaPosOnlyDefaults`, `#lambdaPosOnlyVarArgs`, `#lambdaPosOnlyVarArgsDefaults`, `#lambdaPosOnlyKwArgs`, `#lambdaPosOnlyKwArgsDefaults`, `#lambdaPosOnlyVarKwArgs`, `#lambdaPosOnlyVarKwArgsDefaults`, `#lambdaPosOnlyVarArgsKwOnly`, `#lambdaPosOnlyVarArgsKwDefaults`, `#lambdaPosOnlyVarArgsKwOnlyKwArgs`, `#lambdaPosOnlyVarArgsKwDefaultsKwArgs`, `#lambdaPosOnlyKwOnly`, `#lambdaPosOnlyKwOnlyDefaults`, `#lambdaPosOnlyKwOnlyKwArgs`, `#lambdaPosOnlyKwDefaultsKwArgs`, `#lambdaKwOnly`, `#lambdaKwDefaults`, `#lambdaKwOnlyKwArgs`, `#lambdaKwDefaultsKwArgs`, `#lambdaPosKwOnly`, `#lambdaPosKwDefaults`, `#lambdaPosKwOnlyKwArgs`, and `#lambdaPosKwDefaultsKwArgs` cover zero or more positional parameters, optional suffix defaults evaluated at function/lambda definition time, varargs parameters including suffix defaults and keyword binding for fixed parameters, varargs-plus-keyword-only parameters including sparse keyword-only defaults and kwargs collection, `*args, **kwargs` combinations with optional suffix defaults, `**kwargs` parameters including suffix defaults and without keyword-only parameters, positional-only parameters including suffix defaults, varargs-only, kwargs-only, keyword-only, keyword-only-plus-kwargs, varargs-plus-kwargs, varargs-plus-keyword-only, and varargs-plus-keyword-only-plus-kwargs combinations, keyword-only parameters without positional parameters including sparse defaults and kwargs collection, mixed positional-plus-keyword-only parameters including sparse keyword-only defaults and kwargs collection, keyword-only calls without positional arguments, mixed positional/keyword calls, starred positional call unpacking over current ordered concrete string/bytes/list/tuple/dict/set/range values, with bytes yielding integers and sets using the current internal set iteration order profile, keyword argument unpacking over current concrete dictionary values with string keys, current wrong positional arity `TypeError` behavior for ordinary one-, zero-, and multi-positional function/lambda calls, bare and value-return statements, parameter/return annotations erased in the current no-introspection profile, adapter-backed decorators for the same current function-signature families, and an environment-restore model. Adapter-backed `#genDef` covers only undecorated simple generator functions with ordinary positional parameters and top-level plain `yield` statements; it materializes yielded values at call time into a fresh iterator ref. Full Python function objects and generators need real frames, cells/closures, globals/nonlocals, annotation metadata and annotation scopes, type parameters, descriptors, methods, and remaining argument-binding diagnostics, lazy generator suspension/resumption, `send`/`throw`/`close`, `yield from`, return values carried by `StopIteration`, and generator object identity/introspection.

Current augmented-assignment caveat: matrix-multiplication `@=` remains unsupported because there is no matrix protocol, and augmented assignment is otherwise limited to simple-name targets in the current value/operator subsets rather than full in-place/special-method dispatch.

Current identity caveat: `is` and `is not` are defined for the language singletons `None`, `True`, `False`, and `Ellipsis`, and for the current user object profile allocated by class calls. The adapter maps the ordinary Python `...` atom to the internal `Ellipsis` singleton and models the assignable builtins name `Ellipsis` as a narrow shadowable fallback. General object identity, object lifetime, and broader heap aliasing require a fuller heap/object model and remain unsupported.
