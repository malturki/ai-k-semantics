# SQL Conformance Tests

Conformance tests for the base semantics must cite SQL:2023 standard sections or an explicitly documented local interpretation.

No current official public SQL:2023 conformance suite has been pinned yet. Until one is selected, local tests should be small, section-scoped cases that exercise one construct family at a time.
